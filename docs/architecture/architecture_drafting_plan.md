# 아키텍처 설계 초안 작성 계획

- 문서 목적: 시스템 아키텍처 초안 작성 전 범위, 산출물, 진행 순서, 현재 상태를 계획 문서로 관리한다.
- 범위: 아키텍처 초안 작성 준비와 초안 작성 진행 현황
- 대상 독자: 아키텍트, 기획자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-06
- 관련 문서: `docs/architecture/README.md`, `docs/requirements/system_srs.md`, `docs/overview/role_based_ux_direction.md`

## 문서 위치

- 위키 홈: [../README.md](../README.md)
- 아키텍처 위키: [./README.md](./README.md)
- 후속 초안 문서: [./system_architecture_draft.md](./system_architecture_draft.md)
- 분리 초안 문서: [./system_context_and_integration_draft.md](./system_context_and_integration_draft.md), [./domain_model_draft.md](./domain_model_draft.md), [./domain_entity_definition_draft.md](./domain_entity_definition_draft.md), [./application_and_governance_architecture_draft.md](./application_and_governance_architecture_draft.md)

## 1. 작업 목적

현재 `CRS`, `SRS`, 역할 기반 UX, 프로토타입은 준비되어 있으나 이를 구현 가능한 구조로 연결하는 아키텍처 초안이 부재하다. 본 계획 문서는 아키텍처 초안 작성 전에 설계 범위와 산출물을 고정하고, 설계 진행 중 상태를 같은 문서 안에서 갱신하기 위한 기준으로 사용한다.

## 2. 초안 작성 범위

- Phase 1 통합 관리 플랫폼의 시스템 경계 정의
- 외부 연계 계층, 내부 표준화 계층, 조회/운영 계층의 분리 원칙 정리
- 과제 중심 데이터 모델의 상위 구조 정의
- 역할 기반 UI/운영 콘솔이 어떤 서비스 경계와 연결되는지 설명
- `AI` 보조 기능, 운영 도구, 감사 요구를 수용하는 구조 초안 정리

이번 단계에서는 구현 기술 확정보다 구조와 책임 경계를 우선 정의한다.

## 3. 산출물

- 아키텍처 위키 인덱스 문서
- 시스템 아키텍처 초안 문서
- 분리된 세부 초안 문서
- 엔터티 검토용 분리 문서
- 후속 세부 설계 후보 목록

## 4. 진행 단계

| 단계 | 목표 | 상태 | 비고 |
| --- | --- | --- | --- |
| STEP-01 | 아키텍처 문서 카테고리와 계획 문서 생성 | done | 2026-04-06 완료 |
| STEP-02 | 기존 요구사항과 UX 기반 설계 입력 정리 | done | `SRS`, UX, 프로토타입 기준 최신화 반영 완료 |
| STEP-03 | 시스템 아키텍처 초안 문서 작성 시작 | in_progress | 컨텍스트, 컴포넌트, 데이터 흐름, 도메인 경계 작성 중 |
| STEP-04 | 후속 상세 설계 문서 후보와 다음 작업 정의 | in_progress | 컨텍스트/도메인/거버넌스 문서 분리 후 엔터티 초안 구체화 진행 |

## 5. 설계 판단 기준

- 과제 중심 운영을 최상위 도메인 개념으로 유지한다.
- 외부 `SCM`, `CI/CD`, 정적분석 실행 계층은 대체보다 연계를 우선한다.
- 일반 업무 UX와 관리자 운영 UX는 서비스/권한 경계에서도 분리 가능해야 한다.
- 조직/인력 원천 반영과 내부 운영 마스터를 동시에 다룰 수 있어야 한다.
- `AI` 기능은 보조 기능이며, 감사 가능성과 사람 검토 흐름을 유지해야 한다.

## 6. 진행 현황

- 2026-04-06 `docs/architecture/` 카테고리와 인덱스가 없음을 확인했고, 초안 작성 전에 문서 진입점이 필요하다고 판단했다.
- 2026-04-06 본 계획 문서를 생성해 계획 기반 진행 원칙을 수립했다.
- 2026-04-06 `SRS` 추적 기준, UX 방향 문서, 문서 위키 인덱스를 최신 상태에 맞게 보정하는 작업을 병행한다.
- 2026-04-06 아키텍처 초안 시작 문서를 생성했고, 다음 단계에서는 시스템 컨텍스트, 논리 컴포넌트, 핵심 데이터 흐름을 우선 채운다.
- 2026-04-06 과제/프로젝트, 조직/인력, 연계, 품질, 운영 통제, `AI` 보조 도메인 기준의 핵심 데이터 책임과 역할 기반 응용 서비스 경계를 초안에 추가한다.
- 2026-04-06 아키텍처 초안을 `시스템 컨텍스트/연계`, `도메인 모델`, `응용 서비스/운영 거버넌스` 문서로 분리해 이후 상세화 단위를 정리한다.
- 2026-04-06 `domain_model_draft.md` 에 핵심 엔터티 초안 표, 프로세스 모델 메타모델, 핵심 관계 초안을 추가해 이후 식별자/속성 상세화의 기준을 마련한다.
- 2026-04-06 엔터티 항목 검토와 수정 편의를 위해 `domain_entity_definition_draft.md` 를 별도 문서로 분리하고, `domain_model_draft.md` 는 도메인 경계와 관계 원칙 중심으로 단순화한다.
- 2026-04-06 다음 상세화 우선순위를 `work_item` 관계 규칙과 상태 모델 검토로 잡고, 문서 반영 전 판단 기준을 먼저 정리한다.
- 2026-04-06 `work_item` 상세 검토에서는 본체 엔터티를 슬림하게 유지하고, 계층/계획 연결/상태 이력/외부 참조는 별도 엔터티로 분리하는 방향을 우선 검토한다.
- 2026-04-06 `work_item` 본체 정의에서 `process_model_definition_id`, `progress_percent`, `board_rank` 를 제거 후보로 보고, 각각 프로젝트 프로세스 설정 또는 읽기 모델로 분리하는 방향을 반영한다.
- 2026-04-06 상태 모델 상세화에서는 `workflow_scheme` 만으로는 부족하다고 판단해, `workflow_status_definition` 과 `workflow_transition_definition` 을 추가로 정의하는 방향을 반영한다.

## 7. 도메인 모델 구체화 전 확인 항목

도메인 모델을 엔터티 수준으로 구체화하기 전에 다음 항목을 먼저 확정하거나 최소한 가정으로 명시해야 한다.

### 7.1 업무 기준 식별자

- `work_item` 과 `project` 의 관계를 1:N 로 둘지, 일부 경우 N:N 연결을 허용할지
- `epic`, `story`, `task`, `sub-task` 를 타입과 계층 관계로 표현할지, 일부를 별도 엔터티로 둘지
- 관리 외 프로젝트가 관리 대상 전환 전까지 별도 식별자 체계를 가질지
- 외부 시스템 식별자와 내부 표준 식별자 중 어떤 값을 업무 기준 기본키로 삼을지
- `sprint`, `release`, 마일스톤, `WBS` 를 별도 계획 엔터티로 둘지
- `process_model` 을 고정 열거형으로 둘지, 확장 가능한 정의 모델로 둘지

### 7.2 상태 모델과 라이프사이클

- `work_item` 상태, 프로젝트 상태, 전환 상태, 마이그레이션 상태를 어디까지 독립 모델로 둘지
- 단순 조회용 상태와 승인/운영용 상태를 같은 상태 기계로 묶을지 분리할지
- `AI` 초안 상태와 본업무 상태를 어떤 경계로 분리할지
- `Agile`, `V-Model`, 혼합형 프로젝트에서 상태 모델을 공통 상태 + 프로세스별 상세 상태로 분리할지
- 신규 프로세스 모델이 들어올 때 상태/보드/계획 규칙을 설정만으로 추가할 수 있을지

### 7.3 조직/인력 기준정보 범위

- 조직장, 승인권자, 대행 승인자, 과제 책임자를 모두 별도 엔터티로 둘지 역할 관계로 표현할지
- 조직 개편 이력과 현재 조직 구조를 한 모델에서 표현할지 이력 모델을 분리할지
- 부재 일정이 개인 기준만 필요한지, 프로젝트/조직 단위 캘린더 엔터티가 별도로 필요한지

### 7.4 연계 데이터 보존 수준

- 원시 수집 데이터(`raw`)를 어느 수준까지 보존할지
- 표준화 후 스냅샷과 원천 참조를 둘 다 유지할지
- 조회 최적화를 위해 별도 읽기 모델이나 집계 스냅샷을 둘지

### 7.5 품질/개발 문맥의 경계

- 코드리뷰, 빌드, 테스트, 결함, 정적분석을 독립 도메인으로 더 쪼갤지
- 릴리스 준비도는 계산 뷰인지 별도 엔터티인지
- 품질 게이트 판정 기록을 이력으로 저장할지 계산 결과로만 둘지
- 보드, 간트, `WBS`, 릴리스 뷰를 저장 모델로 둘지 읽기 모델로 계산할지

### 7.6 권한/감사/예외 구조

- 권한을 역할 기반 `RBAC` 중심으로 둘지, 데이터 범위까지 포함한 정책 모델로 확장할지
- 예외 승인을 공통 모델 하나로 통합할지, 품질/권한/연계 예외를 분리할지
- 감사 로그를 업무 이벤트와 운영 이벤트로 분리 저장할지

### 7.7 `AI` 보조 모델의 저장 범위

- `AI` 입력 문맥을 전문 저장할지 해시/참조만 저장할지
- 규칙셋, 프롬프트 버전, 생성 모델 정보를 감사 범위에 포함할지
- `AI` 결과를 업무 엔터티의 부속물로 둘지 독립 엔터티로 둘지

## 8. 다음 업데이트 규칙

- 아키텍처 초안 작성 단계가 바뀌면 `4. 진행 단계`와 `6. 진행 현황`을 함께 갱신한다.
- 계획이 변경되면 산출물과 후속 작업 후보도 같이 수정한다.
