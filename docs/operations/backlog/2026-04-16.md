# 2026-04-16 작업 백로그

- 문서 목적: 2026-04-16에 수행한 작업의 계획, 진행 현황, 완료 내역을 기록한다.
- 범위: 2026-04-16 작업 이력
- 대상 독자: 프로젝트 참여자, 문서 작성자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-16
- 관련 문서: `docs/operations/work_backlog.md`

## TASK-100 brew/npm 기반 개발 도구 복구 가능성 점검

- 상태: done
- 우선순위: high
- 요청일: 2026-04-16
- 완료일: 2026-04-16
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `docs/operations/work_backlog.md`
  - `docs/operations/backlog/2026-04-16.md`
  - `docs/operations/environments/bazzite-192.168.0.122/README.md`
- 작업 내용: 현재 호스트에서 `brew` 와 `npm` 을 사용해 `cargo`, `pytest`, `compose` 보조 도구를 복구할 수 있는지 확인하고, 실제 설치 가능 여부와 차단 원인을 점검한다.
- 진행 현황:
  - 2026-04-16 `brew --version`, `npm --version` 확인 결과 각각 `Homebrew 5.1.6`, `npm 11.12.1` 로 실행 자체는 가능함을 확인했다.
  - 2026-04-16 `brew info docker`, `brew info rust`, `brew info docker-compose` 조회로 설치 후보와 병행 경로를 점검했다.
  - 2026-04-16 현재 호스트에는 이미 `podman 5.8.1` 이 있으므로, 실제 복구 우선순위는 `rust`, `pytest`, `podman compose` provider 확보라고 판단했다.
  - 2026-04-16 `brew install rust` 를 시도했으나 기본 캐시 경로가 읽기 전용이라 먼저 실패했고, `HOMEBREW_CACHE=/tmp/homebrew-cache` 로 우회한 뒤 다시 시도했다.
  - 2026-04-16 두 번째 시도에서는 `formulae.brew.sh` DNS 해석 실패로 다운로드가 막혀 설치를 완료하지 못했다.
  - 2026-04-16 `python3 -m pytest --version`, `pip index versions pytest` 로 `pytest` 경로를 확인했으나 로컬 미설치 상태이며, `pip` 역시 외부 인덱스 접근에서 이름 해석 실패로 설치 불가였다.
  - 2026-04-16 `cargo`, `rustc`, `podman-compose`, `docker-compose` 는 현재 PATH 상에 없음을 재확인했다.
- 완료 기준:
  - `brew` 와 `npm` 사용 가능 여부가 확인되어야 한다.
  - 실제 설치 가능 여부 또는 차단 원인이 명확해야 한다.
  - 결과가 현재 호스트 환경 기록과 백로그에 남아야 한다.
- 작업 결과: 완료. `brew` 와 `npm` 명령은 현재 호스트에서 실행 가능하지만, 이 세션은 외부 네트워크 이름 해석이 되지 않아 패키지 다운로드 기반 설치는 진행할 수 없었다. 따라서 `brew` 나 `pip` 를 통한 `rust`, `pytest`, `podman-compose` 추가 설치는 현재 세션에서 불가능하다. 현재 복구를 막는 핵심 제약은 패키지 매니저 부재가 아니라 외부 네트워크/DNS 차단이다. 후속 작업은 네트워크가 열려 있는 세션에서 같은 명령을 재시도하거나, 오프라인 패키지/사전 설치된 툴체인을 사용하는 방향으로 진행해야 한다.

## TASK-101 네트워크 연결 재확인

- 상태: done
- 우선순위: high
- 요청일: 2026-04-16
- 완료일: 2026-04-16
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `docs/operations/backlog/2026-04-16.md`
  - `docs/operations/environments/bazzite-192.168.0.122/README.md`
- 작업 내용: 이전 설치 실패 원인이 계속 유효한지 확인하기 위해 현재 세션에서 DNS 해석과 HTTPS 접근 가능 여부를 다시 점검한다.
- 진행 현황:
  - 2026-04-16 07:28 KST `getent hosts formulae.brew.sh pypi.org files.pythonhosted.org github.com` 으로 DNS 해석을 확인했다.
  - 2026-04-16 07:28 KST `curl -I --max-time 10 https://formulae.brew.sh/api/formula.jws.json` 로 `HTTP/2 200` 응답을 확인했다.
  - 2026-04-16 07:28 KST `curl -I --max-time 10 https://pypi.org/simple/pytest/` 로 `HTTP/2 200` 응답을 확인했다.
  - 2026-04-16 07:28 KST `curl -I --max-time 10 https://github.com` 로 `HTTP/2 200` 응답을 확인했다.
- 완료 기준:
  - DNS 해석 가능 여부가 확인되어야 한다.
  - 외부 HTTPS 접근 가능 여부가 확인되어야 한다.
  - 결과가 현재 호스트 기록에 반영되어야 한다.
- 작업 결과: 완료. 현재 세션에서는 DNS 해석과 외부 HTTPS 접근이 모두 정상 동작한다. 어제 확인했던 네트워크 차단 상태는 더 이상 현재 기준선으로 보지 않아야 하며, `brew` 와 `pip` 를 통한 설치 재시도가 가능한 상태다.

## TASK-102 개발 도구 설치 및 로컬/격리 테스트 환경 검증

- 상태: done
- 우선순위: high
- 요청일: 2026-04-16
- 완료일: 2026-04-16
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `Makefile`
  - `docker-compose.yml`
  - `docker/python-test.Dockerfile`
  - `docker/rust-test.Dockerfile`
  - `backend/tests/bootstrap.rs`
  - `docs/operations/development_environment.md`
  - `docs/operations/backlog/2026-04-16.md`
  - `docs/operations/environments/bazzite-192.168.0.122/README.md`
- 작업 내용: 네트워크가 복구된 현재 세션에서 `brew` 와 `pip` 로 개발 도구를 설치하고, 로컬 테스트와 `podman` 기반 격리 테스트 경로를 실제로 검증한다.
- 진행 현황:
  - 2026-04-16 `HOMEBREW_CACHE=/tmp/homebrew-cache` 기준으로 `brew install rust` 를 실행해 `cargo 1.94.1`, `rustc 1.94.1` 을 설치했다.
  - 2026-04-16 `python3 -m pip install --user pytest podman-compose` 와 `python3 -m pip install --user -e '.[dev]'` 로 Python 개발 의존성과 테스트 도구를 설치했다.
  - 2026-04-16 `podman` 에서 `postgres:17` short-name 을 비대화형으로 풀지 못하는 문제를 확인하고, `Makefile` 과 `docker-compose.yml` 의 기본 이미지를 `docker.io/library/postgres:17` 로 변경했다.
  - 2026-04-16 `podman` 바인드 마운트가 SELinux 라벨 때문에 컨테이너 안에서 `Permission denied` 를 내는 문제를 확인하고, `Makefile` 과 `docker-compose.yml` 의 소스 마운트에 `:Z` 옵션을 추가했다.
  - 2026-04-16 로컬 Rust 테스트 실행 중 `backend/tests/bootstrap.rs` 의 테이블 정렬 기대값이 실제 DB 결과와 달라 1건 실패하는 것을 확인했고, 현재 마이그레이션 결과 순서에 맞게 기대값을 보정했다.
  - 2026-04-16 Rust 컨테이너 이미지에서 `bash -lc` 로그인 셸이 `cargo` PATH 를 덮어쓰는 문제를 확인하고, `cargo` 직접 실행으로 바꿨다.
  - 2026-04-16 Rust 컨테이너 베이스 이미지 `1.85` 가 현재 의존성의 최소 Rust 버전을 만족하지 못해 실패하는 것을 확인하고, `docker.io/library/rust:1.94-bookworm` 으로 상향했다.
  - 2026-04-16 검증 결과 `python3 -m pytest` 통과, `set -a && source .env.example && cargo test --manifest-path backend/Cargo.toml` 통과, `CONTAINER_RUNTIME=podman make container-test-rust` 통과를 확인했다.
- 완료 기준:
  - `cargo`, `pytest`, `podman-compose` 가 실제 사용 가능해야 한다.
  - 로컬 Python/Rust 테스트가 통과해야 한다.
  - `podman` 기반 격리 테스트 경로가 통과해야 한다.
  - 환경 특이사항과 수정 사항이 문서에 반영되어야 한다.
- 작업 결과: 완료. 현재 호스트는 `cargo 1.94.1`, `rustc 1.94.1`, `pytest 8.4.2`, `podman-compose 1.5.0` 기준으로 개발 가능 상태다. 로컬 Python 테스트와 전체 Rust 테스트가 모두 통과했고, `podman` 기반 격리 테스트도 Rust 경로까지 실제 통과했다. 추가로 `podman` 호환성을 위해 fully qualified image, SELinux `:Z` 마운트, Rust 컨테이너 버전 상향, `cargo` 직접 실행 방식을 저장소 기본값으로 반영했다. 현재 남는 항목은 `build_router` 미사용 경고 1건뿐이며 기능 실패나 환경 차단은 없다.

## TASK-103 운영 UI 실행 및 렌더링 확인

- 상태: done
- 우선순위: high
- 요청일: 2026-04-16
- 완료일: 2026-04-16
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `docs/operations/backlog/2026-04-16.md`
  - `docs/operations/environments/bazzite-192.168.0.122/README.md`
  - `output/playwright/ui_prototype/runtime_check_2026-04-16/01-admin.png`
  - `output/playwright/ui_prototype/runtime_check_2026-04-16/02-organization.png`
- 작업 내용: 실제 백엔드와 정적 UI 서버를 띄운 뒤 `admin.html`, `organization.html` 이 브라우저 기준으로 정상 렌더링되는지 확인한다.
- 진행 현황:
  - 2026-04-16 `cargo run --manifest-path backend/Cargo.toml` 로 Rust 백엔드를 기동하고, `python3 -m http.server 8000` 으로 정적 서버를 기동했다.
  - 2026-04-16 `curl http://127.0.0.1:8080/api/v1/health` 에서 `{\"status\":\"ok\"}` 응답을 확인했다.
  - 2026-04-16 `curl -I` 기준 `http://127.0.0.1:8000/src/ui_prototype/admin.html`, `organization.html` 모두 `HTTP 200` 응답을 확인했다.
  - 2026-04-16 `/tmp/playwright-check` 에 임시 `playwright 1.54.2` 를 설치하고 Chromium 을 내려받아 실제 브라우저 렌더링 검증 스크립트를 실행했다.
  - 2026-04-16 브라우저 검증 결과 `admin.html`, `organization.html` 모두 `#api-status-chip = 연결 정상`, 실패 문구 없음으로 확인했다.
  - 2026-04-16 결과 스크린샷을 `output/playwright/ui_prototype/runtime_check_2026-04-16/01-admin.png`, `02-organization.png` 로 저장했다.
- 완료 기준:
  - 백엔드와 정적 UI 서버가 실제로 떠야 한다.
  - 브라우저 기준으로 주요 운영 화면이 열려야 한다.
  - API 연결 상태가 실패가 아니어야 한다.
  - 렌더링 증적 스크린샷이 남아야 한다.
- 작업 결과: 완료. `admin.html` 과 `organization.html` 은 현재 호스트에서 실제 백엔드와 연결된 상태로 정상 렌더링됐다. 두 화면 모두 브라우저 기준 상태 칩이 `연결 정상` 으로 표시됐고, 실패 문구는 검출되지 않았다. 증적 이미지는 `output/playwright/ui_prototype/runtime_check_2026-04-16/` 아래에 저장했다.

## TASK-104 표준 작업 워크플로우 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-16
- 완료일: 2026-04-16
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `docs/operations/standard_workflow_draft.md`
  - `docs/operations/README.md`
  - `docs/README.md`
  - `docs/operations/backlog/2026-04-16.md`
- 작업 내용: 현재 저장소에서 실제로 사용 중인 작업 방식을 기준으로 표준 작업 워크플로우 초안을 운영 문서로 정리한다.
- 진행 현황:
  - 2026-04-16 사용자가 현재 작업 방식을 설명한 내용을 바탕으로 먼저 표준 워크플로우 초안을 작성하자고 요청했다.
  - 2026-04-16 운영 위키, 백로그 인덱스, 개발 환경 가이드를 다시 확인해 새 문서가 들어갈 위치와 기존 운영 기준과의 연결 지점을 정리했다.
  - 2026-04-16 초안 문서는 `작업 시작`, `환경 확인`, `구현`, `로컬 검증`, `격리 검증`, `실행 확인`, `결과 기록`, `작업 유형별 최소 흐름`, `예외 처리` 순으로 구성했다.
  - 2026-04-16 운영 위키와 문서 위키 홈에 새 문서 링크를 추가해 탐색 경로를 연결했다.
- 완료 기준:
  - 현재 작업 방식이 운영 문서로 정리되어야 한다.
  - 새 문서가 운영 위키와 문서 위키 홈에서 접근 가능해야 한다.
  - 오늘자 백로그에 작업 기록이 남아야 한다.
- 작업 결과: 완료. 현재 저장소에서 실제로 사용 중인 작업 방식을 바탕으로 [표준 작업 워크플로우 초안](../standard_workflow_draft.md) 문서를 작성했다. 이 초안은 브리핑, 백로그 등록, 호스트별 환경 기록, 로컬/격리 테스트, 실행 확인, 문서 반영까지 한 흐름으로 묶어 설명한다. 운영 위키와 문서 위키 홈에도 링크를 추가해 이후 세부 워크플로우 문서의 기준 문서로 사용할 수 있게 했다.
- 다음 세션 시작 포인트: 세션 인계 전용 문서와 백로그 템플릿 보강 여부를 확인한다.
- 남은 리스크: 아직 세션 인계 절차가 표준 워크플로우에 완전히 흡수되지 않았다.
- 후속 작업: `session_handoff.md` 와 세션 시작/종료 절차를 운영 규칙에 반영한다.

## TASK-105 세션 인계 중심 워크플로우 구축

- 상태: done
- 우선순위: high
- 요청일: 2026-04-16
- 완료일: 2026-04-16
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `docs/operations/session_handoff.md`
  - `docs/operations/standard_workflow_draft.md`
  - `docs/operations/work_backlog.md`
  - `docs/operations/README.md`
  - `docs/README.md`
  - `AGENTS.md`
  - `docs/operations/backlog/2026-04-16.md`
- 작업 내용: 새로운 세션이나 새로운 호스트 환경에서 이전 세션의 진행 상태와 잔여 작업을 빠르게 파악할 수 있도록 세션 인계 중심 워크플로우를 구축한다.
- 진행 현황:
  - 2026-04-16 사용자가 새로운 세션/환경에서 이전 세션의 진행 작업, 완료 작업, 잔여 작업을 효율적으로 확인할 수 있는 워크플로우를 만들고 싶다고 요청했다.
  - 2026-04-16 기존 표준 작업 워크플로우, 운영 위키, 백로그 인덱스, 저장소 운영 규칙을 다시 확인해 어디에 세션 인계 흐름을 삽입할지 정리했다.
  - 2026-04-16 `session_handoff.md` 를 새로 작성해 `현재 작업 요약`, `진행 중 작업`, `차단 작업`, `최근 완료 작업`, `잔여 작업 우선순위`, `환경별 검증 현황`, `새 세션 시작 절차`, `세션 종료 절차` 를 정의했다.
  - 2026-04-16 `standard_workflow_draft.md` 에 `세션 인계 확인`, `세션 종료 기록`, `세션 인계 정리` 섹션을 추가하고, 백로그 필수 항목에 인계용 필드를 반영했다.
  - 2026-04-16 `AGENTS.md`, `work_backlog.md`, 운영 위키, 문서 위키 홈에도 세션 인계 문서와 규칙을 연결했다.
- 완료 기준:
  - 세션 시작 시 먼저 읽을 인계 문서가 존재해야 한다.
  - 표준 워크플로우에 세션 시작/종료 절차가 반영되어야 한다.
  - 백로그 기록 기준에 다음 세션 관점의 항목이 포함되어야 한다.
  - 관련 운영 문서가 새 흐름을 참조할 수 있어야 한다.
- 작업 결과: 완료. 이제 새 세션은 상세 백로그보다 먼저 [세션 인계 문서](../session_handoff.md) 를 읽고 현재 상태를 요약 형태로 파악할 수 있다. 표준 워크플로우에는 세션 인계 확인과 종료 기록 절차가 추가됐고, 백로그 템플릿에는 `다음 세션 시작 포인트`, `남은 리스크`, `후속 작업` 항목이 포함됐다. 운영 규칙과 위키 인덱스도 함께 갱신해 이후 세션이 같은 흐름으로 시작할 수 있게 했다.
- 다음 세션 시작 포인트: `session_handoff.md` 를 실제 다음 세션 진입점으로 사용해보고 부족한 섹션을 보완한다.
- 남은 리스크: 아직 모든 기존 백로그 항목이 새 인계 필드를 갖고 있지는 않다.
- 후속 작업: `기능 개발`, `버그 수정`, `PR 대응` 워크플로우를 세션 인계 중심으로 세분화한다.

## TASK-106 워크플로우 체계화 계획 수립 및 세부 문서 구조 정의

- 상태: done
- 우선순위: high
- 요청일: 2026-04-16
- 완료일: 2026-04-16
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `docs/operations/workflow_development_plan.md`
  - `docs/operations/README.md`
  - `docs/README.md`
  - `docs/operations/session_handoff.md`
  - `docs/operations/backlog/2026-04-16.md`
- 작업 내용: 표준 워크플로우 초안을 카테고리별 운영 체계로 확장하기 위한 계획 문서를 먼저 작성하고, 이후 작성할 세부 워크플로우 문서와 자동화/색인 구조의 범위를 정의한다.
- 진행 현황:
  - 2026-04-16 기존 `standard_workflow_draft.md`, `session_handoff.md`, 운영 위키, 오늘자 백로그를 다시 확인해 현재 기준선과 남은 분해 과제를 정리했다.
  - 2026-04-16 `workflow_development_plan.md` 를 새로 작성해 작업 진행, 문서화, 코드 색인, 검증, 병합 견고성 워크플로우로 카테고리를 분해하고, AI 친화 구조와 자동화 후보를 포함한 단계별 계획을 정리했다.
  - 2026-04-16 운영 위키, 문서 위키 홈, 세션 인계 문서에 새 계획 문서를 연결해 다음 세션과 다음 단계 작업이 같은 기준 문서를 참조하도록 정리했다.
- 완료 기준:
  - 워크플로우 개발의 목표, 범위, 산출물, 단계별 수행 계획이 문서화되어야 한다.
  - 운영 위키에서 계획 문서로 접근 가능해야 한다.
  - 다음 단계에서 어떤 세부 문서를 먼저 작성할지 판단할 수 있어야 한다.
- 작업 결과: 완료. [워크플로우 개발 계획](../workflow_development_plan.md) 문서를 작성해 작업 진행, 문서화/동기화, 코드 색인, 검증, 브랜치 병합 견고성의 다섯 축으로 워크플로우를 분해했다. 사람용 문서와 AI agent 용 빠른 참조 구조의 분리 원칙, 자동화 후보, 단계별 수행 계획도 함께 정리했다. 운영 위키, 문서 위키 홈, 세션 인계 문서에도 링크를 연결해 이후 세부 워크플로우 문서 작성의 기준선으로 삼을 수 있게 했다.
- 다음 세션 시작 포인트: `workflow_development_plan.md` 의 `단계 2. 기준 문서 분해` 를 기준으로 `작업 진행 워크플로우` 문서부터 작성할지, `문서화 및 동기화 워크플로우` 문서부터 작성할지 결정한다.
- 남은 리스크: 색인 문서를 사람용과 AI 용으로 완전히 분리할지, 한 문서 안의 요약/상세 구조로 갈지 아직 결정되지 않았다.
- 후속 작업: `workflow_task_execution.md`, `workflow_documentation_sync.md`, `code_index_strategy.md`, `ai_agent_quickstart.md` 의 작성 순서를 확정하고 단계별로 진행한다.

## TASK-107 워크플로우 초안 브랜치 생성 및 원격 푸시

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-16
- 완료일: 2026-04-16
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `.gitignore`
  - `docs/operations/backlog/2026-04-16.md`
- 작업 내용: 현재까지 작성한 운영 문서, 환경 셋업, 테스트 검증, 워크플로우 계획 변경을 `codex/ai_workflow_development` 브랜치로 정리해 원격 저장소에 푸시한다.
- 진행 현황:
  - 2026-04-16 현재 변경 범위와 원격 저장소 설정을 확인했다.
  - 2026-04-16 생성 캐시가 커밋에 포함되지 않도록 `.gitignore` 에 Python 캐시 및 `egg-info` 패턴을 추가했다.
- 완료 기준:
  - `codex/ai_workflow_development` 브랜치가 생성되어야 한다.
  - 현재 작업 변경이 커밋되어야 한다.
  - 원격 `origin` 에 브랜치가 푸시되어야 한다.
- 작업 결과: 완료. 현재까지의 운영 문서, 환경 셋업, 테스트 검증, 워크플로우 계획 변경을 `codex/ai_workflow_development` 브랜치로 정리해 커밋 `6470bd1` 로 묶었고, 원격 `origin/codex/ai_workflow_development` 로 푸시했다. 이 저장소에는 Git 작성자 정보가 없었기 때문에 최근 커밋 작성자 기준으로 로컬 저장소에만 `user.name=ykylee`, `user.email=ddn777@hotmail.com` 을 설정한 뒤 커밋을 진행했다.
- 다음 세션 시작 포인트: 원격 브랜치 기준으로 후속 워크플로우 상세 문서 작성을 이어간다.
- 남은 리스크: 현재 커밋 범위에 운영 증적 스크린샷과 환경 문서가 함께 포함되므로 이후 브랜치 정리 시 산출물 보존 정책을 따로 결정할 필요가 있다.
- 후속 작업: 필요하면 이 브랜치를 기준으로 PR 초안 작성 또는 후속 세부 워크플로우 문서 작업을 이어간다.
