# 2026-04-11 작업 백로그

- 문서 목적: 2026-04-11에 수행한 작업의 계획, 진행 현황, 완료 내역을 기록한다.
- 범위: 2026-04-11 작업 이력
- 대상 독자: 프로젝트 참여자, 문서 작성자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-11
- 관련 문서: `docs/operations/work_backlog.md`, `docs/operations/backlog/2026-04-10.md`

## TASK-001 최근 작업 브랜치 확인 및 브리핑

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-11
- 완료일: 2026-04-11
- 담당: Codex
- 영향 문서:
  - `docs/operations/work_backlog.md`
  - `docs/operations/backlog/2026-04-11.md`
- 작업 내용: 가장 최근 작업 브랜치를 확인하고 해당 브랜치로 체크아웃한 뒤, 최근 작업 내용을 커밋과 백로그 기준으로 요약 브리핑한다.
- 진행 현황:
  - 2026-04-11 `docs/operations/work_backlog.md` 와 최근 백로그 문서를 먼저 확인해 진행 중 또는 최근 완료 작업을 파악했다.
  - 2026-04-11 `git reflog`, `git branch --sort=-committerdate`, `git log` 를 확인한 결과 최근 작업 브랜치는 `codex/org-tree-admin-tuning` 이며 현재도 같은 브랜치에 있는 상태임을 확인했다.
  - 2026-04-11 명시적으로 `git checkout codex/org-tree-admin-tuning` 을 다시 수행해 작업 기준 브랜치를 맞췄다.
- 완료 기준:
  - 최근 작업 브랜치가 식별되어야 한다.
  - 해당 브랜치가 체크아웃되어 있어야 한다.
  - 최근 작업 내용이 백로그와 커밋 기준으로 요약되어야 한다.
- 작업 결과: 완료. 최근 작업 브랜치는 `codex/org-tree-admin-tuning` 이며, 현재 브랜치도 동일하다. 최근 작업은 조직도 관리자 UI 조정 작업으로, 사업부 추가 `+` 버튼과 입력 폼의 위치 정렬을 다듬고 빠른 사업부 생성 흐름을 보강한 내용이 중심이다.

## TASK-002 테스트 환경 기동 및 사업부 추가 UI 렌더링 확인

- 상태: done
- 우선순위: high
- 요청일: 2026-04-11
- 완료일: 2026-04-11
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-11.md`
  - `output/playwright/ui_prototype/debug/`
- 작업 내용: 테스트용 데이터베이스, Rust 백엔드, 정적 UI 서버를 기동한 뒤, 조직 관리 화면에서 사업부 추가 `+` 버튼과 입력 폼의 실제 렌더링이 의도대로 보이는지 확인한다.
- 진행 현황:
  - 2026-04-11 `docs/operations/work_backlog.md`, `docs/operations/backlog/2026-04-10.md`, `docs/operations/backlog/2026-04-09.md` 를 확인해 기존 검증 런타임 기준 포트와 화면 확인 절차를 파악했다.
  - 2026-04-11 현재 `55432`, `28080`, `8000` 포트에 리스닝 프로세스가 없어 테스트 런타임이 내려간 상태임을 확인했다.
  - 2026-04-11 `playwright` 스킬 절차에 따라 `npx` 사용 가능 상태를 확인했고, 정적 UI는 `python3 -m http.server 8000`, 백엔드는 `cargo run --manifest-path backend/Cargo.toml` 기반으로 재기동할 계획이다.
  - 2026-04-11 처음에는 `docker daemon` 과 로컬 PostgreSQL 실행 파일이 모두 없어 DB 런타임 복구가 막혔으나, `colima start` 로 Docker 런타임을 다시 기동했다.
  - 2026-04-11 `alm-postgres-test` 를 다시 올렸지만 `55432` 경로에서는 백엔드 연결 풀이 열리지 않아, healthy 상태였던 `alm-postgres` `5432` 를 사용해 백엔드를 `0.0.0.0:28080` 에 정상 기동했다.
  - 2026-04-11 정적 UI `8000` 과 백엔드 `28080` 헬스 체크를 확인했고, 조직 API 기준 활성 조직 13건, 활성 인력 5건이 조회되는 상태에서 `data_organizations.html?apiBase=http://127.0.0.1:28080/api/v1` 를 Playwright Firefox로 열었다.
  - 2026-04-11 상단 사업부 탭 영역에서 `사업부 추가` 버튼 클릭 전후 스냅샷과 캡처를 재생성했고, 입력 카드가 탭 목록 뒤가 아니라 우측 액션 슬롯에서 `+` 버튼 옆에 열리는 것을 확인했다.
- 완료 기준:
  - 테스트용 데이터베이스, 백엔드, 정적 UI 서버가 접근 가능한 상태여야 한다.
  - 조직 관리 화면에서 최근 사업부 추가 UI를 실제 브라우저로 확인해야 한다.
  - 캡처 또는 검증 근거가 남아야 한다.
- 작업 결과: 완료. 현재 테스트 환경은 정적 UI `http://127.0.0.1:8000`, Rust 백엔드 `http://127.0.0.1:28080/api/v1`, PostgreSQL `5432` 조합으로 확인됐다. `GET /api/v1/health` 는 `{"status":"ok"}` 를 반환했고, 조직 관리 화면에서 사업부 탭 2개와 우측 `+` 버튼이 정상 렌더링됐다. `+` 클릭 후에는 입력 카드가 같은 우측 액션 슬롯 안에서 열려 최근 의도한 레이아웃이 실제 브라우저에서도 유지됨을 확인했다. 검증 캡처는 `output/playwright/ui_prototype/debug/organization_division_tabs_plus_right_before_click_20260411.png`, `output/playwright/ui_prototype/debug/organization_division_tabs_plus_right_after_click_20260411.png` 에 남겼다.

## TASK-003 사업부 추가 버튼 동작 검증

- 상태: done
- 우선순위: high
- 요청일: 2026-04-11
- 완료일: 2026-04-11
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-11.md`
  - `output/playwright/ui_prototype/debug/`
- 작업 내용: 사업부 추가 UI의 `취소` 와 `생성` 버튼이 실제로 의도한 동작을 수행하는지 브라우저와 API 기준으로 검증한다.
- 진행 현황:
  - 2026-04-11 직전 작업에서 정적 UI `8000`, 백엔드 `28080`, PostgreSQL `5432` 기반 테스트 환경을 복구했고 Playwright 브라우저 세션도 확보했다.
  - 2026-04-11 현재 검증 범위는 `취소` 클릭 시 초안 카드가 닫히는지, `생성` 클릭 시 새 사업부가 실제 생성되고 상단 탭과 선택 상태에 반영되는지, 필요 시 검증용 데이터가 정리되는지까지 포함한다.
  - 2026-04-11 `취소` 검증 후 API 기준 활성 조직 수와 최상위 조직 목록이 그대로 `13건`, `default_org / innovation_div / platform_div` 로 유지되는 것을 확인했다.
  - 2026-04-11 `사업부명=버튼검증사업부 131433`, `키=button_verify_131433` 를 입력했을 때 미리보기 코드가 `biz_button_verify_131433` 로 계산되는 것을 확인했다.
  - 2026-04-11 `생성` 클릭 후 API 기준 활성 조직 수가 `14건` 으로 증가했고, `biz_button_verify_131433` 가 최상위 조직으로 생성되었다.
  - 2026-04-11 생성 직후 브라우저 스냅샷에서 새 사업부 탭이 맨 앞에 추가되고, 선택 사업부와 상세 카드도 새 사업부로 전환되며, 상태 메시지 `사업부 biz_button_verify_131433 생성이 완료되었습니다.` 가 노출되는 것을 확인했다.
  - 2026-04-11 검증용 데이터 오염을 막기 위해 생성한 사업부를 `DELETE /admin/master-data/organizations/biz_button_verify_131433` 로 제거했고, API와 브라우저 새로고침 기준으로 다시 `13건` 상태로 복귀한 것을 확인했다.
- 완료 기준:
  - `취소` 버튼 동작이 검증되어야 한다.
  - `생성` 버튼 동작이 검증되어야 한다.
  - 검증 결과와 정리 여부가 기록되어야 한다.
- 작업 결과: 완료. `취소` 버튼은 초안 카드를 닫고 서버 상태를 바꾸지 않았으며, `생성` 버튼은 입력값을 `biz_button_verify_131433` 코드로 변환해 실제 최상위 사업부를 만들고, 상단 탭/선택 사업부/상세 카드/상태 메시지까지 즉시 갱신했다. 이후 검증용 사업부를 삭제해 데이터는 원래 상태로 복구했다. 따라서 사업부 추가 UI의 핵심 버튼 동작은 현재 테스트 환경에서 의도대로 동작한다.

## TASK-004 사업부 추가 입력 카드 하단 배치 조정

- 상태: done
- 우선순위: high
- 요청일: 2026-04-11
- 완료일: 2026-04-11
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-11.md`
- 작업 내용: 사업부 추가 `+` 버튼 클릭 시 입력 카드가 버튼 왼쪽이 아니라 아래쪽에 열리고, 카드가 열려도 기존 사업부 탭이 어색하게 줄바꿈되지 않도록 상단 레이아웃을 조정한다.
- 진행 현황:
  - 2026-04-11 현재 구조를 확인한 결과 `org-division-actions` 안에 입력 카드와 `+` 버튼이 같은 가로 흐름으로 배치되어 있어, 카드가 열리면 탭 목록 폭이 줄고 기존 항목 줄바꿈이 발생하는 상태다.
  - 2026-04-11 수정 방향은 우측 액션 영역을 세로 스택으로 바꿔 `+` 버튼은 상단에 유지하고, 입력 카드는 그 아래에 독립적으로 열리게 하며, 탭 목록은 자기 줄 폭을 유지하도록 정리하는 것이다.
  - 2026-04-11 첫 번째 스타일 조정만으로는 카드가 여전히 버튼의 오른쪽 위 기준 왼쪽으로 펼쳐진 인상이 남는 것을 스크린샷으로 재확인했다.
  - 2026-04-11 `src/ui_prototype/app.js` 구조를 바꿔 상단을 `탭+버튼` 첫 줄과 `입력 카드` 둘째 줄로 분리했고, `styles.css` 에 `org-division-tabs-head`, `org-division-draft-row` 레이아웃을 추가했다.
  - 2026-04-11 새 브라우저 세션에서 다시 렌더링한 결과, `+` 버튼은 첫 줄 우측에 남고 입력 카드는 완전히 다음 줄에 분리되어 열리며, 기존 탭 2개도 같은 줄을 유지하는 것을 확인했다.
- 완료 기준:
  - `+` 버튼 클릭 시 입력 카드가 버튼 아래쪽에 열려야 한다.
  - 입력 카드가 열려도 기존 탭이 불필요하게 줄바꿈되지 않아야 한다.
  - 실제 렌더링으로 결과가 확인되어야 한다.
- 작업 결과: 완료. 사업부 추가 입력 카드는 이제 `+` 버튼의 왼쪽 옆이 아니라 바로 아래쪽에 열리고, 우측 액션 영역 안에서 별도 세로 흐름으로 배치된다. 따라서 카드가 열려도 기존 사업부 탭 목록 폭이 덜 흔들리고, 현재 검증 화면 기준으로 기존 탭 두 개는 같은 줄을 유지한다. 확인 캡처는 `output/playwright/ui_prototype/debug/organization_division_tabs_dropdown_before_click_20260411.png`, `output/playwright/ui_prototype/debug/organization_division_tabs_dropdown_after_click_20260411.png` 에 남겼다.

## TASK-005 사업부 추가 입력 카드 진입 모션 보강 및 후속 검토 포인트 정리

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-11
- 완료일: 2026-04-11
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-11.md`
- 작업 내용: 사업부 추가 입력 카드가 열릴 때 과하지 않은 진입 모션을 추가하고, 현재 조직 관리 화면에서 다음으로 점검할 후속 검토 포인트를 정리한다.
- 진행 현황:
  - 2026-04-11 현재 배치는 의도대로 정리됐고, 이제 카드가 열릴 때만 짧은 `fade + slide-down` 계열 모션을 넣어 반응성을 보강하려 한다.
  - 2026-04-11 후속 검토 포인트는 현재 화면 상단 사업부 탭/트리/삭제 액션 주변의 사용자 흐름과 상태 일관성 위주로 추릴 계획이다.
- 2026-04-11 `styles.css` 에 `org-division-draft-row-enter`, `org-division-draft-card-enter` keyframe 을 추가해 카드가 열릴 때 짧게 아래에서 올라오며 나타나도록 조정했다.
- 2026-04-11 실제 브라우저에서 다시 열어 본 결과, 모션은 짧고 가벼운 수준으로 적용되어 배치 수정 이후의 흐름을 해치지 않음을 확인했다.
- 완료 기준:
  - 입력 카드 진입 모션이 과하지 않게 반영되어야 한다.
  - 실제 렌더링으로 모션 적용 상태가 확인되어야 한다.
  - 다음 검토 포인트가 정리되어야 한다.
- 작업 결과: 완료. 사업부 추가 입력 카드는 현재 배치를 유지한 채 짧은 진입 모션이 추가됐고, 실제 렌더링에서도 과하지 않은 수준으로 보강됐다. 다음 검토 포인트는 `1) 삭제 검토/삭제 확인 흐름이 현재 선택 상태와 일관되게 이어지는지`, `2) 범위 필터와 사업부 탭 조합에서 디렉터리/트리/상세 카드가 항상 같은 기준으로 갱신되는지`, `3) 작은 화면 폭에서 상단 사업부 탭과 입력 카드가 어디까지 자연스럽게 유지되는지` 세 가지를 우선 추천한다.

## TASK-006 삭제 검토 흐름 검증

- 상태: done
- 우선순위: high
- 요청일: 2026-04-11
- 완료일: 2026-04-11
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-11.md`
  - `output/playwright/ui_prototype/debug/`
- 작업 내용: 조직 관리 화면의 `삭제 검토` 버튼, 삭제 확인 모달, 실제 삭제 가능 조건과 서버 반응이 일관되게 연결되는지 검증한다.
- 진행 현황:
  - 2026-04-11 현재 테스트 환경은 정적 UI `8000`, 백엔드 `28080`, PostgreSQL `5432` 기준으로 유지 중이다.
  - 2026-04-11 이번 검증은 `삭제 검토` 비활성/활성 조건, 모달 문구의 정확성, 실제 삭제 요청 성공 또는 차단 시나리오를 함께 확인하는 방향으로 진행한다.
  - 2026-04-11 `pm_part` 선택 상태에서 `삭제 검토` 버튼이 활성화되고, 삭제 가능 카드에는 `직속 하위 조직이 없어 삭제 가능합니다` 라고 표시되지만 하단 점검 카드에는 `아직 어렵습니다. 직속 하위 0개, 직속 구성원 1명이 남아 있습니다.` 라고 남아 있어 문구 일관성 이슈를 확인했다.
  - 2026-04-11 실제 삭제 성공 흐름은 시드 조직 대신 임시 leaf 조직 `tmp_delete_flow_20260411` 를 `pm_part` 하위에 생성한 뒤 검증했다.
  - 2026-04-11 임시 조직 선택 시 트리, 상세 카드, 삭제 가능 여부, 점검 카드가 모두 `직속 하위 0개`, `직속 구성원 0명`, `삭제 가능` 기준으로 일관되게 갱신되는 것을 확인했다.
  - 2026-04-11 `삭제 검토` 모달에는 `삭제검토임시조직 (tmp_delete_flow_20260411) 을 삭제합니다. 직속 구성원 0명은 조직 미배정 상태로 전환됩니다.` 가 표시됐고, `삭제 확인` 후 UI에서는 조직 수가 `14 -> 13`, 선택 조직이 `pm_part` 로 복귀하며 `조직 tmp_delete_flow_20260411 삭제가 완료되었습니다.` 메시지가 노출됐다.
  - 2026-04-11 API 기준으로도 활성 조직 목록에서는 임시 조직이 제거됐고, 삭제 목록에는 `organization_status=deleted`, `effective_to=2026-04-11T13:07:23.875330+00:00` 상태로 남는 것을 확인했다.
- 완료 기준:
  - `삭제 검토` 버튼 상태와 실제 삭제 가능 조건이 맞아야 한다.
  - 삭제 모달 문구와 서버 동작이 일관적이어야 한다.
  - 검증 결과가 기록되어야 한다.
- 작업 결과: 완료. 삭제 검토 흐름은 leaf 조직 기준으로는 의도대로 동작했다. `삭제 검토` 버튼, 확인 모달, `DELETE` 처리, 트리/선택 상태 재계산, 삭제 완료 메시지, 삭제 상태 보존까지 모두 이어졌다. 다만 `pm_part` 처럼 직속 구성원만 남은 경우에는 상단 삭제 가능 카드가 `삭제 가능` 으로 보이는 반면 하단 점검 카드가 `아직 어렵습니다` 로 표기되어 사용자 판단을 흔들 수 있으므로, 다음 검토에서는 이 문구 정책을 하나로 맞추는 것이 좋다. 관련 캡처는 `output/playwright/ui_prototype/debug/delete_review_modal_pm_part_20260411.png`, `output/playwright/ui_prototype/debug/delete_review_modal_tmp_delete_flow_20260411.png`, `output/playwright/ui_prototype/debug/delete_review_after_delete_tmp_delete_flow_20260411.png` 에 남겼다.

## TASK-007 삭제 안내 문구 정합성 보정 및 푸시

- 상태: done
- 우선순위: high
- 요청일: 2026-04-11
- 완료일: 2026-04-11
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/data_organizations.html`
  - `docs/operations/backlog/2026-04-11.md`
- 작업 내용: 삭제 가능 여부 카드, 하단 점검 카드, 삭제 확인 모달이 같은 삭제 정책을 설명하도록 문구를 정합성 있게 맞추고 원격 브랜치로 푸시한다.
- 진행 현황:
  - 2026-04-11 현재 실제 삭제 정책은 `직속 하위 조직이 없으면 삭제 가능` 이고, 직속 구성원은 삭제 시 미배정 상태로 전환되는 것으로 확인됐다.
  - 2026-04-11 이에 맞춰 하단 `지금 정리할 수 있나` 카드와 삭제 모달의 주의 문구를 같은 기준으로 조정하고, 브라우저 재검증 후 푸시할 예정이다.
  - 2026-04-11 `pm_part` 기준으로 다시 렌더링한 결과, 하단 카드가 `정리 판단은 가능합니다. 직속 구성원 1명은 삭제 시 미배정 상태로 전환됩니다.` 로 바뀌어 상단 삭제 가능 카드와 같은 정책을 설명하는 것을 확인했다.
  - 2026-04-11 삭제 모달의 주의 문구도 `직속 하위 조직이 없는 조직만 삭제할 수 있으며, 직속 구성원은 미배정 상태로 전환됩니다.` 로 반영된 것을 확인했다.
- 완료 기준:
  - 삭제 관련 안내 문구가 실제 동작 기준과 일치해야 한다.
  - 실제 브라우저에서 수정된 문구가 확인되어야 한다.
  - 현재 브랜치가 원격으로 푸시되어야 한다.
- 작업 결과: 완료. 삭제 관련 안내 문구는 이제 카드, 보조 질문, 삭제 모달 모두 같은 정책을 설명한다. 즉 `직속 하위 조직이 없으면 삭제 가능` 이고, `직속 구성원은 삭제 시 미배정 상태로 전환` 된다. 브라우저 검증 캡처는 `output/playwright/ui_prototype/debug/delete_copy_consistency_pm_part_20260411.png` 에 남겼고, 후속으로 현재 브랜치를 원격에 푸시했다.
