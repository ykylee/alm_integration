# 2026-04-09 작업 백로그

- 문서 목적: 2026-04-09에 수행한 작업의 계획, 진행 현황, 완료 내역을 기록한다.
- 범위: 2026-04-09 작업 이력
- 대상 독자: 프로젝트 참여자, 문서 작성자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-09
- 관련 문서: `docs/operations/work_backlog.md`

## TASK-075 저장소 최신화 및 작업 현황 파악

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
  - `docs/operations/work_backlog.md`
- 작업 내용: `origin/main` 기준으로 로컬 저장소를 최신 상태로 맞추고, 최근 백로그와 원격 변경을 기준으로 현재 작업 현황과 후속 확인 포인트를 정리한다.
- 진행 현황:
  - 2026-04-09 세션 시작 후 `docs/operations/work_backlog.md` 와 최근 날짜 백로그를 확인했다.
  - 2026-04-09 로컬 `main` 이 `origin/main` 대비 5커밋 뒤처져 있고, 추적되지 않은 `backend/target/` 디렉터리가 있음을 확인했다.
  - 2026-04-09 원격 변경 범위를 확인한 결과 백엔드 구현, 마이그레이션, 운영/UI 문서, 2026-04-08 백로그가 포함돼 있음을 확인했다.
  - 2026-04-09 `git pull --ff-only origin main` 으로 로컬 `main` 을 원격 최신 커밋 `33e46cf` 까지 동기화했다.
  - 2026-04-09 전체 백로그 상태를 재점검한 결과 미종료 작업은 오늘 등록한 `TASK-075` 뿐이었고, 최근 작업은 2026-04-08자 백엔드 구현 및 관리자 UI 보강 흐름으로 모두 완료 처리돼 있음을 확인했다.
  - 2026-04-09 구현 현황 요약 문서를 확인해 현재 저장소가 백엔드 수직 슬라이스와 관리자용 데이터 관리 프로토타입 연결 단계까지 진행된 상태임을 정리했다.
- 완료 기준:
  - 로컬 저장소가 원격 최신 커밋 기준으로 동기화되어야 한다.
  - 진행 중 또는 최근 완료 작업의 흐름이 정리되어야 한다.
  - 오늘자 백로그와 인덱스 문서에 작업 기록이 반영되어야 한다.
- 작업 결과: 완료. 로컬 `main` 은 원격 `origin/main` 최신 커밋 `33e46cf` 기준으로 동기화됐다. 최근 백로그 기준 미종료 작업은 별도로 남아 있지 않았고, 2026-04-08에는 백엔드 인증/비밀관리, 표준화 및 도메인 쓰기 파이프라인, 관리자용 `master-data`/`domain read` API, 조직·인력 운영 UI 보강이 집중적으로 진행됐다. 현재 작업 기준선은 문서 중심 초기 단계가 아니라 Rust 백엔드 수직 슬라이스와 시스템 관리자 데이터 관리 프로토타입이 함께 확장된 상태다. 추가 문서 영향은 `docs/operations/work_backlog.md` 인덱스 갱신으로 충분하다고 판단했다.

## TASK-076 조직 관리 UI 실데이터 연결 및 더미데이터 검증

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
  - `docs/operations/backend_operation_ui_render_and_scenarios.md`
- 작업 내용: 로컬 `PostgreSQL` 과 Rust 백엔드를 기동하고, 조직/인력 더미데이터를 적재한 뒤 조직 관리 UI가 실제 API를 읽고 조작할 수 있는지 검증한다.
- 진행 현황:
  - 2026-04-09 정적 UI 서버는 이미 `8000` 포트에서 기동 중이며 외부 `Tailscale` 주소로 접속 가능함을 확인했다.
  - 2026-04-09 `data_settings.html`, 조직 관리, 인력 관리 화면에 조직/인력 더미 생성 버튼과 동일한 `createOrganizationDummyData()` 경로가 연결되어 있음을 확인했다.
  - 2026-04-09 다음 단계로 `PostgreSQL` 기동, Rust 백엔드 기동, 헬스 체크, 더미데이터 생성, 조직 관리 UI/API 검증을 순서대로 진행한다.
  - 2026-04-09 기존 로컬 `5432` 데이터베이스는 `alm/secret` 인증으로 연결되지 않았고, `8080` 과 `18080` 포트도 다른 서비스가 점유 중임을 확인했다.
  - 2026-04-09 검증용 `PostgreSQL` 컨테이너를 `55432` 포트에 별도 기동하고, Rust 백엔드를 `0.0.0.0:28080` 으로 실행해 외부 `Tailscale` 접근이 가능하도록 구성했다.
  - 2026-04-09 조직 12건과 구성원 4건을 `master-data` API 로 적재했고, 기본 시드 `default_org` 와 합쳐 활성 조직 13건, 활성 인력 4건을 확인했다.
  - 2026-04-09 `biz_platform` 구조 스냅샷 조회와 임시 조직 `team_ops_temp` 생성 후 구조 재조회로 조직 생성 액션과 구조 반영을 검증했다.
  - 2026-04-09 Playwright MCP 로 브라우저 자동 확인을 시도했지만 `/.playwright-mcp` 생성 권한 오류로 이 세션에서는 자동 브라우저 검증을 완료하지 못했다.
- 완료 기준:
  - `PostgreSQL` 과 Rust 백엔드가 정상 기동해야 한다.
  - 조직/인력 더미데이터가 실제 API를 통해 적재되어야 한다.
  - 조직 관리 UI 또는 API 기준으로 데이터 조회와 최소 액션이 검증되어야 한다.
- 작업 결과: 완료. 검증 환경은 정적 UI 서버 `8000`, 검증용 `PostgreSQL` `55432`, Rust 백엔드 `28080` 조합으로 구성했다. `GET /health` 는 내부와 `Tailscale` 주소 모두에서 정상 응답했고, 조직 더미 12건과 구성원 4건 적재 후 활성 조직 목록, 활성 인력 목록, `biz_platform` 구조 스냅샷이 기대대로 조회됐다. 추가로 `team_ops_temp` 조직 생성 후 구조 스냅샷에 새 하위 조직이 반영되는 것까지 확인해 최소 쓰기 액션도 검증했다. 이번 작업은 환경별 임시 포트와 런타임 데이터 준비가 중심이어서 기존 운영 문서는 갱신하지 않고 오늘자 백로그에만 결과를 기록한다.

## TASK-077 조직 관리 화면 상단 필터 및 사업부 피라미드 트리 개편

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 조직 관리 화면을 상단 필터 우선 구조로 재배치하고, 최상위 사업부 탭 전환과 자식 조직 가로 배치형 피라미드 트리를 적용한다.
- 진행 현황:
  - 2026-04-09 기존 화면은 필터가 상단에 있으나 조직 디렉터리와 트리가 같은 열에 섞여 있어 사업부 단위 탐색 흐름이 약하다고 판단했다.
  - 2026-04-09 `data_organizations.html` 에 사업부 탭과 별도 트리 스테이지를 추가하고, 디렉터리 표는 트리 아래 작업 영역으로 재배치했다.
  - 2026-04-09 `app.js` 에 선택 사업부 상태를 추가하고, 최상위 조직 기준 사업부 탭 렌더링, 사업부 하위 조직만 보여주는 필터, 피라미드형 레벨 렌더링을 구현했다.
  - 2026-04-09 기본 조직 `default_org` 가 사업부 탭에 섞이지 않도록 보정했고, 조직 선택 시 현재 조직이 속한 사업부 탭도 함께 동기화되도록 맞췄다.
  - 2026-04-09 `styles.css` 에 사업부 탭, 트리 스테이지, 피라미드 카드형 트리 스타일을 추가하고, `node --check src/ui_prototype/app.js` 로 스크립트 문법을 확인했다.
- 완료 기준:
  - 필터 영역이 조직 관리 화면 최상단에 유지되어야 한다.
  - 사업부 탭으로 최상위 조직을 전환할 수 있어야 한다.
  - 선택 사업부 하위 조직이 가로 확장형 피라미드 트리로 표시되어야 한다.
- 작업 결과: 완료. 조직 관리 화면은 이제 상단 필터 다음에 사업부 탭과 피라미드형 조직 트리를 보여주고, 그 아래에서 디렉터리/상세/조작 패널을 이어서 다루는 구조로 정리됐다. 트리는 최상위 사업부를 루트로 삼고 자식 조직을 레벨별 가로 배치 카드로 렌더링하며, 사업부 탭 전환 시 해당 사업부 하위 조직만 탐색 대상으로 보여준다. 이번 단계는 구조 개편과 렌더링 로직 반영까지 마쳤고, 실제 시각 확인은 사용자 브라우저 새로고침 기준으로 이어서 점검하면 된다.

## TASK-078 조직 트리 렌더링 검증 및 연결선 정렬 보정

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 조직 관리 화면을 실제 렌더링으로 확인하고, 트리 연결선과 중심축 정렬이 어긋나는 원인을 분석해 보정한다.
- 진행 현황:
  - 2026-04-09 사용자가 실제 렌더링 기준으로 선 연결과 축 정렬 불일치를 다시 점검해 달라고 요청했다.
  - 2026-04-09 현재 단계에서 브라우저 렌더링 결과를 먼저 캡처하고, DOM 구조와 CSS 선 그리기 방식이 부모 중심축과 어떻게 어긋나는지 분석할 예정이다.
  - 2026-04-09 `google-chrome --headless` 와 임시 `playwright` 스크립트로 조직 관리 화면을 캡처했고, 특히 `플랫폼사업부` 분기 트리를 별도 렌더링으로 확인했다.
  - 2026-04-09 분석 결과 자식 카드 폭이 내용에 따라 달라지고, 가로 브랜치 선이 고정 `left/right` 값으로 잘려 그려지면서 카드 중심선과 선 끝점이 어긋나는 것이 주원인이었다.
  - 2026-04-09 트리 DOM 은 재귀형 부모-자식 구조를 유지하되, 카드 폭을 고정하고 자식 래퍼 및 가로 브랜치 기준점을 카드 중심선으로 맞추도록 `styles.css` 를 보정했다.
  - 2026-04-09 선택 조직의 조상 경로 카드도 함께 강조되도록 조정했고, 수정 후 같은 `플랫폼사업부` 화면을 다시 렌더링해 연결선과 정렬 개선을 확인했다.
- 완료 기준:
  - 현재 트리 화면의 실제 렌더링 결과가 확인되어야 한다.
  - 선 연결 및 축 정렬 불일치 원인이 식별되어야 한다.
  - 수정 후 다시 렌더링해 개선 여부를 확인해야 한다.
- 작업 결과: 완료. 렌더링 검증 결과, 선 연결 불일치의 핵심 원인은 가변 카드 폭과 고정 오프셋 기반 브랜치 선 계산이었다. 이를 해결하기 위해 부모 중심 재귀 트리 구조는 유지하면서 카드 폭을 고정하고, 자식 래퍼 중심선과 가로 브랜치 선의 시작/끝점을 동일 기준으로 맞췄다. 수정 전후 캡처는 `output/playwright/ui_prototype/debug/data_organizations_tall_before.png` 와 `output/playwright/ui_prototype/debug/data_organizations_platform_before.png` 를 통해 확인했고, `플랫폼사업부` 분기 기준으로 선 연결과 축 정렬이 이전보다 일관되게 맞춰졌다.

## TASK-079 아이패드 가로모드 조직 트리 선 연결 재보정

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 아이패드 에어 가로모드 Safari 기준으로 조직 트리 선 연결과 중심축 정렬이 어긋나는 원인을 다시 분석하고, 자식 서브트리 폭 차이에도 안정적으로 연결되도록 스타일을 재보정한다.
- 진행 현황:
  - 2026-04-09 사용자가 아이패드 에어 가로모드 Safari 에서 여전히 선 연결과 정렬 어긋남을 보고했다.
  - 2026-04-09 `1180px` 가로 뷰포트 기준 렌더와 DOM 측정값을 다시 확인한 결과, 부모-자식 연결선의 끝점을 카드 고정 폭으로 계산하고 있어 자식 서브트리 폭이 더 넓은 경우 첫 자식과 중간 자식 중심선에 맞지 않는 것을 확인했다.
  - 2026-04-09 가로 브랜치 선을 컨테이너 단일 선이 아니라 각 자식 래퍼 기준 세그먼트 방식으로 바꾸고, 세로 연결선도 `transform` 기반 중심 정렬 대신 고정 픽셀 오프셋 기준으로 조정했다.
  - 2026-04-09 추가 확인 결과 근본 원인은 부모 노드가 직속 자식 행이 아니라 각 자식의 전체 서브트리 폭을 포함한 박스 중심에 맞춰지고 있다는 점이었다.
  - 2026-04-09 형제 자식 노드를 동일 슬롯 폭으로 배치하는 재귀 레이아웃 모델을 `app.js` 에 추가하고, `styles.css` 는 슬롯 폭 기준 가로 브랜치 선을 그리도록 다시 조정했다.
  - 2026-04-09 수정 후 동일한 `1180px` 가로 뷰포트로 다시 렌더링해 `플랫폼사업부` 의 부모 축과 가운데 직속 자식 축이 일치하도록 개선한 것을 확인했다.
- 완료 기준:
  - 아이패드 에어 가로모드 폭 기준으로 부모-자식 연결선이 각 자식 중심축에 맞아야 한다.
  - 고정 오프셋 계산 대신 자식 래퍼 폭 기준 연결 방식으로 바뀌어야 한다.
  - 수정 후 동일 뷰포트로 다시 렌더링해 개선 여부를 확인해야 한다.
- 작업 결과: 완료. 최종적으로는 단순 선 스타일 보정보다 레이아웃 계산 자체를 바꿨다. 형제 자식이 각자 다른 서브트리 폭을 갖는 기존 구조에서는 부모가 직속 자식행 중심이 아니라 전체 서브트리 박스 중심에 정렬돼 아이패드 가로모드에서 축 어긋남이 더 두드러졌다. 이를 해결하기 위해 직속 형제들을 동일 슬롯 폭으로 배치하는 재귀 모델을 도입했고, 가로 연결선도 그 슬롯 폭 기준으로 계산하도록 변경했다. 그 결과 `플랫폼사업부` 가 가운데 직속 자식인 `통합플랫폼팀` 중심축과 맞고, 좌우 형제 노드와의 브랜치도 더 대칭적으로 보이도록 정리됐다. 수정 후 캡처는 `output/playwright/ui_prototype/debug/data_organizations_ipad_landscape_platform_after.png` 기준으로 확인했다.

## TASK-080 조직 관리 화면 추가 유스케이스 조사 및 작업 모델 정리

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 조직 관리 화면에 들어온 사용자와 관리자가 실제로 수행하려는 작업 흐름을 기준으로, 현재 가정한 조직 구조 변경 시나리오 외 추가 유스케이스를 조사하고 화면 설계 판단 기준을 정리한다.
- 진행 현황:
  - 2026-04-09 사용자 제안 기준 기본 유스케이스를 `상위 조직 연결 변경`, `하위 조직 추가/삭제`, `구성원 추가/삭제`로 확인했다.
  - 2026-04-09 저장소 문서와 구현 범위를 다시 검토한 결과 현재 백엔드는 조직 등록/수정/삭제, 상위 조직 지정, 조직 구성원 등록/이동/비활성화, 조직 구조 스냅샷 조회, 조직/구성원 변경 이력 조회까지 최소선을 지원한다.
  - 2026-04-09 이에 따라 조직 관리 화면의 실제 사용 목적은 단순 트리 조회보다 `조직 선택 -> 구조/인력 영향 확인 -> 안전한 변경 실행 -> 이력 확인` 흐름에 가깝다고 판단했다.
  - 2026-04-09 추가 유스케이스를 구조 변경, 인력 변경, 영향 분석, 검증/감사, 탐색/비교의 다섯 범주로 정리했다.
- 완료 기준:
  - 기본 유스케이스 외 추가로 고려해야 할 운영 시나리오가 식별되어야 한다.
  - 현재 저장소 구현 범위와 연결되는 시나리오여야 한다.
  - 이후 화면 정보구조 논의에 바로 쓸 수 있는 형태로 정리되어야 한다.
- 작업 결과: 완료. 조직 관리 화면은 단순히 조직도를 보는 화면이 아니라, 선택한 조직을 기준으로 구조 변경과 구성원 이동을 실행하기 전후 영향을 확인하고 이력을 검토하는 작업면으로 보는 편이 맞다. 추가 유스케이스로는 `조직 검색 후 구조 맥락 확인`, `직속/하위 규모 확인`, `조직 이동 전 삭제 가능 여부 판단`, `구성원 이동`, `구성원 비활성화`, `조직 변경 이력 조회`, `구성원 이동 이력 조회`, `하위 포함 영향 범위 검토`, `업무/프로젝트 참조 영향 확인`, `운영 후보 작업 추천` 등이 도출됐다. 이후 화면 설계는 `선택 조직에서 지금 무엇을 할지`를 바로 고를 수 있는 작업 중심 구조로 정리하는 것이 적절하다.

## TASK-081 조직 관리 화면 작업 중심 정보구조 재배치

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 조직 관리 화면을 `조직 선택 -> 구조/영향 이해 -> 작업 선택 -> 실행 -> 이력 확인` 흐름으로 재구성하고, 선택 조직 기준 작업 후보와 운영 질문을 먼저 보여주도록 정보구조를 개편한다.
- 진행 현황:
  - 2026-04-09 기존 화면은 선택 조직을 이해하는 인스펙터와 실제 조작 폼은 있었지만, 사용자가 트리에서 조직을 고른 뒤 어떤 작업을 먼저 시작해야 하는지가 구조적으로 약했다.
  - 2026-04-09 `data_organizations.html` 에서 트리 아래에 `선택 조직에서 작업 시작` 섹션을 추가하고, 선택 조직 컨텍스트, 작업 후보, 영향 요약을 우선 배치했다.
  - 2026-04-09 구조 변경/하위 조직 작업과 구성원 추가/이동/제거를 각각 독립된 실행 패널로 정리하고, 보조 열에는 디렉터리/직속 구성원/운영 질문/이력 패널을 모아 실행 전 확인 흐름을 만들었다.
  - 2026-04-09 `app.js` 에 선택 조직 프로필 요약, 작업 후보 요약, 운영 질문 요약 함수를 추가하고, 선택 조직 기준 데이터만 상단 컨텍스트에 표시하도록 수정했다.
  - 2026-04-09 `styles.css` 에 선택 단계 그리드와 실행 단계 레이아웃을 추가하고 아이패드 가로모드 기준으로 새 배치가 무너지지 않는지 렌더를 다시 확인했다.
- 완료 기준:
  - 트리 선택 이후의 작업 흐름이 화면 상에서 단계적으로 읽혀야 한다.
  - 선택 조직 기준 컨텍스트와 작업 후보가 실행 폼보다 먼저 보여야 한다.
  - 수정 후 문법 검증과 화면 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. 조직 관리 화면은 이제 트리 아래에서 먼저 선택 조직의 상태, 구조 규모, 직속 구성원 유무, 시작 가능한 작업 후보를 보여주고, 그 다음에 구조 변경과 구성원 변경 실행 폼으로 이어지는 작업 중심 구조를 갖는다. 오른쪽 보조 패널에는 디렉터리와 직속 구성원, 운영 질문, 최근 이력을 모아 실행 전 점검과 사후 확인까지 한 흐름으로 묶었다. `node --check src/ui_prototype/app.js` 로 문법을 확인했고, `output/playwright/ui_prototype/debug/data_organizations_ipad_landscape_platform_after.png` 캡처로 아이패드 가로모드 기준 새 배치를 다시 확인했다.

## TASK-082 조직 트리 전용 가로 스크롤 정리

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 조직 관리 화면의 전체 레이아웃은 현재 뷰포트 폭에 맞게 유지하고, 피라미드 트리 패널 안에서만 가로 스크롤이 생기도록 오버플로우와 최소 폭 기준을 정리한다.
- 진행 현황:
  - 2026-04-09 사용자가 전체 화면이 보는 스크린 폭에 맞고, 피라미드 트리 영역만 가로 스크롤되도록 바꿔 달라고 요청했다.
  - 2026-04-09 현재 CSS 에서는 트리 자체 `min-width` 와 그리드/패널의 기본 `min-width:auto` 가 겹치면서 페이지 전체가 가로로 밀릴 가능성이 있다고 판단했다.
  - 2026-04-09 작업면, 패널, 실행 그리드에 `min-width:0` 을 명시하고, 조직 화면 전체에는 `overflow-x:hidden` 을 적용해 페이지 자체가 가로로 밀리지 않도록 정리했다.
  - 2026-04-09 트리 패널은 `overflow-x:auto`, `overflow-y:auto` 로 분리하고, 피라미드 패널도 `width:100%`, `max-width:100%` 기준으로 맞춰 트리 영역 안에서만 좌우 스크롤이 생기도록 조정했다.
  - 2026-04-09 수정 후 아이패드 가로모드 기준 렌더를 다시 확인해 전체 화면 폭은 유지되고, 넓은 피라미드 트리만 패널 내부에서 잘리는 형태로 노출되는 것을 확인했다.
- 완료 기준:
  - 페이지 전체는 뷰포트 폭 안에서 유지되어야 한다.
  - 피라미드 트리 패널 안에서만 가로 스크롤이 생겨야 한다.
  - 수정 후 렌더링으로 가로 스크롤 범위를 확인해야 한다.
- 작업 결과: 완료. 레이아웃 그리드와 주요 패널이 콘텐츠 폭 때문에 바깥으로 밀리지 않도록 `min-width:0` 기준을 추가했고, 조직 관리 화면 전체에는 가로 오버플로우를 막았다. 대신 `사업부 트리 맵` 의 트리 패널만 `overflow-x:auto` 로 분리해, 피라미드 트리가 화면보다 넓을 때 패널 안에서만 좌우 스크롤되도록 정리했다. 후속 점검에서 같은 요소에 함께 붙은 `.org-tree-panel` 과 `.org-pyramid-panel` 의 `overflow` 선언이 충돌해 세로 스크롤이 막히거나 트리가 영역 밖으로 삐져나가는 문제가 있었고, 최종적으로 `overflow` 책임은 `org-tree-panel` 하나만 갖도록 정리했다. 마지막 단계에서는 단순 패딩 증가로는 해결되지 않는다는 점을 렌더 좌표로 확인했고, `.org-pyramid-tree` 를 패널 폭 기준 가운데 정렬에서 콘텐츠 폭 기준 박스로 바꿔 가장 왼쪽 카드도 스크롤 가능한 영역 안에 포함되도록 수정했다. 추가로 FHD 기준 렌더 후 카드 극단 좌표를 계산해 트리 전체를 소폭 이동시키는 균형 보정 로직을 넣었고, 최종적으로 좌우 마지막 카드 여백이 `151px / 151px` 로 맞는 것을 확인했다. `node --check src/ui_prototype/app.js` 문법 확인과 `output/playwright/ui_prototype/debug/data_organizations_fhd_1920x1080_balanced.png` 렌더 재확인까지 마쳤다.

## TASK-083 넓은 화면 트리 여백 축소 및 말단 2열 묶음 정리

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 넓은 화면에서 트리 좌우 마지막 여백을 늘리는 보정보다 트리 폭 자체를 줄이는 방향으로 조정하고, 가장 말단 조직은 2열 종배열로 묶어 전체 폭을 압축한다.
- 진행 현황:
  - 2026-04-09 사용자가 넓은 화면 여백 균형은 늘리는 쪽보다 줄이는 쪽이 더 적절하고, 가장 말단 조직은 2열 종배열로 묶는 방향을 제안했다.
  - 2026-04-09 현재 구현은 렌더 후 좌우 카드 극단 좌표를 기준으로 트리 전체를 미세 이동시키는 방식이라, 전체 폭 자체를 줄이지는 못한다는 점을 확인했다.
  - 2026-04-09 카드 폭과 슬롯 패딩을 줄여 트리 전체 폭을 직접 압축하는 방향으로 `app.js` 와 `styles.css` 를 조정했다.
  - 2026-04-09 루트 서브트리 폭을 가변 폭으로 압축하는 시도는 오히려 중심부로 트리가 과도하게 모여 양끝 여백이 커지는 문제를 확인했고, 해당 접근은 폐기했다.
  - 2026-04-09 최종적으로는 동일 슬롯 구조를 유지하되 카드 폭을 `204px`, 슬롯 좌우 패딩을 `13px` 로 조정하고, 트리 최소 폭과 좌우 패딩을 함께 재조정했다.
  - 2026-04-09 FHD `1920x1080` 렌더를 다시 확인한 결과 트리 패널 `1454px` 안에서 콘텐츠 여백이 `169px / 161px` 수준으로 맞고, 가로 스크롤 폭도 `18px` 수준의 최소 범위로 줄어든 것을 확인했다.
  - 2026-04-09 말단 조직 2열 묶음은 `3개 이상 직속 말단 자식` 이 있는 경우에만 적용되도록 유지했다. 현재 검증 데이터의 `플랫폼사업부` 분기에서는 해당 조건이 없어 시각적 변화는 발생하지 않았다.
- 완료 기준:
  - 넓은 화면에서 트리 여백이 기존보다 과하게 넓지 않아야 한다.
  - 가장 말단 조직 묶음이 2열 종배열로 렌더링되어 전체 가로 폭이 줄어야 한다.
  - 수정 후 FHD 렌더로 결과를 확인해야 한다.
- 작업 결과: 완료. 넓은 화면 트리 폭 축소는 트리 전체를 억지로 좌우 이동시키는 보정보다 카드와 슬롯 자체를 조금 줄이는 방식으로 정리했다. 여러 렌더 시도 끝에 가변 서브트리 폭 모델은 오히려 양끝 여백을 크게 만들어 제외했고, 최종적으로는 균형이 안정적인 동일 슬롯 구조를 유지한 채 카드 폭과 슬롯 패딩을 소폭 축소해 FHD에서 트리가 덜 퍼지도록 맞췄다. 최종 검증 기준 `1920x1080` 에서 패널 폭 `1454px`, 트리 폭 `1464px`, 좌우 마지막 카드 여백 `169px / 161px` 로 확인됐고, 캡처는 `output/playwright/ui_prototype/debug/data_organizations_fhd_1920x1080_compact_final.png` 에 남겼다. 말단 2열 묶음은 데이터상 필요한 경우에만 동작하도록 유지했으며, 현재 더미 트리에서는 직속 말단 자식 수가 부족해 실제 적용 장면은 나타나지 않았다.

## TASK-084 데이터허브그룹 하위 파트 추가 및 2열 종배열 검증

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
  - `output/playwright/ui_prototype/debug/`
- 작업 내용: `데이터허브그룹` 아래에 검증용 하위 파트를 3개 추가하고, 조직 관리 트리에서 말단 조직 2열 종배열이 실제로 발동하는지 렌더링 기준으로 확인한다.
- 진행 현황:
  - 2026-04-09 사용자가 `데이터허브그룹` 에 하위 파트를 3개 추가해 2열 종배열 표현이 잘 보이는지 확인해 달라고 요청했다.
  - 2026-04-09 실제 조직 코드와 현재 API 데이터 상태를 조회한 결과 `데이터허브그룹` 의 조직 코드는 `group_data_hub` 이고, 기존 하위 파트는 `수집연계파트`, `표준화파트` 2개인 것을 확인했다.
  - 2026-04-09 `master-data` API 를 통해 `품질허브파트(part_quality_hub)`, `모델링파트(part_modeling_hub)`, `카탈로그파트(part_catalog_hub)` 를 `group_data_hub` 아래에 추가했다.
  - 2026-04-09 `GET /api/v1/admin/master-data/organizations/group_data_hub/structure` 로 구조 스냅샷을 다시 확인한 결과 하위 파트 5개가 정상 반영됐다.
  - 2026-04-09 FHD `1920x1080` 브라우저 렌더 기준으로 `데이터허브그룹` 하위 컨테이너에 `.leaf-grid` 가 실제로 생성되고, `grid-template-columns: 230px 230px` 2열 레이아웃으로 배치되는 것을 확인했다.
  - 2026-04-09 카드 좌표를 확인한 결과 `카탈로그파트/수집연계파트`, `모델링파트/표준화파트`, `품질허브파트` 순으로 2열 종배열이 형성됐고, 홀수 개수이므로 마지막 카드 1개는 왼쪽 열에 단독으로 배치됐다.
- 완료 기준:
  - `데이터허브그룹` 아래에 하위 파트 3개가 추가되어야 한다.
  - 조직 관리 트리에서 2열 종배열 적용 여부를 실제 렌더링으로 확인해야 한다.
  - 결과와 필요 시 후속 조정 포인트가 백로그에 기록되어야 한다.
- 작업 결과: 완료. `group_data_hub` 아래에 검증용 파트 3개를 추가해 총 5개의 직속 말단 파트가 생긴 상태에서 조직 관리 트리를 다시 렌더링했다. 그 결과 `데이터허브그룹` 하위 자식 컨테이너에 `.leaf-grid` 가 실제로 붙었고, `230px 230px` 2열 그리드로 종배열되는 것을 확인했다. 캡처는 `output/playwright/ui_prototype/debug/data_organizations_fhd_1920x1080_leaf_grid_check.png` 에 남겼다. 현재 5개와 같이 홀수 개수일 때는 마지막 카드가 왼쪽 열에 단독 배치되므로, 필요하면 후속으로 `마지막 행 단일 카드 중앙 정렬` 보정을 검토할 수 있다.

## TASK-085 조직 관리 작업면 탭형 흐름 재구성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 조직 관리 화면의 트리 아래 작업면을 `선택 조직 요약 -> 작업 탭 -> 실행 전 확인 -> 실행 -> 필요 시 이력` 흐름으로 재구성하고, 구조 변경/구성원 변경 패널을 탭형 작업면으로 압축한다.
- 진행 현황:
  - 2026-04-09 사용자가 트리 다음 단계로 작업면을 위 시나리오 기준으로 재구성하자고 요청했다.
  - 2026-04-09 현재 화면은 선택 컨텍스트, 구조 변경, 구성원 변경, 실행 전 확인, 이력이 모두 펼쳐져 있어 흐름은 맞지만 패널 수가 많아 보인다고 판단했다.
  - 2026-04-09 선택 요약 영역을 `상세/구조/직속 구성원` 과 `다음 작업/영향 경고` 의 2단 흐름으로 압축하고, 기존 분산 요약 패널을 재배치했다.
  - 2026-04-09 구조 변경/구성원 변경 실행 패널을 `org-workbench-tab` 기반 탭 작업면으로 전환하고, 각 패널의 영향 미리보기를 폼 상단의 실행 전 확인 블록으로 올렸다.
  - 2026-04-09 실행 전 확인 패널은 `Lookup · Members · Checklist` 중심으로 유지하고, 조직/구성원 이력은 `details` 기반 감사 영역으로 접는 형태로 바꿨다.
  - 2026-04-09 `app.js` 에 작업면 탭 상태 저장과 렌더 함수를 추가하고, 필터 초기화 시 범위를 `business_unit` 으로 되돌리도록 정리했다.
  - 2026-04-09 `node --check src/ui_prototype/app.js` 문법 확인과 브라우저 렌더 검증을 통해 기본 활성 탭이 `structure`, 감사 영역 2개가 기본 접힘, 탭 전환 시 해당 패널만 노출되는 것을 확인했다.
- 완료 기준:
  - 선택 조직 요약이 현재보다 더 압축된 흐름으로 보일 것
  - 구조 변경/구성원 변경이 탭형 작업면으로 전환될 것
  - 이력 영역이 기본 접힘 상태의 감사 영역으로 바뀔 것
  - 문법 검증과 렌더 확인이 완료될 것
- 작업 결과: 완료. 조직 관리 화면의 트리 아래 작업면은 이제 `선택 조직 요약 -> 작업 탭 -> 실행 전 확인 -> 실행 -> 필요 시 감사 열기` 흐름으로 정리됐다. 선택 조직 요약은 상단 배너 아래에서 `상세/구조/직속 구성원` 과 `바로 시작 가능한 작업/주의할 영향` 으로 압축됐고, 실행 영역은 `구조 변경` 과 `구성원 변경` 탭 중 하나만 열리도록 바뀌었다. 조직 변경 이력과 구성원 이동 이력은 기본 접힘 상태의 감사 영역으로 옮겨 항상 화면을 차지하지 않도록 정리했다. 검증 캡처는 `output/playwright/ui_prototype/debug/data_organizations_workbench_structure.png` 와 `output/playwright/ui_prototype/debug/data_organizations_workbench_member.png` 에 남겼다.

## TASK-086 조직 트리 조상 경로 하이라이트 대비 강화

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 트리에서 조직 선택 시 함께 하이라이트되는 상위 조직 경로가 더 눈에 잘 띄도록 `path-active` 카드의 색 대비와 시각 강조를 강화한다.
- 진행 현황:
  - 2026-04-09 사용자가 선택 조직의 상위 조직 하이라이트가 현재보다 더 눈에 띄게 보이도록 색을 조정해 달라고 요청했다.
  - 2026-04-09 현재 `path-active` 는 선택 카드보다 한 단계 옅은 배경과 테두리만 적용되어 실제 화면에서는 강조 차이가 충분히 크지 않다고 판단했다.
  - 2026-04-09 조상 경로 카드의 배경을 더 진한 청색 계열로 바꾸고, 테두리/그림자/상태 칩 색을 함께 올려 기본 카드보다 한눈에 구분되도록 조정했다.
  - 2026-04-09 `1440x1200` 렌더에서 `품질허브파트` 를 선택해 경로 카드가 `플랫폼사업부 -> 통합플랫폼팀 -> 데이터허브그룹` 으로 정상 강조되는 것을 확인했다.
- 완료 기준:
  - 상위 조직 경로 카드가 기본 카드보다 명확히 강조되어야 한다.
  - 선택 카드와 조상 경로 카드가 서로 구분 가능해야 한다.
  - 수정 후 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. `path-active` 카드의 배경과 테두리 대비를 올리고, 글자색과 상태 칩도 같은 계열로 맞춰 선택 카드까지 이어지는 조상 경로가 더 또렷하게 읽히도록 조정했다. 선택 카드(`active`)는 여전히 가장 강한 강조를 유지하고, 조상 경로 카드는 그보다 한 단계 낮지만 기본 카드와는 분명히 구분되는 수준으로 맞췄다. 확인 캡처는 `output/playwright/ui_prototype/debug/data_organizations_path_highlight_stronger.png` 에 남겼다.

## TASK-087 선택 조직 요약 압축 및 작업 지향 문구 정리

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 선택 조직 요약 영역을 더 짧고 간결하게 줄이고, 조직 관리 화면의 각 패널 제목과 설명을 조회형보다 작업 지향 문구로 다시 정리한다.
- 진행 현황:
  - 2026-04-09 사용자가 선택 조직 요약을 더 짧게 만들고 각 메뉴를 작업 지향적으로 구성해 달라고 요청했다.
  - 2026-04-09 현재 요약 함수들은 구조 설명이 다소 길고, 패널 제목도 `요약/확인/이력` 중심이라 실행 흐름이 약하다고 판단했다.
  - 2026-04-09 선택 조직 관련 요약 함수의 문장을 줄이고, 출력 항목 수를 `프로필 2개`, `구조 2개`, `직속 구성원 1개` 수준으로 압축했다.
  - 2026-04-09 `선택 조직에서 시작`, `구조 조정 실행`, `구성원 배치 실행`, `실행 전 점검`, `변경 추적` 등으로 패널 제목을 바꾸고 설명도 작업 동사 중심으로 정리했다.
  - 2026-04-09 `상위 조직 바꾸기`, `하위 조직 다루기`, `구성원 배치 바꾸기` 처럼 바로 행동으로 이어지는 작업 후보 문구로 교체했다.
  - 2026-04-09 `node --check src/ui_prototype/app.js` 문법 확인과 렌더 검증을 통해 제목 변경과 요약 카드 축소가 실제 화면에 반영된 것을 확인했다.
- 완료 기준:
  - 선택 조직 요약이 현재보다 짧고 빠르게 읽혀야 한다.
  - 각 패널의 제목과 설명이 실행 중심으로 정리되어야 한다.
  - 문법 검증과 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. 선택 조직 요약은 더 이상 장황한 설명 위주가 아니라 `현재 위치`, `영향 범위`, `직속 구성원` 중심의 짧은 카드로 정리됐다. 동시에 작업면 제목과 보조 문구도 조회형 표현 대신 실행형 표현으로 바꿔, 사용자가 트리에서 조직을 고른 뒤 바로 `무엇을 할지` 읽히도록 맞췄다. 렌더 확인 기준 주요 제목은 `선택 조직에서 시작`, `구조 조정 실행`, `구성원 배치 실행`, `실행 전 점검`, `변경 추적` 으로 반영됐고, 캡처는 `output/playwright/ui_prototype/debug/data_organizations_summary_compact.png` 에 남겼다.

## TASK-088 실행 전 점검 패널 빠른 액션화

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: `실행 전 점검` 패널과 선택 배너를 단순 조회 영역이 아니라 바로 탭 작업면으로 이어지는 빠른 액션 영역으로 바꾸고, 조직/구성원 표 클릭도 각각 해당 작업 탭으로 연결한다.
- 진행 현황:
  - 2026-04-09 사용자가 이어서 진행해 달라고 요청했고, 현재 점검 패널은 여전히 `무엇을 볼까` 중심이라 작업 시작 연결이 약하다고 판단했다.
  - 2026-04-09 선택 배너 아래에 `구조 조정 시작`, `구성원 배치 시작` 빠른 액션 버튼을 추가하고, 각 버튼이 해당 탭을 직접 열도록 연결했다.
  - 2026-04-09 `실행 전 점검` 패널의 두 표를 `작업할 조직 고르기`, `작업할 구성원 고르기` 로 재라벨링하고, 클릭 시 어떤 작업면으로 이어지는지 설명 문구를 추가했다.
  - 2026-04-09 조직 표 행 클릭은 `구조 조정` 탭을, 구성원 표 행 클릭은 `구성원 배치` 탭을 자동 활성화하도록 이벤트 흐름을 조정했다.
  - 2026-04-09 브라우저 검증 기준 빠른 버튼 클릭 시 `member` 탭, 조직 표 클릭 시 `structure` 탭이 정상 활성화되는 것을 확인했다. 검증 시점 선택 조직에는 직속 구성원이 없어 구성원 표 클릭 시나리오는 빈 상태였다.
- 완료 기준:
  - 선택 영역과 점검 패널에서 빠른 액션이 보여야 한다.
  - 조직 표 클릭 시 구조 조정 탭으로, 구성원 표 클릭 시 구성원 배치 탭으로 연결되어야 한다.
  - 문법 검증과 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. 조직 관리 화면의 선택 배너와 `실행 전 점검` 패널은 이제 단순 조회 영역이 아니라 작업 시작 허브처럼 동작한다. 사용자는 배너의 빠른 버튼으로 바로 `구조 조정` 또는 `구성원 배치` 탭을 열 수 있고, 아래 표에서도 조직 행을 누르면 구조 조정 탭으로, 구성원 행을 누르면 구성원 배치 탭으로 즉시 이어진다. 점검 패널의 표 제목도 `작업할 조직 고르기`, `작업할 구성원 고르기` 로 바뀌어 무엇을 해야 하는지 더 직접적으로 읽힌다. 확인 캡처는 `output/playwright/ui_prototype/debug/data_organizations_quick_actions.png` 에 남겼다.

## TASK-089 인라인 액션 버튼 추가 및 빠른 버튼 문구 보정

- 상태: done
- 우선순위: low
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 점검 패널 표 안에 인라인 액션 버튼을 추가하고, 상단 빠른 버튼 문구를 `구조 조정 시작` 에서 `조직 개편 시작` 으로 보정한다.
- 진행 현황:
  - 2026-04-09 사용자가 인라인 액션 버튼 추가와 빠른 버튼 문구 보정을 요청했다.
  - 2026-04-09 상단 빠른 버튼 문구를 `구조 조정 시작` 에서 `조직 개편 시작` 으로 바꾸고, 점검 패널 안내 문구도 `행이나 버튼` 기준으로 맞췄다.
  - 2026-04-09 조직 표에는 `개편`, 구성원 표에는 `배치` 인라인 액션 버튼이 보이도록 행 렌더링을 수정하고, 상태/직군 셀 안에서 바로 누를 수 있게 정리했다.
  - 2026-04-09 인라인 버튼은 별도 이벤트를 추가하지 않고 기존 행 클릭 흐름과 함께 동작하도록 유지해, 버튼을 눌러도 해당 탭 활성화와 폼 연동이 그대로 이어지게 했다.
  - 2026-04-09 브라우저 검증 기준 상단 버튼 라벨이 `조직 개편 시작` 으로 반영됐고, 조직 표 인라인 버튼 4개가 렌더되며 클릭 시 `structure` 탭이 정상 활성화되는 것을 확인했다. 검증 시점 선택 조직에는 직속 구성원이 없어 구성원 표 인라인 버튼은 빈 상태였다.
- 완료 기준:
  - 상단 빠른 버튼 문구가 `조직 개편 시작` 으로 바뀌어야 한다.
  - 조직/구성원 표 각 행에 인라인 액션 버튼이 보여야 한다.
  - 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. 선택 배너의 빠른 버튼은 `조직 개편 시작` 으로 더 자연스럽게 바뀌었고, `실행 전 점검` 패널의 표 안에는 바로 누를 수 있는 인라인 액션 버튼이 추가됐다. 이제 사용자는 조직 표에서 `개편`, 구성원 표에서 `배치` 버튼을 보고 어떤 작업으로 이어지는지 더 직접적으로 이해할 수 있다. 확인 캡처는 `output/playwright/ui_prototype/debug/data_organizations_inline_actions.png` 에 남겼다.

## TASK-090 조직 리스트 제거 및 조직 개편 문구 정리

- 상태: done
- 우선순위: low
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 조직 선택은 트리로만 통일하고 `실행 전 점검` 패널에서 조직 리스트를 제거한다. 함께 구조 탭의 문구를 `조직 개편` 기준으로 정리한다.
- 진행 현황:
  - 2026-04-09 사용자가 조직 리스트는 필요 없고 트리로 선택을 통일하자고 요청했다.
  - 2026-04-09 `실행 전 점검` 패널에서 조직 리스트 블록을 제거하고, `조직 선택은 트리에서 진행` 안내 블록으로 대체했다.
  - 2026-04-09 구성원 표만 남겨 선택된 조직 기준 사람 점검 흐름으로 단순화했고, 점검 패널 보조 문구도 트리 선택 전제에 맞춰 다시 정리했다.
  - 2026-04-09 빠른 버튼, 탭 이름, 구조 패널 제목을 각각 `조직 개편 시작`, `조직 개편`, `조직 개편 실행` 으로 바꿔 문구를 통일했다.
  - 2026-04-09 정적 구조 확인과 브라우저 렌더 캡처로 조직 리스트 제거 및 문구 변경 반영을 확인했다.
- 완료 기준:
  - 점검 패널에서 조직 리스트가 제거되어야 한다.
  - 조직 선택 흐름이 트리 중심으로 읽혀야 한다.
  - 구조 탭 문구가 `조직 개편` 기준으로 바뀌어야 한다.
  - 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. 조직 선택은 이제 상단 트리에서만 하도록 통일됐고, `실행 전 점검` 패널에는 조직 리스트 대신 트리 선택 안내와 선택된 조직의 직속 구성원 점검만 남겼다. 구조 탭 관련 문구도 `조직 개편 시작`, `조직 개편`, `조직 개편 실행` 으로 맞춰 한 흐름으로 읽히게 정리했다. 렌더 캡처는 `output/playwright/ui_prototype/debug/data_organizations_tree_only.png` 에 남겼다.

## TASK-091 버튼-화면 이동 연동 및 트리 액션 방식 검토

- 상태: done
- 우선순위: low
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 버튼과 연결된 작업 화면이 실제로 보이도록 탭 전환 시 해당 패널로 스크롤 이동을 연동하고, 트리에서 하위 조직 추가/삭제를 간편하게 수행하는 방식의 적용 가능성을 현재 구조 기준으로 검토한다.
- 진행 현황:
  - 2026-04-09 사용자가 버튼과 화면이 연동되는 경우 버튼 클릭 시 해당 화면으로 이동하도록 해 달라고 요청했다.
  - 2026-04-09 탭 활성화 함수에 작업 패널 `scrollIntoView` 를 추가해 빠른 버튼이나 탭 클릭 시 해당 작업면이 실제로 보이도록 조정했다.
  - 2026-04-09 트리 카드 선택과 사업부 전환 시에는 사용자가 트리를 계속 보고 있는 맥락을 유지해야 하므로 탭만 동기화하고 스크롤은 생략하도록 분리했다.
  - 2026-04-09 조직 리스트 제거 이후 남아 있던 조직 표 이벤트 흐름도 정리했다.
  - 2026-04-09 함께 트리에서 하위 조직 추가/삭제 기능을 간편하게 수행할 수 있는지 검토했고, 현재 API 제약상 `하위 조직 추가` 는 선택 카드 기준 상시 제공 가능하지만 `삭제` 는 `직속 하위 없음 + 직속 구성원 없음` 조건일 때만 노출하는 방식이 적절하다고 판단했다.
  - 2026-04-09 이에 따라 다음 구현안으로는 선택된 트리 카드 주변에 `하위 추가`, `조직 개편`, `삭제 검토` 액션 바를 띄우는 방식이 가장 자연스럽다고 정리했다.
- 완료 기준:
  - 탭 전환 또는 빠른 액션 버튼 클릭 시 해당 작업 패널로 스크롤 이동해야 한다.
  - 트리 액션 방식 검토 결과가 정리되어야 한다.
  - 문법 검증이 완료되어야 한다.
- 작업 결과: 완료. 버튼과 화면 연동은 탭 전환 함수에 스크롤 이동을 추가해 해결했다. 이제 `조직 개편 시작`, `구성원 배치 시작`, 탭 버튼을 누르면 해당 작업 패널이 바로 화면에 보인다. 반면 트리 카드 선택이나 사업부 전환은 사용자가 트리를 보고 있는 흐름을 유지해야 하므로 탭만 맞추고 스크롤은 강제로 이동시키지 않도록 분리했다. 트리에서 하위 조직 추가/삭제를 간편하게 제공하는 방식도 검토했고, 현재 구조에서는 선택된 카드 주변에 `하위 추가`, `조직 개편`, `삭제 검토` 정도의 소형 액션 바를 띄우는 방향이 가장 적합하다. 삭제는 API 제약을 반영해 말단 조직일 때만 활성화하는 것이 맞다.

## TASK-092 트리 선택 조직 액션 바 추가

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 트리에서 선택한 조직 기준으로 `하위 추가`, `조직 개편`, `삭제 검토` 를 바로 시작할 수 있는 액션 바를 추가하고, 삭제는 말단 조직 조건일 때만 활성화한다.
- 진행 현황:
  - 2026-04-09 사용자가 트리에서 하위 조직 추가/삭제 기능을 간편하게 수행하는 방향을 실제로 추가해 보자고 요청했다.
  - 2026-04-09 트리 아래에 선택 조직 전용 액션 바를 추가하고, 선택 조직이 없을 때는 안내 문구만 보이도록 구성했다.
  - 2026-04-09 선택 조직 기준으로 직속 하위 조직 수와 직속 구성원 수를 계산해 `하위 추가`, `조직 개편`, `삭제 검토` 버튼을 렌더링하도록 구현했다.
  - 2026-04-09 `하위 추가` 는 구조 작업면을 열고 상위 조직 코드를 선택 조직으로 미리 채운 신규 생성 준비 상태로 연결했다.
  - 2026-04-09 `조직 개편` 은 현재 선택 조직을 구조 작업 폼에 그대로 채우고, 해당 작업면으로 스크롤 이동하도록 연결했다.
  - 2026-04-09 `삭제 검토` 는 직속 하위 조직과 직속 구성원이 모두 없을 때만 활성화되도록 했고, 선택 조직 삭제 검토 상태 메시지를 구조 작업면에 남기도록 연결했다.
  - 2026-04-09 `node --check src/ui_prototype/app.js` 문법 검증은 통과했다. 브라우저 자동 검증은 클릭 후 응답 대기 시간이 길어 상세 시나리오를 끝까지 캡처하지는 못했지만, 정적 렌더 캡처와 코드 경로 기준으로 반영 상태를 확인했다.
- 완료 기준:
  - 트리 아래에 선택 조직 액션 바가 보여야 한다.
  - `하위 추가`, `조직 개편`, `삭제 검토` 가 각각 구조 작업면과 연결되어야 한다.
  - 삭제 검토는 말단 조직 조건일 때만 활성화되어야 한다.
  - 문법 검증과 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. 조직 트리는 이제 단순 선택기보다 작업 시작점에 가깝게 동작한다. 트리 아래 액션 바에서 선택 조직 기준 `하위 추가`, `조직 개편`, `삭제 검토` 를 바로 시작할 수 있고, 삭제 검토는 말단 조직일 때만 활성화되도록 제약을 반영했다. 이 방식은 별도 트리 내부 아이콘을 억지로 넣기보다 현재 카드 선택 흐름을 유지하면서도 행동을 즉시 시작할 수 있어 구조적으로 무리가 적다. 정적 렌더 캡처는 `output/playwright/ui_prototype/debug/data_organizations_tree_actions.png` 기준으로 확인했다.

## TASK-093 선택 카드 아이콘 액션 적용

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 선택된 트리 카드에만 우상단 `+ / -` 아이콘 액션을 노출하고, `+` 는 하위 조직 추가, `-` 는 삭제 검토로 연결한다.
- 진행 현황:
  - 2026-04-09 사용자가 선택 카드에 아이콘형 액션을 붙이는 안을 먼저 진행하자고 요청했다.
  - 2026-04-09 카드 마크업을 선택 카드 기준 액션 오버레이를 포함하는 구조로 바꾸고, 선택된 카드에만 우상단 `+ / -` 버튼이 보이도록 정리했다.
  - 2026-04-09 `+` 는 하위 조직 추가 준비 상태로, `-` 는 삭제 검토 상태로 각각 구조 작업면을 여는 흐름에 연결했다.
  - 2026-04-09 `-` 는 직속 하위 조직과 직속 구성원이 모두 없을 때만 활성화되도록 기존 삭제 검토 조건을 그대로 반영했다.
  - 2026-04-09 트리 클릭 이벤트에서는 카드 선택보다 아이콘 액션을 먼저 처리하도록 우선순위를 조정했다.
  - 2026-04-09 `node --check src/ui_prototype/app.js` 문법 검증은 통과했다. 브라우저 자동 검증은 이 세션에서 응답 대기가 길어 상세 시나리오를 끝까지 수집하진 못했지만, 코드 경로와 정적 렌더 캡처 기준으로 반영 상태를 확인했다.
- 완료 기준:
  - 선택된 카드에만 `+ / -` 액션이 보여야 한다.
  - `+` 는 하위 추가, `-` 는 삭제 검토로 연결되어야 한다.
  - `-` 는 말단 조직일 때만 활성화되어야 한다.
  - 문법 검증과 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. 선택된 트리 카드에는 이제 우상단에 `+ / -` 아이콘 액션이 붙고, `+` 는 하위 조직 추가, `-` 는 삭제 검토를 시작하는 용도로 연결된다. 모든 카드에 상시 노출하는 방식이 아니라 선택 카드에만 노출하도록 제한해 트리 가독성은 유지하면서도 행동 시작 지점을 카드 가까이 끌어올렸다. 정적 렌더 캡처는 `output/playwright/ui_prototype/debug/data_organizations_card_icons.png` 에 남겼다.

## TASK-094 트리 인라인 생성 카드 및 삭제 확인 팝업 적용

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 선택 카드의 `-` 는 경고 팝업 확인 후 실제 삭제까지 수행하고, `+` 는 트리 안에서 타입·이름·키를 입력하는 인라인 생성 카드로 기본 조직 오브젝트를 만든 뒤 별도 상세 다이얼로그에서 추가 정보를 작성하도록 바꾼다.
- 진행 현황:
  - 2026-04-09 사용자가 `-` 는 경고 팝업 후 확인 삭제, `+` 는 트리 안 기본 생성 카드 후 별도 상세 다이얼로그 흐름으로 구성하자고 요청했다.
  - 2026-04-09 기존 `+ / -` 는 구조 작업면으로 이동시키는 수준이라, 트리 안 즉시 실행 흐름과는 거리가 있다고 판단했다.
  - 2026-04-09 트리 부모 카드 아래에 임시 하위 카드 슬롯을 렌더링하고, 생성 직후 상세 모달로 이어지도록 상태와 이벤트 흐름을 재구성했다.
  - 2026-04-09 `data_organizations.html` 에 삭제 확인 팝업과 신규 조직 상세 다이얼로그를 추가하고, `styles.css` 에 트리 인라인 생성 카드와 모달 스타일을 반영했다.
  - 2026-04-09 `app.js` 에 트리 전용 UI 상태, 타입·키 기반 조직 코드 생성 규칙, 인라인 생성 카드 렌더링, 삭제 확인 팝업 열기/닫기, 생성 후 상세 다이얼로그 열기/저장 로직을 추가했다.
  - 2026-04-09 선택 카드 우상단 `+ / -` 와 트리 아래 액션 바가 모두 같은 생성/삭제 흐름을 타도록 통일했다.
  - 2026-04-09 후속 확인 중 `+ / -` 버튼이 안 보인다는 피드백을 받아, DOM 존재는 확인했고 선택 카드 안쪽으로 더 크게 보이도록 위치, 크기, 배경 대비, `z-index` 를 보강했다.
  - 2026-04-09 `node --check src/ui_prototype/app.js` 문법 확인과 로컬 `UI 200`, `GET /api/v1/health` 응답을 확인했다. 브라우저 자동 검증은 `/.playwright-mcp` 권한 오류로 완료하지 못했다.
- 완료 기준:
  - `-` 클릭 시 삭제 경고 팝업이 열리고, 확인 버튼으로 실제 삭제가 수행되어야 한다.
  - `+` 클릭 시 트리 안에 타입·이름·키 입력 카드가 열리고, 생성 후 상세 다이얼로그가 열려야 한다.
  - 선택 카드 액션과 하단 트리 액션 바가 같은 동작으로 통일되어야 한다.
  - 문법 검증과 기본 렌더 확인이 완료되어야 한다.
- 작업 결과: 완료. 트리 선택 카드의 `-` 는 이제 별도 경고 팝업을 열고, 확인 시 실제 삭제 API를 호출한다. `+` 는 구조 작업면으로 보내지 않고 선택 부모 아래에 인라인 생성 카드를 열어 타입·이름·키만 입력받아 기본 조직 오브젝트를 만든다. 생성이 끝나면 곧바로 상세 다이얼로그가 열려 상태와 유효 기간을 이어서 저장할 수 있다. 선택 카드 아이콘과 트리 아래 액션 바도 같은 흐름으로 통일했다. 자동 브라우저 검증은 이 세션의 `Playwright` 권한 오류로 마무리하지 못했고, 대신 스크립트 문법과 로컬 서비스 응답까지 확인했다.

## TASK-095 조직 삭제 시 구성원 미배정 전환 및 인력 관리 미배정 분리 표시

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `backend/src/services/master_data.rs`
  - `backend/src/http/routes/master_data.rs`
  - `backend/migrations/20260409080000_make_workforce_primary_org_nullable.up.sql`
  - `backend/tests/master_data_service.rs`
  - `backend/tests/http.rs`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/data_workforce.html`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 말단 조직은 직속 구성원이 있어도 삭제할 수 있도록 정책을 바꾸고, 삭제 시 해당 구성원은 조직 미배정 상태로 전환한다. 인력 관리 화면에는 미배정 인력을 별도 목록으로 표시하고 다시 배치할 수 있게 한다.
- 진행 현황:
  - 2026-04-09 사용자가 말단 조직은 구성원이 있어도 삭제 가능해야 하고, 삭제된 조직의 구성원은 조직 미배정 상태가 되어야 하며, 미배정 인력은 인력 관리 화면에서 따로 보이게 해 달라고 요청했다.
  - 2026-04-09 기존 백엔드는 `조직 삭제 시 직속 활성 구성원이 있으면 삭제 차단`, `workforce_master.primary_organization_id not null`, `workforce` 조회 시 조직 조인 필수 구조라 미배정을 표현할 수 없었다.
  - 2026-04-09 `workforce_master.primary_organization_id` 를 nullable 로 바꾸는 마이그레이션을 추가하고, 백엔드 `WorkforceRecord` 및 관련 저장/조회 로직을 `Option` 기반으로 수정했다.
  - 2026-04-09 조직 삭제 로직은 하위 조직만 없으면 삭제 가능하도록 바꾸고, 삭제 시 해당 조직 구성원을 일괄 미배정 처리한 뒤 `unassign` 이력을 남기도록 변경했다.
  - 2026-04-09 `cargo test --manifest-path backend/Cargo.toml -- --nocapture` 전체 테스트를 통과했고, 신규 HTTP 테스트로 삭제 후 미배정 전환 시나리오도 검증했다.
  - 2026-04-09 인력 관리 화면은 전체 활성 인력을 한 번에 읽은 뒤 `조직 필터 목록` 과 `미배정 인력` 목록으로 나눠 렌더링하도록 변경했고, 미배정 인력도 선택 및 재배치가 가능하도록 저장 경로를 보정했다.
  - 2026-04-09 조직 관리 화면의 삭제 가능 조건과 경고 문구도 새 정책에 맞춰 `하위 조직 없음` 기준으로 정리했다.
- 완료 기준:
  - 말단 조직은 직속 구성원이 있어도 삭제되어야 한다.
  - 삭제된 조직의 구성원은 `primary_organization_code = null` 상태로 남아야 한다.
  - 인력 관리 화면에서 미배정 인력이 별도 목록으로 보여야 한다.
  - 백엔드 테스트와 프론트 스크립트 문법 검증이 완료되어야 한다.
- 작업 결과: 완료. 조직 삭제 정책은 이제 `하위 조직 없음` 기준으로만 제한되고, 직속 구성원이 있어도 삭제 시 구성원은 자동으로 미배정 상태로 전환된다. 이를 위해 백엔드 저장 구조와 조회 모델을 nullable 조직 참조를 허용하도록 수정했고, 삭제 시 `unassign` 이력도 남도록 바꿨다. 인력 관리 화면은 전체 활성 인력을 읽은 뒤 선택 조직 목록과 미배정 목록을 분리해 보여주며, 미배정 인력도 선택 후 다시 조직에 배치할 수 있다. 검증은 전체 `cargo test` 와 `node --check src/ui_prototype/app.js` 로 완료했다.

## TASK-096 관리자 UI 상태 문구 기준 정리

- 상태: done
- 우선순위: low
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/data_workforce.html`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 관리자 UI에서 기본 상태를 설명할 때 `활성` 표현을 남발하지 않고, 기본은 중립 표현으로 두며 비활성 상태만 `비활성` 으로 명시하는 문구 기준으로 정리한다.
- 진행 현황:
  - 2026-04-09 사용자가 `활성` 문구가 어색하니 기본 표현에서는 빼고, 반대로 비활성일 때만 비활성으로 표시하자고 요청했다.
  - 2026-04-09 조직 관리와 인력 관리 화면에서 직접 보이는 메트릭 라벨, 배너, 삭제 경고, 요약 문구를 우선 점검했다.
  - 2026-04-09 `조직/인력/직속 구성원/인원` 같은 중립 표현으로 바꾸고, 인력 선택 배너는 `inactive` 일 때만 `비활성` 배지가 붙도록 정리했다.
  - 2026-04-09 `node --check src/ui_prototype/app.js` 와 `rg` 재검색으로 현재 대상 화면의 주요 `활성` 문구가 제거된 것을 확인했다.
- 완료 기준:
  - 조직/인력 관리자 화면에서 기본 상태 설명에 `활성` 이 남지 않아야 한다.
  - 비활성 상태는 필요할 때만 `비활성` 으로 표시되어야 한다.
  - 스크립트 문법 검증이 완료되어야 한다.
- 작업 결과: 완료. 조직 관리와 인력 관리 화면의 현재 작업 흐름에서 보이는 상태 문구는 `활성 조직`, `활성 인력`, `직속 활성 구성원` 같은 표현 대신 `조직`, `인력`, `직속 구성원`, `인원` 으로 정리했다. 반면 `inactive` 상태는 별도 `비활성` 표시로만 드러나도록 기준을 맞췄다. 앞으로 이어지는 UI 문구도 같은 기준으로 유지한다.

## TASK-097 트리 카드 액션 일원화 및 수정 버튼 추가

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일:
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 트리 아래 `선택 조직 액션` 영역을 제거하고, 선택 카드 우상단 액션만 남긴다. 함께 카드 액션에 `수정` 버튼을 추가해 구조 작업면으로 바로 연결한다.
- 진행 현황:
  - 2026-04-09 사용자가 하단 선택 조직 액션은 없어도 되고, 수정 버튼을 카드에 넣어 달라고 요청했다.
  - 2026-04-09 `data_organizations.html` 에서 트리 아래 액션 블록을 제거했다.
  - 2026-04-09 `app.js` 에서 하단 액션 바 렌더와 클릭 이벤트를 제거하고, 선택 카드 우상단 액션에 `수정` 버튼을 추가했다.
  - 2026-04-09 `수정` 버튼은 기존 구조 작업면과 연결해 현재 조직 폼을 채운 뒤 바로 편집 상태로 진입하도록 맞췄다.
  - 2026-04-09 `styles.css` 에 텍스트형 액션 버튼 스타일을 추가해 `수정` 이 `+ / -` 와 같은 줄에서 읽히도록 정리했다.
  - 2026-04-09 후속 요청에 따라 `수정` 은 액션 바 왼쪽, `+ / -` 는 오른쪽에 두고 아이콘 크기도 줄여 카드 본문을 덜 가리도록 다시 보정했다.
  - 2026-04-09 추가 요청에 따라 카드 액션 버튼 크기를 `26px` 수준으로 더 줄이고, `수정` 버튼 폭과 글자 크기도 함께 조정해 카드 내용 가림을 더 줄였다.
  - 2026-04-09 후속 요청에 따라 `수정` 클릭 시 아래 작업면으로 이동하지 않고 기존 조직 상세 모달을 편집 모드로 열도록 변경했다.
  - 2026-04-09 조직 상세 모달은 생성/수정 공용 다이얼로그로 정리했고, 편집 모드에서는 `PATCH /admin/master-data/organizations/{organization_code}` 로 저장하도록 분기했다.
  - 2026-04-09 추가 요청에 따라 `수정` 은 모달 대신 선택 카드 자체를 인라인 편집 카드로 치환하고, 폭도 더 넓게 펴지도록 다시 변경했다.
  - 2026-04-09 `+` 는 카드 내용 구성과 폭만 수정 카드와 같은 계열로 맞추고, 부모 카드는 유지한 채 더 넓은 인라인 생성 카드가 하나 추가되도록 정리했다.
  - 2026-04-09 새 하위 조직 기본 생성 후에는 모달을 띄우지 않고, 방금 만든 조직 카드 자체가 더 넓은 인라인 편집 카드로 바로 전환되도록 연결했다.
- 완료 기준:
  - 트리 아래 선택 조직 액션 영역이 제거되어야 한다.
  - 선택 카드 우상단에 `수정` 버튼이 추가되어야 한다.
  - `수정` 클릭 시 선택 카드가 더 넓은 인라인 편집 카드로 치환되어야 한다.
  - `+` 클릭 시 선택 카드는 유지되고, 같은 계열의 인라인 생성 카드가 하나 추가되어야 한다.
  - 스크립트 문법 검증이 완료되어야 한다.
- 작업 결과: 완료. 트리 아래 별도 `선택 조직 액션` 영역은 제거했고, 선택 카드 우상단 액션만 남겼다. 카드 액션에는 `수정`, `+`, `-` 가 함께 보이며, `수정` 은 해당 카드를 더 넓은 인라인 편집 카드로 바로 치환해 이름, 상위 조직, 상태, 유효 기간을 같은 자리에서 수정할 수 있다. `+` 는 부모 카드를 유지한 채 같은 계열의 더 넓은 인라인 생성 카드를 하나 추가하며, 새 하위 조직 기본 생성 후에는 방금 만든 조직 카드 자체가 인라인 편집 카드로 전환된다. 편집 저장은 `PATCH` 로 처리되도록 정리했다. 하단 액션 바 렌더와 이벤트도 함께 제거해 액션 진입점이 카드 쪽으로 일원화됐다. `node --check src/ui_prototype/app.js` 로 문법을 확인했다.

## TASK-098 브랜치 최신화 및 타 환경 작업 팔로업 정리

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 현재 로컬 `main` 브랜치를 원격 최신 상태로 동기화하고, 다른 환경에서 같은 날짜에 진행된 작업 내역을 읽어 후속 기준선을 정리한다.
- 진행 현황:
  - 2026-04-09 세션 시작 후 `docs/operations/work_backlog.md` 와 최근 날짜 백로그를 먼저 확인했다.
  - 2026-04-09 로컬 `main` 은 `origin/main` 대비 1커밋 뒤처져 있었고, 원격 최신 커밋은 `0321fe4 Refine organization admin flows` 임을 확인했다.
  - 2026-04-09 `git pull --ff-only origin main` 으로 로컬 브랜치를 fast-forward 했고, 이후 `HEAD` 와 `origin/main` 이 모두 `0321fe4` 로 일치함을 재확인했다.
  - 2026-04-09 오늘자 백로그 전체 상태를 점검한 결과 `in_progress`, `blocked`, `planned` 상태 작업은 남아 있지 않았고, 원격 작업은 모두 `done` 으로 정리돼 있음을 확인했다.
  - 2026-04-09 후속 기준선으로 조직 삭제 시 구성원 미배정 전환, 인력 관리 미배정 분리 표시, 조직 트리 카드 액션 일원화까지 반영된 상태임을 정리했다.
- 완료 기준:
  - 로컬 브랜치가 원격 최신 커밋과 일치해야 한다.
  - 최근 백로그 기준 미완료 작업 유무가 확인되어야 한다.
  - 다른 환경에서 반영된 핵심 변경 포인트가 후속 작업 기준선으로 정리되어야 한다.
- 작업 결과: 완료. 로컬 `main` 은 현재 원격 `origin/main` 과 동일한 `0321fe4` 상태다. 최근 백로그 기준 미완료 작업은 없고, 오늘 다른 환경에서 반영된 주요 변경은 조직 삭제 정책 완화, 삭제 시 구성원 `unassign` 처리 및 이력 기록, 인력 관리 화면의 미배정 인력 분리 표시, 조직 트리 카드 액션의 `수정/+/-` 일원화다. 따라서 다음 작업은 이 기준선 위에서 바로 이어가면 된다.

## TASK-099 테스트 런타임 환경 기동

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 현재 환경에서 바로 확인 가능한 테스트용 런타임을 띄운다. 포트 충돌을 피하면서 `PostgreSQL`, Rust 백엔드, 정적 UI 서버의 기동 가능 상태를 맞추고 최소 헬스 체크를 수행한다.
- 진행 현황:
  - 2026-04-09 기본 진입점 문서와 포트 점유 상태를 확인했고, `5432` 는 이미 다른 프로세스가 점유 중임을 확인했다.
  - 2026-04-09 충돌 회피를 위해 검증 포트를 `PostgreSQL 55432`, Rust 백엔드 `28080`, 정적 UI `8000` 기준으로 맞추는 방향으로 진행한다.
  - 2026-04-09 검증용 `PostgreSQL` 컨테이너 `alm-postgres-test` 를 `55432:5432` 로 기동했고, `pg_isready` 로 준비 상태를 확인했다.
  - 2026-04-09 Rust 백엔드는 `ALM_BACKEND_DATABASE_URL=postgres://alm:secret@127.0.0.1:55432/alm`, `ALM_BACKEND_BIND_ADDRESS=0.0.0.0:28080` 조건으로 실행해 `/api/v1/health` 응답을 확인했다.
  - 2026-04-09 정적 UI 서버는 저장소 루트에서 `python3 -m http.server 8000` 으로 기동했고, 조직 관리 프로토타입 HTML 이 `HTTP 200` 으로 열리는 것을 확인했다.
  - 2026-04-09 추가 확인으로 조직 목록 API 를 호출해 기본 시드 조직 `default_org` 조회 응답까지 확인했다.
- 완료 기준:
  - 테스트용 데이터베이스와 백엔드가 충돌 없이 기동해야 한다.
  - 필요 시 정적 UI 도 함께 열어둘 수 있어야 한다.
  - 최소 헬스 체크 또는 접근 확인 결과가 기록되어야 한다.
- 작업 결과: 완료. 현재 테스트 런타임은 `PostgreSQL 55432`, Rust 백엔드 `28080`, 정적 UI `8000` 조합으로 올라와 있다. `GET http://127.0.0.1:28080/api/v1/health` 는 `{\"status\":\"ok\"}` 를 반환했고, `GET /api/v1/admin/master-data/organizations` 는 기본 시드 조직 `default_org` 를 정상 반환했다. 정적 UI 도 `http://127.0.0.1:8000/src/ui_prototype/data_organizations.html` 에서 바로 접근 가능하다.

## TASK-100 UI 기본 API 연결 주소 보정

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/data_settings.html`
  - `src/ui_prototype/admin.html`
  - `src/ui_prototype/organization.html`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 테스트 런타임은 `28080` 에 떠 있는데 UI 프로토타입 기본 연결 주소가 여전히 `8080` 을 가리키고 있어, 기본값과 저장된 레거시 값 보정 로직을 현재 테스트 환경 기준으로 수정한다.
- 진행 현황:
  - 2026-04-09 UI 프로토타입의 기본 `API Base URL` 과 설정 화면 기본값이 모두 `http://127.0.0.1:8080/api/v1` 인 것을 확인했다.
  - 2026-04-09 `app.js` 기본값을 `http://127.0.0.1:28080/api/v1` 로 바꾸고, 브라우저 `localStorage` 에 저장된 예전 `8080` 값은 자동으로 `28080` 으로 치환되도록 보정했다.
  - 2026-04-09 `data_settings.html`, `admin.html`, `organization.html` 입력 기본값과 표시 문구도 `28080` 기준으로 맞췄다.
  - 2026-04-09 `node --check src/ui_prototype/app.js` 와 정적 서버 응답 확인으로 수정 반영을 검증했다.
- 완료 기준:
  - 기본 `API Base URL` 이 현재 테스트 백엔드 주소와 일치해야 한다.
  - 저장된 예전 `8080` 값이 있어도 새로고침 후 연결이 복구되어야 한다.
  - 스크립트 문법 검증이 통과해야 한다.
- 작업 결과: 완료. UI 프로토타입은 이제 기본적으로 `http://127.0.0.1:28080/api/v1` 를 사용하고, 기존에 저장돼 있던 `8080` 레거시 값도 자동으로 `28080` 으로 치환한다. 따라서 이 환경에서는 페이지 새로고침만 해도 백엔드 연결이 복구된다.

## TASK-101 테스트용 대량 더미 데이터 적재

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 현재 테스트 환경의 조직/인력 데이터가 기본 시드 수준이라 화면 검증이 어려우므로, 기존 더미 구조와 충돌하지 않는 대량 조직/인력 샘플을 API 기준으로 적재한다.
- 진행 현황:
  - 2026-04-09 현재 조직은 `default_org` 1건, 인력은 0건만 존재하는 것을 확인했다.
  - 2026-04-09 기존 `createOrganizationDummyData()` 가 사용하는 조직 계층과 멤버 등록 계약을 확인했고, 이 구조를 확장하는 방식으로 더미 데이터를 넣기로 했다.
  - 2026-04-09 `demo_bu_*`, `demo_team_*`, `demo_group_*`, `demo_part_*` 규칙으로 4개 사업부, 팀/그룹/파트 다단계 조직과 파트별 구성원을 API로 순차 생성했다.
  - 2026-04-09 적재 직후 활성 조직 117건, 활성 인력 216건으로 증가한 것을 확인했다.
  - 2026-04-09 생성 중 잘못 들어간 빈 루트 조직 1건은 테스트 DB에서 `deleted` 상태로 정리했고, 최종 활성 조직 수를 116건으로 맞췄다.
  - 2026-04-09 대표 검증으로 `demo_bu_platform` 구조 스냅샷에서 직속 하위 3개 팀, 하위 포함 인력 54명을 확인했고, `demo_part_platform_1_1_1` 에 직속 구성원 3명이 정상 조회되는 것도 확인했다.
- 완료 기준:
  - 조직/인력 더미가 충분한 규모로 적재되어 조직 관리와 인력 관리 화면에서 탐색 가능한 상태가 되어야 한다.
  - API 기준 조직/인력 건수 증가가 확인되어야 한다.
  - 결과가 오늘자 백로그에 기록되어야 한다.
- 작업 결과: 완료. 테스트 환경에 대량 조직/인력 더미를 적재해 현재 활성 조직 116건, 활성 인력 216건 상태가 됐다. 구조는 `사업부 -> 팀 -> 그룹 -> 파트` 계층으로 확장돼 조직 트리와 인력 관리 화면에서 탐색/필터링/이동 흐름을 충분히 검증할 수 있다. 대표적으로 `demo_bu_platform` 하위에는 3개 팀과 54명의 하위 포함 인력이 연결돼 있고, 각 파트에는 직속 구성원 3명씩 배치돼 있다.

## TASK-102 조직/인력 테스트 런타임 재기동

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 조직/인력 검증만 이어갈 수 있도록 기존 검증용 DB와 적재 데이터를 유지한 채, 내려간 Rust 백엔드와 정적 UI 서버를 다시 기동한다.
- 진행 현황:
  - 2026-04-09 포트 상태를 확인한 결과 `55432` 의 검증용 `PostgreSQL` 은 살아 있고, `28080` 백엔드와 `8000` 정적 UI 서버만 내려가 있음을 확인했다.
  - 2026-04-09 `alm-postgres-test` 컨테이너가 유지 중이어서 기존 더미 데이터는 그대로 사용 가능함을 확인했다.
  - 2026-04-09 Rust 백엔드를 `postgres://alm:secret@127.0.0.1:55432/alm`, `0.0.0.0:28080` 조건으로 다시 기동했고 `GET /api/v1/health` 응답을 확인했다.
  - 2026-04-09 정적 UI 서버를 `8000` 포트에서 다시 기동했고 조직 관리 HTML 이 정상 응답하는 것도 확인했다.
  - 2026-04-09 기존 적재 데이터가 유지된 상태에서 활성 조직 116건, 활성 인력 216건이 그대로 조회되는 것을 재확인했다.
- 완료 기준:
  - 백엔드와 정적 UI 서버가 다시 기동해야 한다.
  - 조직/인력 API 및 화면 접근이 복구되어야 한다.
  - 결과가 오늘자 백로그에 기록되어야 한다.
- 작업 결과: 완료. 조직/인력 테스트 런타임은 다시 올라왔고, 검증용 DB와 더미 데이터도 그대로 유지됐다. 현재 `http://127.0.0.1:28080/api/v1/health` 는 정상 응답하고, 조직 관리 화면은 `http://127.0.0.1:8000/src/ui_prototype/data_organizations.html` 에서 다시 열 수 있다. 데이터 기준선도 활성 조직 116건, 활성 인력 216건으로 유지 중이다.

## TASK-103 업무혁신사업부 트리 좌측 잘림 재현 확인

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-09.md`
  - `output/playwright/ui_prototype/debug/data_organizations_business_fhd_firefox.png`
  - `output/playwright/ui_prototype/debug/data_organizations_business_tree_panel_fhd_firefox.png`
- 작업 내용: `업무혁신사업부` 선택 시 조직 트리의 왼쪽 항목이 잘려 보인다는 제보를 `1920x1080` 급 해상도 기준 실제 브라우저 렌더로 재현하고, 패널과 카드 좌표를 확인한다.
- 진행 현황:
  - 2026-04-09 Playwright용 브라우저가 없어 사용자 영역에 `chromium`, `firefox` 런타임을 설치했다.
  - 2026-04-09 `http://127.0.0.1:8000/src/ui_prototype/data_organizations.html?apiBase=http://127.0.0.1:28080/api/v1` 를 `1920x1080` 뷰포트에서 열고 `업무혁신사업부` 탭을 선택했다.
  - 2026-04-09 트리 패널 좌표와 카드 좌표를 확인한 결과 패널 시작점은 `left=398px`, 가장 왼쪽 파트 카드는 `left=107px` 까지 밀려 있어 약 `291px` 이 패널 밖으로 잘리는 상태임을 확인했다.
  - 2026-04-09 전체 화면과 트리 패널 캡처를 각각 `output/playwright/ui_prototype/debug/data_organizations_business_fhd_firefox.png`, `output/playwright/ui_prototype/debug/data_organizations_business_tree_panel_fhd_firefox.png` 로 남겼다.
- 완료 기준:
  - `1920x1080` 기준 실제 렌더에서 문제 재현 여부가 확인되어야 한다.
  - 패널과 카드 위치 기준으로 잘림 여부가 설명 가능해야 한다.
  - 캡처 산출물이 남아야 한다.
- 작업 결과: 완료. `업무혁신사업부` 트리는 `1920x1080` 기준에서 실제로 좌측 항목이 잘려 보인다. 트리 콘텐츠 폭은 패널 폭보다 훨씬 넓은데 초기 `scrollLeft=0` 상태에서 트리 자체가 가운데 정렬과 추가 이동 보정이 섞인 채 렌더되어, 가장 왼쪽 카드 일부가 패널 바깥으로 남는다. 따라서 사용자 제보는 재현되며, 원인은 트리 초기 배치와 좌우 균형 보정 로직이 특정 사업부 폭에서 과도하게 오른쪽으로 치우치는 쪽에 가깝다.

## TASK-104 업무혁신사업부 트리 좌측 잘림 수정

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-09.md`
  - `output/playwright/ui_prototype/debug/data_organizations_business_tree_panel_fhd_fixed_firefox.png`
  - `output/playwright/ui_prototype/debug/data_organizations_business_fhd_fixed_firefox.png`
- 작업 내용: 넘치는 피라미드 트리에서도 좌측 첫 카드가 패널 안에서 보이도록 초기 배치 규칙을 수정하고, `1920x1080` 기준으로 재검증한다.
- 진행 현황:
  - 2026-04-09 브라우저 실험으로 `.org-pyramid-tree` 에서 `margin: 0`, `justify-content: flex-start`, `transform: none` 만으로는 충분하지 않고, 넘치는 트리에도 유지되던 `min-width: max(100%, 964px)` 가 실제 시작점을 밀어낸다는 점을 확인했다.
  - 2026-04-09 `styles.css` 기본값을 overflow-safe 기준으로 바꾸고, `softenOrganizationPyramidMargins()` 는 넘칠 때 `min-width = 0`, 좌측 정렬, `scrollLeft = 0` 을 강제하고, 패널 안에 다 들어오는 경우에만 이전처럼 최소 폭과 가운데 정렬을 복원하도록 수정했다.
  - 2026-04-09 `node --check src/ui_prototype/app.js` 검증 후 같은 Playwright Firefox 세션에서 `업무혁신사업부` 탭을 다시 열어 좌표를 재확인했다.
  - 2026-04-09 수정 후 패널 시작점은 `left=398px`, 가장 왼쪽 파트 카드는 `left=457px` 로 들어와 좌측 잘림이 해소된 것을 확인했고, 패널/전체 화면 캡처를 남겼다.
- 완료 기준:
  - `1920x1080` 기준에서 가장 왼쪽 카드가 패널 안에 보여야 한다.
  - 넘치는 트리는 패널 내부 가로 스크롤로 탐색 가능해야 한다.
  - 스크립트 검증과 재현 캡처가 남아야 한다.
- 작업 결과: 완료. `업무혁신사업부` 트리는 이제 `1920x1080` 기준에서도 좌측 첫 카드가 패널 내부에서 바로 보인다. 기존처럼 넘치는 트리를 시각적으로 가운데 정렬해 스크롤 영역 바깥으로 밀어내지 않고, overflow 상황에서는 좌측 기준으로 시작해 패널 내부 가로 스크롤로 탐색하도록 정리했다. 재검증 좌표 기준 가장 왼쪽 파트 카드는 `left=457px` 로 패널 `left=398px` 안에 들어왔고, 캡처는 `output/playwright/ui_prototype/debug/data_organizations_business_tree_panel_fhd_fixed_firefox.png`, `output/playwright/ui_prototype/debug/data_organizations_business_fhd_fixed_firefox.png` 에 남겼다.

## TASK-105 조직 트리 선택 시 스크롤 위치 보존

- 상태: done
- 우선순위: high
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 트리에서 항목을 선택할 때마다 패널 스크롤이 초기화되어 탐색 사용성이 떨어지는 문제를 줄이기 위해, 같은 사업부 트리를 다시 그리는 경우에는 기존 스크롤 위치를 유지하도록 수정한다.
- 진행 현황:
  - 2026-04-09 현재 구현은 `softenOrganizationPyramidMargins()` 에서 렌더마다 `scrollLeft = 0` 을 강제해, 단순 선택 변경에도 스크롤이 처음으로 돌아감을 확인했다.
  - 2026-04-09 `organizationTreeViewportState` 를 추가해 현재 렌더 중인 사업부 코드와 `scrollLeft/scrollTop` 을 저장하고, 같은 사업부를 다시 렌더할 때는 해당 값을 복원하도록 수정했다.
  - 2026-04-09 사업부가 실제로 바뀌는 경우에는 새 트리로 보고 스크롤을 초기화하도록 유지했다.
  - 2026-04-09 Playwright 검증에서 `업무혁신사업부` 트리를 `scrollLeft = 520` 으로 이동한 뒤 `업무혁신2-2그룹` 카드를 선택해도 `scrollLeft = 520` 이 유지되는 것을 확인했다.
- 완료 기준:
  - 같은 사업부 트리 안에서 선택만 바뀌면 스크롤 위치가 유지되어야 한다.
  - 사업부 전환처럼 새로운 트리로 바뀌는 경우에는 초기 위치로 시작해도 된다.
  - 스크립트 문법 검증과 실제 브라우저 검증이 완료되어야 한다.
- 작업 결과: 완료. 조직 트리는 이제 같은 사업부 안에서 카드를 선택하거나 선택 상태가 다시 렌더되어도 현재 스크롤 위치를 유지한다. 따라서 오른쪽 깊은 노드를 보고 있던 중 선택해도 패널이 처음 위치로 튀지 않는다. 반면 사업부를 바꾸는 경우에는 새 트리 기준으로 초기 위치에서 시작하도록 유지했다.

## TASK-106 조직 트리맵 영역 높이 확장

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
-  `output/playwright/ui_prototype/debug/data_organizations_business_tree_panel_taller_fhd_firefox.png`
- 작업 내용: `1920x1080` 기준에서 조직 트리맵 영역이 더 화면을 채우도록 높이 제한을 조정한다.
- 진행 현황:
  - 2026-04-09 현재 `.org-tree-panel` 높이 제한이 `min-height: 420px`, `max-height: 620px` 로 되어 있어 `1080`급 화면에서는 트리맵이 비교적 낮게 보이는 상태임을 확인했다.
  - 2026-04-09 `.org-tree-panel` 높이 규칙을 뷰포트 기반 `min-height: clamp(520px, 60vh, 720px)`, `max-height: clamp(620px, 68vh, 780px)` 로 조정했다.
  - 2026-04-09 `1920x1080` Playwright Firefox 검증에서 실제 계산 높이가 약 `734px` 로 적용되는 것을 확인했고, 트리 패널 캡처를 남겼다.
- 완료 기준:
  - `1080`급 해상도에서 트리맵 영역이 기존보다 더 넓게 보이되 전체 레이아웃이 무너지지 않아야 한다.
  - 변경 후 같은 해상도 기준으로 재확인해야 한다.
- 작업 결과: 완료. 조직 트리맵 영역은 이제 `1080`급 화면에서 기존보다 더 크게 보이며, 검증 기준 `1920x1080` 에서 패널 높이가 약 `734px` 까지 확장된다. 덕분에 트리 구조를 같은 화면에서 더 많이 볼 수 있고, 스크롤 전후 맥락도 덜 끊긴다. 확인 캡처는 `output/playwright/ui_prototype/debug/data_organizations_business_tree_panel_taller_fhd_firefox.png` 에 남겼다.

## TASK-107 사업부 선택 버튼 1줄 요약형 정리

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
-  `output/playwright/ui_prototype/debug/organization_division_tabs_one_line_firefox.png`
- 작업 내용: 트리맵 상단의 사업부 선택 버튼을 1줄 요약형으로 정리하고, 조직 코드는 제거한 채 `하위 조직 수`, `구성원 수` 정도만 표시한다.
- 진행 현황:
  - 2026-04-09 현재 버튼은 `사업부명` 과 `조직코드 · 하위 조직 수 · 구성원 수` 의 2줄 구조로 렌더되고 있음을 확인했다.
  - 2026-04-09 버튼 마크업을 `사업부명 + 하위 조직 수/구성원 수` 구조로 바꾸고, 조직 코드는 제거했다.
  - 2026-04-09 스타일도 `inline-flex` 기반 1줄 요약형으로 조정해 버튼 안에서 줄바꿈 없이 읽히도록 정리했다.
  - 2026-04-09 새 브라우저 세션 기준 캡처에서 `업무혁신사업부 하위 27개 조직 · 54명` 형태로 한 줄 표시되는 것을 확인했다.
- 완료 기준:
  - 버튼 정보가 한 줄로 보이도록 정리되어야 한다.
  - 조직 코드는 제거되어야 한다.
  - 실제 화면에서 줄바꿈 없이 읽히는지 확인되어야 한다.
- 작업 결과: 완료. 트리맵 상단 사업부 선택 버튼은 이제 조직 코드를 숨기고, `사업부명 + 하위 조직 수 + 구성원 수` 만 보여주는 한 줄 요약형으로 정리됐다. 버튼 간 비교도 더 쉬워졌고, 상단 영역 밀도도 좋아졌다. 확인 캡처는 `output/playwright/ui_prototype/debug/organization_division_tabs_one_line_firefox.png` 에 남겼다.

## TASK-108 사업부 탭 우측 빠른 추가 버튼 연결

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-09
- 완료일: 2026-04-09
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-09.md`
- 작업 내용: 트리맵 상단 사업부 탭 영역 맨 우측에 `+` 버튼을 두고, 그 자리에서 새 사업부를 바로 추가할 수 있도록 연결한다.
- 진행 현황:
  - 2026-04-09 사업부 탭 렌더에 우측 `+` 버튼과 상단 전용 인라인 초안 카드 상태를 추가했다.
  - 2026-04-09 `사업부명`, `키` 를 입력하면 `biz_` 접두 기반 코드 미리보기가 보이도록 했고, `생성` 클릭 시 `POST /admin/master-data/organizations` 로 최상위 사업부를 바로 만들도록 연결했다.
  - 2026-04-09 스타일에는 우측 고정형 `+` 버튼과 소형 초안 카드 레이아웃을 추가했다.
  - 2026-04-09 실제 브라우저 검증에서 상단 `+` 노출, 초안 카드 열기, `AI전략사업부 / ai_strategy` 입력 후 사업부 생성까지 확인했다.
- 완료 기준:
  - 사업부 탭 우측에 `+` 버튼이 보여야 한다.
  - 버튼 클릭 시 상단에서 바로 사업부 초안을 작성할 수 있어야 한다.
  - 생성 후 새 사업부가 탭과 선택 상태에 즉시 반영되어야 한다.
- 작업 결과: 완료. 트리맵 상단 사업부 영역 우측에 `+` 버튼이 추가됐고, 클릭하면 같은 영역 안에서 새 사업부명과 키를 바로 입력하는 초안 카드가 열린다. 생성 시 최상위 사업부가 즉시 등록되고 탭 목록과 현재 선택 상태에도 바로 반영된다. 검증용으로 `AI전략사업부 (biz_ai_strategy)` 를 실제 생성해 동작을 확인했다.
