# 2026-04-15 작업 백로그

- 문서 목적: 2026-04-15에 수행한 작업의 계획, 진행 현황, 완료 내역을 기록한다.
- 범위: 2026-04-15 작업 이력
- 대상 독자: 프로젝트 참여자, 문서 작성자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-15
- 관련 문서: `docs/operations/work_backlog.md`

## TASK-098 개발 환경 점검 및 컨테이너 기반 테스트 경로 정비

- 상태: done
- 우선순위: high
- 요청일: 2026-04-15
- 완료일: 2026-04-15
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `Makefile`
  - `docker-compose.yml`
  - `.env.docker.example`
  - `.dockerignore`
  - `docker/python-test.Dockerfile`
  - `docker/rust-test.Dockerfile`
  - `README.md`
  - `docs/README.md`
  - `docs/operations/README.md`
  - `docs/operations/development_environment.md`
  - `docs/operations/work_backlog.md`
  - `docs/operations/backlog/2026-04-15.md`
- 작업 내용: 현재 세션의 개발 도구 설치 상태를 점검하고, 저장소를 처음 사용하는 개발자가 로컬 또는 컨테이너 기반으로 Python/Rust 테스트 환경을 재현할 수 있도록 진입점과 문서를 정비한다.
- 진행 현황:
  - 2026-04-15 세션 시작 시 `docs/operations/work_backlog.md` 와 최근 백로그 `2026-04-09.md` 를 확인했고, 미완료 작업은 없음을 확인했다.
  - 2026-04-15 저장소 구조와 현재 개발 명령을 확인한 결과 Python 임시 골격, Rust 백엔드, `docker-compose.yml`, `.env.example` 이 이미 존재했지만 테스트 러너를 격리 실행하는 경로는 부족했다.
  - 2026-04-15 현재 실행 환경을 확인한 결과 `python3 3.14.3`, `podman 5.8.1`, `pip` 는 존재하지만 `docker`, `cargo`, `pytest`, `podman compose` 는 바로 사용할 수 없었다.
  - 2026-04-15 이에 따라 `docker compose` 전용 경로만 의존하지 않도록 `Makefile` 에 `CONTAINER_RUNTIME` 기반 `container-test-python`, `container-test-rust`, `container-test`, `container-backend-run` 대상을 추가했다.
  - 2026-04-15 컨테이너 전용 환경 파일 `.env.docker.example`, 테스트 이미지용 `Dockerfile` 2종, `.dockerignore` 를 추가하고 `docker-compose.yml` 도 Python 테스트, Rust 테스트, Rust 개발 실행 서비스를 포함하도록 확장했다.
  - 2026-04-15 관련 문서 영향 범위를 검토한 결과 개발 환경 진입점이 README 와 운영 위키에 직접 영향을 주므로 `README.md`, `docs/README.md`, `docs/operations/README.md` 와 신규 가이드 문서를 함께 갱신하기로 결정했다.
- 완료 기준:
  - 현재 환경의 도구 설치 상태가 정리되어야 한다.
  - 컨테이너 기반 테스트 진입점이 저장소에 추가되어야 한다.
  - 개발자용 가이드 문서가 최신 경로를 설명해야 한다.
  - 실제 검증이 불가능한 항목은 미검증 사유가 기록되어야 한다.
- 작업 결과: 완료. 현재 세션은 `docker` 와 `cargo` 가 없는 상태라 실제 컨테이너 기동과 Rust 테스트까지 끝까지 검증하지는 못했지만, 저장소 기준으로는 로컬과 컨테이너 양쪽에서 재현 가능한 개발/테스트 진입점을 정리했다. `Makefile` 은 이제 `docker` 또는 `podman` 런타임을 바꿔 사용할 수 있고, Python/Rust 테스트를 각 전용 이미지에서 실행할 수 있다. `docker-compose.yml` 은 `python-test`, `backend-test`, `backend-dev` 서비스를 포함하도록 확장했으며, 운영 문서에는 현재 세션에서 확인한 제약과 권장 실행 절차를 명시했다. 후속 검증은 `docker` 또는 `podman` 런타임과 `cargo`, `pytest` 가 준비된 환경에서 `make container-test` 와 `make backend-test` 를 실제 실행하는 단계가 필요하다.

## TASK-099 호스트별 환경 기록 체계 도입

- 상태: done
- 우선순위: high
- 요청일: 2026-04-15
- 완료일: 2026-04-15
- 담당: Codex
- 호스트명: `bazzite`
- 호스트 IP: `192.168.0.122`
- 영향 문서:
  - `AGENTS.md`
  - `docs/README.md`
  - `docs/operations/README.md`
  - `docs/operations/work_backlog.md`
  - `docs/operations/development_environment.md`
  - `docs/operations/environments/README.md`
  - `docs/operations/environments/env_old/README.md`
  - `docs/operations/environments/bazzite-192.168.0.122/README.md`
  - `docs/architecture/current_backend_implementation_status_summary.md`
  - `docs/operations/backlog/2026-04-15.md`
- 작업 내용: 여러 호스트 세션에서 수행하는 작업을 구분할 수 있도록 백로그에 호스트명과 IP를 의무 기록하고, 환경 의존적인 설치/검증 기록은 호스트별 폴더 구조로 분리한다. 기존 출처 불명 환경 기록은 `env_old` 로 모은다.
- 진행 현황:
  - 2026-04-15 사용자 요청에 따라 현재 호스트 식별 정보 `bazzite / 192.168.0.122` 와 추가 IPv6 주소를 확인했다.
  - 2026-04-15 `AGENTS.md` 와 운영 백로그 인덱스 규칙을 검토한 결과 호스트 식별 항목과 환경 기록 분리 규칙이 없음을 확인했다.
  - 2026-04-15 `docs/operations/environments/` 아래에 인덱스, `env_old`, 현재 호스트 폴더를 추가하고, 일반 개발 환경 가이드에는 공통 절차만 남기도록 구조를 조정했다.
  - 2026-04-15 기존 문서 중 호스트가 명확하지 않은 환경 참조는 `env_old` 에 집계하고, 아키텍처 요약 문서의 개발 환경 상태도 호스트별 기록을 보도록 표현을 조정했다.
  - 2026-04-15 오늘 백로그 작업 `TASK-098`, `TASK-099` 에 호스트명과 IP를 기록해 새로운 기준을 바로 적용했다.
- 완료 기준:
  - 백로그 기록 기준에 호스트명과 호스트 IP가 추가되어야 한다.
  - 환경 의존 기록이 `hostname-ip` 폴더 구조로 정리되어야 한다.
  - 기존 출처 불명 환경 기록의 보관 위치가 정의되어야 한다.
- 작업 결과: 완료. 이제 작업 백로그에는 `호스트명`, `호스트 IP` 를 함께 기록하고, 실제 설치 상태와 검증 결과는 `docs/operations/environments/<hostname>-<ip>/` 아래에 분리해 남긴다. 현재 세션 기준 기록은 `docs/operations/environments/bazzite-192.168.0.122/README.md` 에 저장했고, 기존 출처 불명 환경 참조는 `docs/operations/environments/env_old/README.md` 에 집계했다. 일반 가이드 문서는 호스트 독립적인 절차만 유지하도록 정리했다.
