# 2026-04-08 작업 백로그

- 문서 목적: 2026-04-08에 수행한 작업의 계획, 진행 현황, 완료 내역을 기록한다.
- 범위: 2026-04-08 작업 이력
- 대상 독자: 프로젝트 참여자, 문서 작성자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-08
- 관련 문서: `docs/operations/work_backlog.md`

## TASK-136 통합 데이터 관리 UI 하위 관리 페이지 분리

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_admin.html`
  - `src/ui_prototype/data_alm.html`
  - `src/ui_prototype/data_external_jira.html`
  - `src/ui_prototype/data_external_bitbucket.html`
  - `src/ui_prototype/data_external_bamboo.html`
  - `src/ui_prototype/data_external_confluence.html`
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/app.js`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 통합 데이터 관리 센터를 허브로 유지하되, 자체 `ALM`, 외부 시스템별 데이터, 조직 관리, 인력 관리를 각각 별도 관리 페이지로 분리하고 실제 운영 API 조회까지 연결한다.
- 진행 현황:
  - 2026-04-08 사용자가 시스템 관리자 전용 통합 데이터 관리 UI 를 새 방향으로 다시 만들고 프로토타입까지 보고 싶다고 요청했다.
  - 2026-04-08 센터 홈 `data_admin.html` 은 유지하되, 실제 관리 화면을 `data_alm`, 외부 시스템별 4개 화면, `data_organizations`, `data_workforce` 로 분리하는 방향으로 정리했다.
  - 2026-04-08 각 하위 페이지 HTML 골격을 추가하고, 공용 `app.js` 에 화면별 전용 로더를 붙여 관리자 API를 읽어 화면별 데이터에 맞게 렌더링하도록 연결했다.
  - 2026-04-08 센터 홈에도 각 하위 관리 화면으로 이동하는 링크를 추가해 허브 역할이 드러나도록 보강했다.
  - 2026-04-08 UI/UX 문서에 하위 관리 화면과 열람 경로를 반영하고, `node --check`, `git diff --check` 로 정적 검증을 진행한다.
- 완료 기준:
  - 데이터 관리 UI 가 센터 홈과 하위 관리 페이지 구조로 분리되어야 한다.
  - 하위 페이지들이 실제 관리자 API를 읽어 최소 조회 결과를 렌더링해야 한다.
  - 문서에 포함 화면과 열람 경로가 반영되어야 한다.
- 작업 결과: 완료. 통합 데이터 관리 센터를 허브로 유지하면서 자체 `ALM`, 외부 시스템별 4개 화면, 조직 관리, 인력 관리 화면을 별도 페이지로 분리했다. 공용 `app.js` 에 화면별 전용 로더를 추가해 각 페이지가 `admin/projects`, `admin/work-items`, `admin/sync-runs`, `admin/master-data/*` API 를 실제로 조회하도록 연결했고, 센터 홈에서 각 하위 화면으로 이동하는 링크도 보강했다. `node --check src/ui_prototype/app.js`, `git diff --check` 를 통과했고, 로컬 정적 서버 기준 모든 신규 페이지가 `HTTP 200` 으로 응답하는 것을 확인했다. 브라우저 자동 캡처는 이 환경에 `Chrome` 바이너리가 없어 미실행 상태다.

## TASK-137 통합 데이터 관리 UI 운영 판단 패널 보강

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_alm.html`
  - `src/ui_prototype/data_external_jira.html`
  - `src/ui_prototype/data_external_bitbucket.html`
  - `src/ui_prototype/data_external_bamboo.html`
  - `src/ui_prototype/data_external_confluence.html`
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/app.js`
  - `docs/overview/integrated_data_management_ui_direction.md`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 분리된 하위 관리 페이지들이 단순 조회를 넘어서 운영 판단이 가능하도록 `대표 상세`, `운영 액션`, `영향 범위` 패널을 추가하고 실제 API 데이터 기반 요약을 연결한다.
- 진행 현황:
  - 2026-04-08 하위 관리 페이지 분리까지는 완료됐지만, 현재 화면은 목록 중심이라 운영자가 다음 행동을 판단하기에는 부족한 상태였다.
  - 2026-04-08 자체 `ALM`, 외부 시스템별 페이지, 조직, 인력 화면에 공통 운영 판단 패널을 추가하는 방향으로 구조를 보강했다.
  - 2026-04-08 `app.js` 에 화면별 대표 레코드 요약, 운영 액션, 영향 범위 계산 헬퍼를 추가하고 각 로더가 해당 패널을 실제 데이터로 채우도록 연결했다.
  - 2026-04-08 방향 문서와 프로토타입 문서에도 `목록 -> 대표 상세 -> 운영 액션 -> 영향 범위` 흐름을 현재 기준선으로 반영했다.
- 완료 기준:
  - 하위 관리 화면에 운영 판단 패널이 추가되어야 한다.
  - 각 패널이 실제 관리자 API 데이터 기반으로 최소 요약을 렌더링해야 한다.
  - 관련 문서에 화면 구조 보강 내용이 반영되어야 한다.
- 작업 결과: 완료. 자체 `ALM`, 외부 시스템별 화면, 조직 관리, 인력 관리 하위 페이지에 `대표 상세`, `운영 액션`, `영향 범위` 또는 이에 준하는 운영 판단 패널을 추가했다. 공용 `app.js` 는 이제 각 페이지에서 실제 관리자 API 응답을 바탕으로 대표 레코드 요약, 후속 점검 체크리스트, 연결 영향 설명을 함께 렌더링한다. 방향 문서에는 운영 판단 패널 원칙을, 프로토타입 문서에는 `목록 -> 대표 상세 -> 운영 액션 -> 영향 범위` 흐름을 반영했다. `node --check src/ui_prototype/app.js`, `git diff --check` 를 통과했고, 로컬 정적 서버 기준 모든 대상 페이지가 `HTTP 200` 으로 응답함을 다시 확인했다.

## TASK-138 조직 관리 CRUD 및 조직 구성원 조작 API/프로토타입 연결

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/src/services/master_data.rs`
  - `backend/src/http/routes/master_data.rs`
  - `backend/tests/http.rs`
  - `backend/tests/master_data_service.rs`
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/app.js`
  - `docs/overview/integrated_data_management_ui_direction.md`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 등록/수정/삭제, 다단계 조직 계층, 조직 구성원 등록/이동/비활성화를 지원하는 최소 백엔드 계약을 추가하고, 조직/인력 관리 프로토타입 화면에 실제 액션 폼을 연결한다.
- 진행 현황:
  - 2026-04-08 기존 구현은 조직/인력 `upsert + list`까지만 가능해 실제 운영 관리 기능으로는 부족했다.
  - 2026-04-08 HTTP 테스트와 저장소 테스트를 먼저 추가해 조직 수정/삭제, 계층 순환 방지, 조직 구성원 중첩 경로 계약을 실패 테스트로 고정했다.
  - 2026-04-08 `MasterDataStore`, `MasterDataRepository` 에 조직 수정, 소프트 삭제, 계층 순환 검증, 구성원 수정, 구성원 비활성화를 구현했다.
  - 2026-04-08 `master_data` 라우트에 `PATCH/DELETE /organizations/{code}` 와 `GET/POST/PATCH/DELETE /organizations/{code}/members` 를 추가했다.
  - 2026-04-08 조직 관리, 인력 관리 프로토타입에 실제 API 호출 폼을 붙여 조직 등록/수정/삭제와 조직 구성원 등록/이동/제거를 화면에서 실행할 수 있게 했다.
  - 2026-04-08 `cargo test --manifest-path backend/Cargo.toml`, `node --check src/ui_prototype/app.js`, `git diff --check` 전체 통과를 확인했다.
- 완료 기준:
  - 조직 등록/수정/삭제가 가능해야 한다.
  - 조직 계층이 여러 층으로 구성될 수 있고 순환 참조를 막아야 한다.
  - 조직별 구성원 등록/이동/비활성화가 가능해야 한다.
  - 프로토타입 화면에서 위 액션을 실제 API와 연결할 수 있어야 한다.
- 작업 결과: 완료. 조직 관리 백엔드는 이제 등록/수정/삭제와 다단계 상하위 조직 구조를 지원하고, 자기참조 및 계층 순환을 `BAD_REQUEST` 로 차단한다. 삭제는 하위 조직이나 활성 구성원이 있을 때 막고, 가능할 때는 `deleted` 상태의 소프트 삭제로 처리한다. 조직 구성원은 중첩 멤버 라우트로 등록, 조직 이동, 비활성화가 가능하며, 조직/인력 프로토타입 화면에도 해당 액션 폼을 연결했다. 전체 Rust 테스트와 프론트 스크립트 검증도 모두 통과했다.

## TASK-139 조직 트리 시각화 및 계층형 더미 데이터 생성 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/overview/integrated_data_management_ui_direction.md`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 관리 화면에 다단계 조직 트리를 시각화하고, `사업부 -> 팀 -> 그룹 -> 파트` 순서의 계층형 더미 데이터와 샘플 구성원을 한 번에 생성할 수 있는 프로토타입 기능을 추가한다.
- 진행 현황:
  - 2026-04-08 사용자가 조직 트리와 계층형 더미 데이터를 요청했다.
  - 2026-04-08 조직 화면에 클릭 가능한 트리 패널과 선택 조직 요약 패널을 추가하고, 선택 조직이 인력 관리 화면의 조직 필터와 연결되도록 설계했다.
  - 2026-04-08 공용 `app.js` 에 조직 깊이 계산, 트리 렌더링, 선택 조직 동기화, 더미 데이터 생성 헬퍼를 추가했다.
  - 2026-04-08 더미 데이터는 `사업부 -> 팀 -> 그룹 -> 파트` 4단계 구조와 샘플 구성원 4명을 생성하도록 구성했고, 조직/인력 화면 양쪽에서 실행 가능하게 버튼을 배치했다.
  - 2026-04-08 `node --check src/ui_prototype/app.js`, `git diff --check`, 페이지 `HTTP 200` 응답을 다시 확인했다.
- 완료 기준:
  - 조직 관리 화면에 다단계 조직 트리가 시각화되어야 한다.
  - 더미 데이터 생성 버튼으로 계층형 조직과 샘플 구성원이 생성되어야 한다.
  - 선택 조직이 인력 관리 화면과 연동되어야 한다.
- 작업 결과: 완료. 조직 관리 화면에는 클릭 가능한 조직 트리와 선택 조직 영향 요약이 추가됐고, 조직을 선택하면 조직 관리 폼과 인력 관리 기본 대상 조직이 함께 동기화된다. 더미 데이터 생성 버튼은 `플랫폼사업부/업무혁신사업부` 아래에 팀, 그룹, 파트 조직과 샘플 구성원을 순서대로 upsert 하도록 연결했으며, 동일 버튼을 인력 관리 화면에도 배치해 어디서든 샘플 계층을 만들 수 있게 했다.

## TASK-140 조직 관리 화면에 직속 구성원 액션 흡수

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 관리 화면 안에서 선택 조직의 직속 구성원을 바로 등록, 이동, 비활성화할 수 있도록 인력 액션 일부를 흡수한다.
- 진행 현황:
  - 2026-04-08 사용자가 조직 관리 화면 안에서 직속 구성원 조작을 먼저 하자고 방향을 정했다.
  - 2026-04-08 조직 화면에 직속 구성원 액션 폼을 추가하고, 선택 조직 코드를 기본값으로 유지하도록 연결했다.
  - 2026-04-08 직속 구성원 표에서 행 클릭 시 구성원 정보가 액션 폼에 채워지게 했고, 같은 화면에서 등록/이동/비활성화 요청을 보낼 수 있게 했다.
  - 2026-04-08 `node --check`, `git diff --check`, 정적 서버 `HTTP 200` 응답을 확인했다.
- 완료 기준:
  - 조직 관리 화면에서 직속 구성원 등록/이동/비활성화가 가능해야 한다.
  - 트리 선택 조직과 직속 구성원 액션 대상 조직이 동기화되어야 한다.
- 작업 결과: 완료. 조직 관리 화면은 이제 조직 트리 선택, 직속 구성원 목록 확인, 직속 구성원 등록/조직 이동/비활성화까지 같은 페이지에서 수행할 수 있다. 선택 조직 코드는 직속 구성원 액션 폼과 자동 동기화되며, 목록 행 클릭으로 사번/이름/직군/이메일 정보도 바로 폼에 채워진다.

## TASK-141 조직 변경 이력 및 구성원 이동 이력 조회 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/migrations/20260408152000_add_master_data_change_log.up.sql`
  - `backend/migrations/20260408152000_add_master_data_change_log.down.sql`
  - `backend/src/services/master_data.rs`
  - `backend/src/http/routes/master_data.rs`
  - `backend/tests/http.rs`
  - `backend/tests/bootstrap.rs`
  - `backend/tests/migrations.rs`
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 관리 화면에서 선택 조직 기준 변경 이력과 구성원 이동 이력을 바로 볼 수 있도록, 백엔드 이력 테이블과 조회 API를 추가하고 프로토타입 화면에 이력 패널을 연결한다.
- 진행 현황:
  - 2026-04-08 조직 관리 화면은 구조와 액션까지는 가능하지만 최근 변경 내용을 확인할 수 없어 운영자 판단 맥락이 부족했다.
  - 2026-04-08 조직 변경용 `organization_change_log`, 인력 이동용 `workforce_change_log` 테이블 마이그레이션을 추가하고, 부트스트랩/마이그레이션 테스트 기대값을 갱신했다.
  - 2026-04-08 `MasterDataStore`, `MasterDataRepository` 가 조직 저장/수정/삭제와 구성원 저장/이동/비활성화 시 이력을 남기고, `GET /admin/master-data/organizations/{code}/history`, `GET /admin/master-data/organizations/{code}/member-history` 조회를 지원하도록 구현했다.
  - 2026-04-08 조직 관리 프로토타입은 선택 조직 기준으로 두 이력 엔드포인트를 함께 호출해 최근 변경 6건씩 보여주도록 연결했다.
  - 2026-04-08 `cargo test --manifest-path backend/Cargo.toml master_data_history_endpoints_return_organization_and_member_logs`, `node --check src/ui_prototype/app.js` 로 우선 검증을 마쳤다.
- 완료 기준:
  - 조직/구성원 변경 이력이 별도 저장 구조로 남아야 한다.
  - 조직 관리 화면에서 선택 조직 기준 최근 변경 이력과 구성원 이동 이력을 볼 수 있어야 한다.
  - 관련 테스트와 스크립트 검증이 통과해야 한다.
- 작업 결과: 완료. 조직과 인력 마스터 변경은 이제 별도 change log 테이블에 기록되며, 조직 관리 화면은 선택한 조직에 대해 최근 조직 변경 이력과 구성원 이동 이력을 함께 표시한다. 운영자는 같은 화면에서 구조 확인, 액션 실행, 최근 변경 내역 확인까지 연속적으로 수행할 수 있다. 백엔드 이력 엔드포인트 테스트와 프론트 스크립트 구문 검증도 통과했다.

## TASK-142 통합 데이터 관리 센터 홈 폐기 및 직접 진입 구조 정리

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/index.html`
  - `src/ui_prototype/data_alm.html`
  - `src/ui_prototype/data_external_jira.html`
  - `src/ui_prototype/data_external_bitbucket.html`
  - `src/ui_prototype/data_external_bamboo.html`
  - `src/ui_prototype/data_external_confluence.html`
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/data_admin.html`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/overview/integrated_data_management_ui_direction.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 프로토타입에서 혼선을 주는 통합 데이터 관리 센터 홈을 폐기하고, 자체 `ALM`, 외부 시스템, 조직, 인력 화면으로 직접 진입하는 구조로 정리한다.
- 진행 현황:
  - 2026-04-08 사용자가 센터 홈 관련 UI 는 헷갈리므로 일단 폐기하자고 요청했다.
  - 2026-04-08 프로토타입 탐색과 하위 관리 화면 좌측 메뉴에서 `센터 홈` 링크를 제거하고, `data_admin.html` 페이지는 폐기 대상으로 삭제한다.
  - 2026-04-08 문서 기준도 `센터 홈` 대신 하위 관리 화면 직접 진입 구조로 갱신한다.
- 완료 기준:
  - 프로토타입 주요 탐색에서 `센터 홈` 링크가 제거되어야 한다.
  - `data_admin.html` 이 더 이상 현재 기준 프로토타입에 포함되지 않아야 한다.
  - 문서가 직접 진입 구조를 현재 기준으로 설명해야 한다.
- 작업 결과: 완료. 메인 프로토타입과 데이터 관리 하위 화면들에서 `센터 홈` 링크를 제거했고, `data_admin.html` 은 폐기 대상으로 삭제했다. 현재 시스템 관리자용 데이터 관리 프로토타입은 자체 `ALM`, 외부 시스템별 화면, 조직 관리, 인력 관리 화면으로 직접 진입하는 구조를 기준으로 한다. 관련 개요 문서와 오늘 백로그도 같은 기준으로 갱신했다.

## TASK-143 조직 구조 스냅샷 조회 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/src/services/master_data.rs`
  - `backend/src/http/routes/master_data.rs`
  - `backend/tests/http.rs`
  - `backend/tests/master_data_service.rs`
  - `src/ui_prototype/app.js`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 관리 화면에서 선택 조직의 상위 경로, 직속 하위 조직, 직속 구성원 수, 하위 포함 조직/인원 규모를 더 정확히 보여주기 위해 조직 구조 스냅샷 API와 화면 연결을 추가한다.
- 진행 현황:
  - 2026-04-08 조직 관리 화면은 트리와 요약이 있지만, 현재까지는 프론트 계산 중심이라 상위 경로와 하위 규모를 명시적으로 계약화하지 못한 상태였다.
  - 2026-04-08 `MasterDataStore`, `MasterDataRepository` 에 선택 조직 구조 스냅샷 계산을 추가하고, `GET /admin/master-data/organizations/{code}/structure` 엔드포인트를 연다.
  - 2026-04-08 조직 관리 프로토타입은 선택 조직 변경 시 구조 스냅샷을 조회해 상위 경로, 직속 하위 조직, 하위 포함 활성 구성원 규모를 요약 패널에 표시하도록 연결한다.
  - 2026-04-08 HTTP 테스트, 저장소 테스트, 프론트 스크립트 검증으로 최소 회귀를 확인한다.
- 완료 기준:
  - 조직 구조 스냅샷 API 가 상위 경로, 직속 하위 조직, 직속/하위 포함 인원 규모를 반환해야 한다.
  - 조직 관리 화면이 이 계약을 사용해 구조 요약을 표시해야 한다.
  - 관련 테스트와 스크립트 검증이 통과해야 한다.
- 작업 결과: 완료. 조직 관리 백엔드는 이제 선택 조직의 구조 스냅샷을 조회할 수 있고, 조직 화면은 이 응답을 사용해 상위 경로, 직속 하위 조직 목록, 하위 포함 조직 수와 활성 구성원 규모를 표시한다. `master_data_structure_endpoint_returns_ancestor_child_and_member_counts`, `master_data_repository_returns_organization_structure_snapshot` 테스트와 `node --check src/ui_prototype/app.js` 검증도 통과했다.

## TASK-122 `work_item_status_history` 최소 반영 구현

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/migrations/`
  - `backend/src/services/work_item_write.rs`
  - `backend/tests/domain_write_services.rs`
  - `backend/tests/pull_sync_orchestrator.rs`
  - `backend/tests/bootstrap.rs`
  - `backend/tests/migrations.rs`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: `work_item` 본체 반영과 함께 최소 `work_item_status_history` 를 적재해 현재 상태와 최신 이력이 일관되게 유지되도록 구현한다.
- 진행 현황:
  - 2026-04-08 09:31 KST 어제까지 `project`/`work_item` 본체 반영까지는 구현 완료 상태임을 확인했다.
  - 2026-04-08 09:31 KST 다음 후속으로 `work_item_status_history` 최소 반영이 가장 자연스럽다고 판단했고, 오늘자 백로그와 운영 인덱스를 새로 열었다.
  - 2026-04-08 상태 이력 테이블 마이그레이션, `WorkItemWriteService` 보강, 통합 테스트 추가 순으로 진행한다.
  - 2026-04-08 `work_item_status_history` 마이그레이션을 추가하고, `WorkItemWriteService` 가 payload 의 `status.common`, `status.detailed` 값을 `work_item` 본체와 상태 이력에 함께 반영하도록 구현했다.
  - 2026-04-08 상태가 신규이거나 변경된 경우에만 이력 레코드를 남기도록 처리해 최신 상태와 최신 이력의 일관성을 유지하게 했다.
  - 2026-04-08 `domain_write_services`, `pull_sync_orchestrator`, 전체 `cargo test --manifest-path backend/Cargo.toml` 를 실행해 신규 테스트와 기존 회귀 테스트가 모두 통과함을 확인했다.
  - 2026-04-08 구현 체크리스트 문서의 최근 진행 메모에도 상태 이력 최소 반영 현황을 반영했다.
- 완료 기준:
  - `work_item_status_history` 최소 저장 구조가 마이그레이션에 반영되어야 한다.
  - `work_item` 반영 시 최신 상태 이력이 함께 기록되어야 한다.
  - 관련 서비스 테스트와 전체 Rust 테스트가 통과해야 한다.
- 작업 결과: 완료. `work_item_status_history` 최소 테이블과 인덱스를 마이그레이션으로 추가했고, `WorkItemWriteService` 는 payload 상태를 읽어 `work_item.current_common_status`, `current_detailed_status_code` 를 갱신한 뒤 신규 또는 변경 시점에만 상태 이력 레코드를 적재하도록 보강했다. 통합 테스트에서 `OPS-101` 업무 항목의 현재 상태가 `in_progress/doing` 으로 저장되고 최신 이력도 같은 값을 가지는 것을 확인했으며, 전체 Rust 테스트도 모두 통과했다.

## TASK-123 `work_item_hierarchy` 최소 반영 구현

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/migrations/`
  - `backend/src/services/work_item_write.rs`
  - `backend/tests/domain_write_services.rs`
  - `backend/tests/pull_sync_orchestrator.rs`
  - `backend/tests/bootstrap.rs`
  - `backend/tests/migrations.rs`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: `work_item` 반영 시 payload 의 부모 키를 읽어 단일 부모 기준 `work_item_hierarchy` 를 upsert 하는 최소 반영 경로를 구현한다.
- 진행 현황:
  - 2026-04-08 현재 `work_item` 본체와 상태 이력 반영까지는 구현됐지만 부모-자식 구조를 저장하는 `work_item_hierarchy` 는 아직 비어 있다.
  - 2026-04-08 `work_item_hierarchy` 마이그레이션, `WorkItemWriteService` 계층 반영, 통합 테스트 추가 순으로 진행한다.
  - 2026-04-08 `work_item_hierarchy` 마이그레이션을 추가하고, payload 의 `parent_key` 가 있을 때 부모-자식 관계를 `child_work_item_id` 기준으로 upsert 하도록 `WorkItemWriteService` 를 보강했다.
  - 2026-04-08 같은 실행 안에서 부모/자식 처리 순서가 달라도 최종 계층이 생성되도록, 본체 반영 후 계층 링크를 후처리하는 2단계 방식으로 정리했다.
  - 2026-04-08 `domain_write_services`, `pull_sync_orchestrator`, 전체 `cargo test --manifest-path backend/Cargo.toml` 를 실행해 신규 테스트와 기존 회귀 테스트가 모두 통과함을 확인했다.
  - 2026-04-08 구현 체크리스트 문서의 최근 진행 메모에도 계층 반영 현황을 반영했다.
- 완료 기준:
  - `work_item_hierarchy` 최소 저장 구조가 마이그레이션에 반영되어야 한다.
  - payload 의 부모 키가 있으면 부모-자식 관계가 실제 테이블에 반영되어야 한다.
  - 관련 테스트와 전체 Rust 테스트가 통과해야 한다.
- 작업 결과: 완료. `work_item_hierarchy` 최소 테이블과 인덱스를 마이그레이션으로 추가했고, `WorkItemWriteService` 는 payload 의 `parent_key` 를 읽어 같은 프로젝트 안의 부모 `work_item` 을 찾아 `parent_child` 관계를 upsert 하도록 구현했다. 부모/자식이 같은 실행 안에 함께 있어도 처리 순서에 영향받지 않도록 본체 반영과 계층 반영을 분리했고, 통합 테스트에서 `OPS-100 -> OPS-101` 관계가 실제 `work_item_hierarchy` 에 저장되는 것을 확인했다. 전체 Rust 테스트도 모두 통과했다.

## TASK-124 구현/문서 중간점검 및 서비스 분류 정리

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `docs/architecture/service_classification_and_msa_assessment_draft.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 현재까지 작성된 문서와 구현 범위를 중간점검하고, 시스템이 제공하는 서비스를 분류한 뒤 `MSA` 채택 적합성을 평가한다.
- 진행 현황:
  - 2026-04-08 현재 문서와 구현은 `project`, `work_item`, 상태 이력, 계층, 표준화 파이프라인까지 상당 부분 진행된 상태여서 한 번 중간 정리가 필요한 시점이다.
  - 2026-04-08 아키텍처 위키에 서비스 분류와 `MSA` 판단 기준, 권장 전개 방향을 정리하는 새 초안을 작성한다.
  - 2026-04-08 현재 문서 체계, 구현 범위, 서비스 경계 후보를 종합해 `service_classification_and_msa_assessment_draft.md` 를 작성했다.
  - 2026-04-08 아키텍처 위키 인덱스에도 새 문서를 연결해 이후 구현/설계 중간점검 기준으로 재사용할 수 있게 했다.
- 완료 기준:
  - 현재 작성 문서와 구현 범위가 요약되어야 한다.
  - 제공 서비스 분류가 아키텍처 관점에서 정리되어야 한다.
  - `MSA` 채택 여부와 권장 아키텍처 방향이 명시되어야 한다.
- 작업 결과: 완료. 현재 저장소의 문서와 구현 범위를 중간점검해, 제품 관점의 서비스 분류를 `연계 수집`, `표준화/기준키 매핑`, `업무 도메인 반영`, `기준정보 관리`, `운영 통제`, `관리자/운영 API` 로 정리했다. 평가 결과 현 시점에는 강한 모듈 경계를 가진 `modular monolith` 가 가장 적절하며, `MSA` 는 수집 계층과 운영 통제 계층을 중심으로 중장기 선택적 분리를 검토하는 편이 타당하다고 판단했다.

## TASK-125 `work_item_plan_link` 최소 반영 구현

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/migrations/`
  - `backend/src/services/work_item_write.rs`
  - `backend/tests/domain_write_services.rs`
  - `backend/tests/pull_sync_orchestrator.rs`
  - `backend/tests/bootstrap.rs`
  - `backend/tests/migrations.rs`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: `work_item` 반영 시 최소 계획 단위와 `work_item_plan_link` 를 함께 반영하도록 구현한다. 이번 단계는 `iteration` 기준 최소선부터 적용한다.
- 진행 현황:
  - 2026-04-08 현재 `work_item` 본체, 상태 이력, 계층 반영까지는 구현됐지만 계획 단위 연결을 저장하는 `work_item_plan_link` 는 아직 없다.
  - 2026-04-08 `iteration` 과 `work_item_plan_link` 최소 마이그레이션, `WorkItemWriteService` 보강, 통합 테스트 추가 순으로 진행한다.
  - 2026-04-08 `iteration`, `work_item_plan_link` 최소 마이그레이션을 추가하고, payload 의 `iteration_name` 이 있을 때 계획 단위를 upsert 한 뒤 `work_item_plan_link` 를 생성하도록 `WorkItemWriteService` 를 보강했다.
  - 2026-04-08 계획 단위 연결도 계층과 마찬가지로 본체 반영 후 후처리 단계에서 처리하도록 정리해, 실행 내 레코드 순서의 영향을 줄였다.
  - 2026-04-08 `domain_write_services`, `pull_sync_orchestrator`, 전체 `cargo test --manifest-path backend/Cargo.toml` 를 실행해 신규 테스트와 기존 회귀 테스트가 모두 통과함을 확인했다.
  - 2026-04-08 구현 체크리스트 문서의 최근 진행 메모에도 계획 연결 반영 현황을 반영했다.
- 완료 기준:
  - `iteration`, `work_item_plan_link` 최소 저장 구조가 마이그레이션에 반영되어야 한다.
  - payload 의 계획 연결 정보가 있으면 실제 링크가 생성되어야 한다.
  - 관련 테스트와 전체 Rust 테스트가 통과해야 한다.
- 작업 결과: 완료. `iteration`, `work_item_plan_link` 최소 테이블과 인덱스를 마이그레이션으로 추가했고, `WorkItemWriteService` 는 payload 의 `iteration_name` 을 읽어 프로젝트 기준 `iteration` 을 upsert 한 뒤 `work_item_plan_link` 를 `plan_type = iteration`, `link_role = planned`, `is_primary = true` 기준으로 저장하도록 구현했다. 통합 테스트에서 `OPS-101` 업무 항목이 `Sprint 1` 반복 단위와 실제 링크로 연결되는 것을 확인했으며, 전체 Rust 테스트도 모두 통과했다.

## TASK-126 조직/인사 기준정보 관리 API 및 운영 UI 최소선 구현

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/migrations/`
  - `backend/src/app_state.rs`
  - `backend/src/http/routes/`
  - `backend/src/http/router.rs`
  - `backend/src/services/`
  - `backend/tests/http.rs`
  - `backend/tests/bootstrap.rs`
  - `backend/tests/migrations.rs`
  - `src/ui_prototype/organization.html`
  - `src/ui_prototype/admin.html`
  - `src/ui_prototype/styles.css`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직/인사 기준정보를 실제 운영 가능한 최소 단위로 다루기 위해 `organization_master`, `workforce_master` 중심의 관리자 API 와 운영 UI 최소선을 함께 구현한다.
- 진행 현황:
  - 2026-04-08 사용자가 데이터 통합 백엔드와 병행해 백엔드 운영 UI, 조직/인사 정보 관리 기능을 우선적으로 구축하겠다고 방향을 확정했다.
  - 2026-04-08 현재 구현에는 `sync-runs`, `ingestion` 운영 API 만 있고, 조직/인사 기준정보는 실제 런타임 서비스와 API 가 비어 있음을 확인했다.
  - 2026-04-08 이번 작업은 `organization_master` 확장, `workforce_master` 최소 테이블/서비스, 관리자 API, 정적 운영 UI 프로토타입 보강까지 하나의 수직 슬라이스로 진행한다.
  - 2026-04-08 `organization_master` 에 상위 조직과 유효기간 컬럼을 추가하고 `workforce_master` 최소 테이블을 새 마이그레이션으로 반영했다.
  - 2026-04-08 `master-data` 서비스와 관리자 API 를 추가해 조직/인력 기준정보의 목록 조회와 최소 등록/수정 경로를 `in-memory`/`PostgreSQL` 양쪽에서 지원하도록 연결했다.
  - 2026-04-08 `http`, `master_data_service`, `bootstrap`, `migrations` 테스트를 보강하고 `cargo fmt`, `cargo test --manifest-path backend/Cargo.toml` 전체 통과를 확인했다.
  - 2026-04-08 정적 운영 UI 프로토타입의 `organization`/`admin` 화면과 UX 문서에 조직 마스터, 인력 기준정보, 연계 설정, `sync-runs` 운영 흐름을 반영했다.
- 완료 기준:
  - 조직/인사 기준정보 최소 저장 구조가 마이그레이션에 반영되어야 한다.
  - 관리자 API 로 조직/인력 목록 조회와 최소 등록/수정이 가능해야 한다.
  - 운영 UI 프로토타입에 조직/인사 관리 흐름과 백엔드 운영 흐름이 반영되어야 한다.
  - 관련 테스트와 전체 Rust 테스트가 통과해야 한다.
- 작업 결과: 완료. `organization_master` 확장과 `workforce_master` 최소 테이블을 마이그레이션으로 추가했고, `GET/POST /api/v1/admin/master-data/organizations`, `GET/POST /api/v1/admin/master-data/workforce` 경로를 구현해 조직/인력 기준정보의 목록 조회와 최소 등록/수정이 가능해졌다. `AppState` 에 `master_data_store` 를 추가해 데이터베이스가 없는 HTTP 테스트에서도 동일 계약을 검증할 수 있게 했고, 별도 저장소 테스트로 `PostgreSQL` 기준 upsert/list 경로도 고정했다. 운영 UI 프로토타입은 조직 운영 화면과 관리자 콘솔 화면 모두 기준정보 관리와 연계 운영 흐름이 드러나도록 보강했으며, `cargo fmt --manifest-path backend/Cargo.toml --all`, `cargo test --manifest-path backend/Cargo.toml` 전체 통과를 확인했다.

## TASK-127 조직/인사 기준정보를 통합 파이프라인에 연결

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/src/services/normalization.rs`
  - `backend/src/services/pull_sync.rs`
  - `backend/src/services/`
  - `backend/tests/domain_write_services.rs`
  - `backend/tests/pull_sync_orchestrator.rs`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직/인사 기준정보가 `pull -> raw_ingestion -> normalization -> domain write` 흐름 안에서 실제로 `organization_master`, `workforce_master` 에 반영되도록 최소 도메인 쓰기 경로를 연결한다.
- 진행 현황:
  - 2026-04-08 현재 조직/인사 기준정보는 관리자 API 로 수동 등록/수정은 가능하지만, 통합 파이프라인을 통한 자동 반영 경로는 아직 비어 있다.
  - 2026-04-08 이번 작업은 `organization`, `workforce` source object 를 표준화/매핑하고, 실행 단위 도메인 쓰기 단계에 조직/인력 마스터 반영 서비스를 추가하는 범위로 진행한다.
  - 2026-04-08 `NormalizationPipeline` 의 target entity 매핑에 `organization`, `workforce` 를 추가해 조직/인력 원시 레코드도 표준화와 `identity_mapping` 갱신 대상에 포함시켰다.
  - 2026-04-08 `OrganizationWriteService`, `WorkforceWriteService` 를 추가하고, `PullSyncOrchestrator` 가 `organization -> workforce -> project -> work_item` 순서로 반영하도록 연결했다.
  - 2026-04-08 서비스 단위 테스트와 오케스트레이터 통합 테스트를 보강해 조직/인력 원시 레코드가 실제 `organization_master`, `workforce_master` 에 반영되는 경로를 고정했다.
  - 2026-04-08 `cargo fmt --manifest-path backend/Cargo.toml --all`, `cargo test --manifest-path backend/Cargo.toml` 전체 통과를 확인했다.
- 완료 기준:
  - `organization`, `workforce` 원시 적재 레코드가 표준화 이후 실제 마스터 테이블에 반영되어야 한다.
  - `pull` 실행 안에서 조직 -> 인력 순서 반영이 가능해야 한다.
  - 관련 테스트와 전체 Rust 테스트가 통과해야 한다.
- 작업 결과: 완료. 조직/인력 기준정보는 이제 수동 관리자 API 뿐 아니라 통합 수집 파이프라인에서도 반영된다. `NormalizationPipeline` 이 `organization`, `workforce` 레코드를 표준화하고 `identity_mapping` 을 갱신하도록 확장했고, 새 `OrganizationWriteService`, `WorkforceWriteService` 를 통해 실행 단위 기준 조직 마스터와 인력 마스터를 실제 테이블에 upsert 하도록 연결했다. `PullSyncOrchestrator` 는 반영 순서를 `organization -> workforce -> project -> work_item` 으로 고정해 인력의 주 소속 참조가 같은 실행 안에서 안정적으로 해소되게 했고, 서비스 테스트와 오케스트레이터 통합 테스트에서 `hr` 원천의 조직/인력 레코드가 실제 `organization_master`, `workforce_master` 에 저장되는 것을 확인했다.

## TASK-128 조직/인사 기준정보의 `push` 수신 후처리 연결

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/src/http/routes/ingestion.rs`
  - `backend/src/services/push_ingestion.rs`
  - `backend/src/services/sync_runs.rs`
  - `backend/tests/adapter_integration.rs`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: `POST /api/v1/ingestion/events` 로 수신한 조직/인사 이벤트도 원시 적재 후 즉시 표준화와 도메인 반영까지 이어지도록 후처리 오케스트레이션을 연결한다.
- 진행 현황:
  - 2026-04-08 현재 `push` 경로는 원시 적재까지만 연결돼 있고, `organization_master`, `workforce_master` 자동 반영은 `pull` 경로에만 붙어 있다.
  - 2026-04-08 이번 작업은 `push` 수신 후처리 서비스, 실행 완료 상태 반영, HTTP 통합 테스트 추가 범위로 진행한다.
  - 2026-04-08 `PushIngestionProcessor` 를 추가해 `ingestion/events` 수신 직후 표준화, 참조 해소, 조직/인력/프로젝트/업무항목 반영이 같은 실행 안에서 이어지도록 연결했다.
  - 2026-04-08 `SyncRunRepository` 에 `complete_push_run` 경로를 추가해 `push` 실행 이력이 `completed/push_completed` 또는 부분 완료 상태로 종료되도록 정리했다.
  - 2026-04-08 `adapter_integration` 테스트에 `POST /api/v1/ingestion/events` 기반 조직/인력 마스터 반영 검증을 추가했고, 전체 Rust 테스트 통과를 확인했다.
- 완료 기준:
  - `ingestion/events` 로 수신한 조직/인사 이벤트가 실제 마스터 테이블에 반영되어야 한다.
  - `push` 실행 이력이 `completed/push_completed` 로 종료되어야 한다.
  - 관련 테스트와 전체 Rust 테스트가 통과해야 한다.
- 작업 결과: 완료. `ingestion/events` 라우트는 이제 원시 적재 뒤에 `PushIngestionProcessor` 를 호출해 `organization`, `workforce`, `project`, `work_item` 이벤트를 즉시 표준화와 도메인 반영까지 내려보낸다. 조직/인사 기준정보 `push` 이벤트는 별도 배치를 기다리지 않고 실제 `organization_master`, `workforce_master` 까지 반영되며, 실행 이력도 `push_completed` 로 닫힌다. `adapter_integration` 테스트에서 `hr` 조직/인력 `push` 이벤트가 실제 마스터 테이블 반영과 실행 상태 종료까지 이어지는 것을 확인했고, `cargo fmt --manifest-path backend/Cargo.toml --all`, `cargo test --manifest-path backend/Cargo.toml` 전체 통과를 확인했다.

## TASK-129 `project/work_item` 조직·인력 마스터 참조 반영

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/migrations/`
  - `backend/src/services/project_write.rs`
  - `backend/src/services/work_item_write.rs`
  - `backend/tests/domain_write_services.rs`
  - `backend/tests/pull_sync_orchestrator.rs`
  - `backend/tests/migrations.rs`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: `project` 와 `work_item` 가 payload 의 조직 코드, 사번 기반으로 실제 `organization_master`, `workforce_master` 를 참조하도록 도메인 필드와 쓰기 로직을 확장한다.
- 진행 현황:
  - 2026-04-08 현재 조직/인사 기준정보는 마스터 테이블까지 반영되지만, `project`, `work_item` 본체는 아직 해당 마스터를 실참조하지 않는다.
  - 2026-04-08 이번 작업은 참조 컬럼 마이그레이션, 쓰기 서비스 보강, 통합 테스트 확장, 커밋/푸시까지 포함해 진행한다.
  - 2026-04-08 `project_owner_workforce_id`, `owning_organization_id`, `assignee_workforce_id`, `reporter_workforce_id` 컬럼과 `FK`/인덱스를 추가하는 마이그레이션을 반영했다.
  - 2026-04-08 `ProjectWriteService`, `WorkItemWriteService` 가 payload 의 조직 코드와 사번을 기준으로 실제 `organization_master`, `workforce_master` 를 조회해 본체 참조를 갱신하도록 보강했다.
  - 2026-04-08 수직 슬라이스 통합 테스트를 확장해 `project` 와 `work_item` 가 실제 조직/인력 마스터를 참조하는 것을 검증했고, 전체 Rust 테스트 통과를 확인했다.
  - 2026-04-08 변경사항을 커밋하고 `origin/main` 에 푸시했다.
- 완료 기준:
  - `project` 와 `work_item` 이 조직/인력 마스터 참조 컬럼을 가져야 한다.
  - payload 의 조직 코드, 담당자/등록자 사번이 실제 참조로 반영되어야 한다.
  - 관련 테스트와 전체 Rust 테스트가 통과해야 한다.
  - 변경사항이 커밋되고 원격에 푸시되어야 한다.
- 작업 결과: 완료. `project` 에 `project_owner_workforce_id`, `work_item` 에 `owning_organization_id`, `assignee_workforce_id`, `reporter_workforce_id` 를 추가하고 실제 `organization_master`, `workforce_master` 를 참조하도록 확장했다. `ProjectWriteService` 는 payload 의 `owning_organization_code`, `project_owner_employee_number` 를 읽어 프로젝트 주관 조직과 책임자를 연결하고, `WorkItemWriteService` 는 `owning_organization_code`, `assignee_employee_number`, `reporter_employee_number` 를 읽어 담당 조직과 담당자/등록자를 연결한다. 통합 테스트에서 `OPS` 프로젝트와 `OPS-101` 업무 항목이 모두 `platform` 조직과 `E9001` 인력을 실제 참조하는 것을 확인했고, `cargo fmt --manifest-path backend/Cargo.toml --all`, `cargo test --manifest-path backend/Cargo.toml` 전체 통과 후 커밋과 푸시까지 완료했다.

## TASK-130 프로젝트/업무 항목 운영 조회 API 최소선 구현

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/src/http/routes/`
  - `backend/src/http/router.rs`
  - `backend/src/services/`
  - `backend/tests/`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 운영자가 `project/work_item` 와 연결된 조직/인력 참조를 바로 확인할 수 있도록 조회 API 최소선을 구현한다.
- 진행 현황:
  - 2026-04-08 현재 쓰기 파이프라인은 충분히 연결됐지만, 운영자가 결과를 바로 볼 수 있는 `project/work_item` 조회 API 는 아직 없다.
  - 2026-04-08 이번 작업은 `GET /api/v1/admin/projects`, `GET /api/v1/admin/work-items` 최소선과 DB 기반 통합 테스트 추가 범위로 진행한다.
  - 2026-04-08 `DomainReadRepository` 와 `domain_data` 라우트를 추가해 `project/work_item` 목록 조회 시 조직/인력 참조가 함께 내려오도록 구현했다.
  - 2026-04-08 `project_status`, `project_code`, `owning_organization_code`, `current_common_status`, `assignee_employee_number` 기준 최소 필터를 지원하도록 정리했다.
  - 2026-04-08 DB 기반 통합 테스트를 추가해 `project` 와 `work_item` 조회 API 가 실제 조직/인력 마스터 참조를 반환하는 것을 검증했고, 전체 Rust 테스트 통과를 확인했다.
- 완료 기준:
  - 프로젝트 목록과 업무 항목 목록에서 조직/인력 참조가 함께 조회되어야 한다.
  - 필터 최소선이 지원되어야 한다.
  - 관련 테스트와 전체 Rust 테스트가 통과해야 한다.
- 작업 결과: 완료. `GET /api/v1/admin/projects`, `GET /api/v1/admin/work-items` 최소 조회 API 를 추가했고, `project` 는 주관 조직과 프로젝트 책임자, `work_item` 은 담당 조직과 담당자/등록자 정보를 함께 반환한다. 운영자는 이제 쓰기 파이프라인 결과를 바로 조회 API 로 확인할 수 있고, `project_code`, `project_status`, `owning_organization_code`, `current_common_status`, `assignee_employee_number` 기준 필터도 사용할 수 있다. DB 기반 통합 테스트에서 `OPS` 프로젝트와 `OPS-100/OPS-101` 업무 항목이 실제 조직/인력 참조와 함께 반환되는 것을 확인했고, `cargo fmt --manifest-path backend/Cargo.toml --all`, `cargo test --manifest-path backend/Cargo.toml` 전체 통과를 확인했다.

## TASK-131 구현 리뷰 및 문서 누락 점검

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/src/http/routes/domain_data.rs`
  - `backend/tests/http.rs`
  - `docs/README.md`
  - `docs/architecture/README.md`
  - `docs/architecture/current_backend_implementation_status_summary.md`
  - `docs/operations/README.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 운영 UI 연결 전에 현재 구현을 리뷰하고 문서 누락 여부를 점검한 뒤, 전체 구현 현황을 요약한 기준 문서를 추가한다.
- 진행 현황:
  - 2026-04-08 현재 `pull/push`, 표준화, 마스터 반영, 도메인 반영, 운영 조회 API 까지 구현 범위가 넓어져 있어 한 번 기준선 문서화가 필요한 시점이다.
  - 2026-04-08 운영 조회 API 리뷰 과정에서 DB 미연결 상태를 `200 OK + 빈 목록` 으로 반환하는 동작을 확인했고, 운영 오판을 막기 위해 보정이 필요하다고 판단했다.
  - 2026-04-08 `domain_data` 라우트를 수정해 DB 미연결 상태에는 `503 SERVICE_UNAVAILABLE` 를 반환하도록 변경하고, 회귀 테스트를 추가한다.
  - 2026-04-08 위키 인덱스 문서의 `최종 수정일` 과 핵심 문서 링크를 점검하고, 구현 현황 스냅샷 문서를 새로 작성해 연결한다.
- 완료 기준:
  - 리뷰 결과에서 수정이 필요한 실제 이슈가 있으면 반영되어야 한다.
  - 문서 누락 또는 인덱스 누락이 정리되어야 한다.
  - 현재 구현 범위를 한눈에 설명하는 요약 문서가 생성되어야 한다.
- 작업 결과: 완료. 운영 조회 API 가 DB 미연결 상태를 정상 빈 목록처럼 반환하던 동작을 `503 SERVICE_UNAVAILABLE` 로 보정했고, 관련 HTTP 테스트를 추가했다. 또한 `docs/README.md`, `docs/architecture/README.md`, `docs/operations/README.md` 의 메타데이터를 최신화하고, 현재 구현 범위와 실행 흐름, 남은 공백을 정리한 `docs/architecture/current_backend_implementation_status_summary.md` 를 새로 추가해 아키텍처 위키와 문서 홈에서 바로 접근할 수 있게 했다.

## TASK-132 운영 UI 프로토타입 실데이터 연결 최소선 구현

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/admin.html`
  - `src/ui_prototype/organization.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/overview/ui_ux_prototype.md`
  - `README.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 정적 운영 UI 프로토타입의 `admin`/`organization` 화면이 실제 운영 API 를 조회해 동기화 실행, 조직/인력 마스터, 프로젝트/업무 항목 정보를 표시하도록 최소 연결을 구현한다.
- 진행 현황:
  - 2026-04-08 현재 운영 UI 는 정적 프로토타입 수준이라 API 응답을 직접 보여주지 못한다.
  - 2026-04-08 React 전환 전 단계로, 기존 정적 프로토타입 구조를 유지하면서 공용 `app.js` 에 실데이터 fetch/렌더링 계층을 추가하는 방식으로 진행한다.
  - 2026-04-08 `admin.html`, `organization.html` 에 API 기준 URL, 새로고침, 조직 코드 필터, 라이브 데이터 패널을 추가하고 `sync-runs`, `master-data`, `projects`, `work-items` API 를 연결한다.
  - 2026-04-08 루트 `README` 와 UI 프로토타입 문서, 구현 현황 문서를 함께 갱신해 실데이터 연결 방법과 현재 상태를 일치시킨다.
- 완료 기준:
  - 관리자 콘솔과 조직 운영 화면이 실제 운영 API 조회 결과를 표시해야 한다.
  - API 미연결/실패 상태를 화면에서 구분 가능해야 한다.
  - 로컬 확인 방법이 문서에 반영되어야 한다.
- 작업 결과: 완료. `admin.html`, `organization.html` 상단에 `API Base URL` 입력과 새로고침 컨트롤을 추가했고, 공용 `app.js` 에 운영 API fetch/렌더링 계층을 구현해 `sync-runs`, 조직/인력 마스터, 프로젝트, 업무 항목 조회 결과가 실제 화면에 표시되도록 연결했다. 조직 운영 화면은 조직 코드 기준 필터도 지원하며, 두 화면 모두 연결 성공/실패 상태를 상단 상태 칩과 메시지로 구분해 보여준다. 또한 [ui_ux_prototype.md](/Users/yklee/repos/alm_integration/docs/overview/ui_ux_prototype.md), [README.md](/Users/yklee/repos/alm_integration/README.md), [current_backend_implementation_status_summary.md](/Users/yklee/repos/alm_integration/docs/architecture/current_backend_implementation_status_summary.md) 를 갱신해 로컬 실행 방법과 현재 구현 상태를 문서에 반영했다.

## TASK-133 운영 UI 렌더 점검 및 기능 시나리오 문서화

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `docs/operations/backend_operation_ui_render_and_scenarios.md`
  - `docs/operations/README.md`
  - `docs/README.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 운영 UI 프로토타입의 실제 렌더 및 서버 연결 상태를 점검하고, 현재 구현된 기능의 운영 시나리오를 문서로 정리한다.
- 진행 현황:
  - 2026-04-08 Rust 백엔드와 정적 파일 서버를 각각 실행하고, 운영 UI 진입 URL 과 관리자 API 응답 상태를 점검했다.
  - 2026-04-08 `health`, `admin` 운영 API, 정적 HTML 서빙, 조직/인력 기준정보, `sync-runs` 생성 및 조회를 실제 명령으로 확인했다.
  - 2026-04-08 Playwright 기반 실제 브라우저 캡처는 브라우저 바이너리 미설치로 실패했고, Safari 자동 DOM 추출도 AppleEvent timeout 으로 미완료 상태임을 확인했다.
  - 2026-04-08 확인된 항목과 미확인 항목을 구분해 운영 UI 렌더 점검 및 기능 시나리오 문서를 새로 작성하고 운영 위키 인덱스에 연결한다.
- 완료 기준:
  - 실제 확인한 서버/API 상태와 렌더 점검 결과가 문서로 정리되어야 한다.
  - 현재 운영 가능한 기능 흐름이 시나리오 형태로 정리되어야 한다.
  - 운영 위키에서 새 문서로 접근 가능해야 한다.
- 작업 결과: 완료. `backend_operation_ui_render_and_scenarios.md` 를 새로 작성해 서버 기동 확인, 운영 API 응답 확인, 렌더 점검 결과, 로컬 재현 절차, 운영 UI 기능 시나리오를 정리했다. 이번 점검에서는 정적 HTML 서빙과 관리자 API 응답, 조직/인력/동기화 실행 데이터 생성 및 조회를 실제로 확인했고, 브라우저 픽셀 캡처는 Playwright 브라우저 미설치와 Safari AppleEvent timeout 으로 미완료임을 문서에 명시했다. 운영 위키와 문서 위키 홈에도 새 문서를 연결해 다음 검증과 UI 보강 작업의 기준 문서로 사용할 수 있게 했다.

## TASK-134 시스템 관리자 전용 통합 데이터 관리 UI 방향 재정의

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `docs/overview/integrated_data_management_ui_direction.md`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/overview/role_based_ux_direction.md`
  - `docs/overview/README.md`
  - `docs/architecture/application_and_governance_architecture_draft.md`
  - `docs/README.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 기존 운영 UI 방향을 재검토하고, 시스템 관리자만 접근 가능한 별도 통합 데이터 관리 UI 의 정보구조와 관리 대상 분류를 새 기준 문서로 정리한다.
- 진행 현황:
  - 2026-04-08 사용자가 현재 UI 방향은 유지하지 않고, 시스템 관리자 전용 데이터 관리 UI 를 별도 구조로 다시 설계하겠다고 방향을 명확히 했다.
  - 2026-04-08 새 UI 는 역할 기반 운영 화면이 아니라 관리 대상 데이터 종류 중심으로 구성해야 하며, 자체 `ALM` 데이터, 외부 연결 시스템 데이터, 조직 관리, 인력 관리 4축이 우선이라는 요구를 확인했다.
  - 2026-04-08 이 요구를 기준으로 새 개요 문서를 작성하고, 기존 역할 기반 UX/프로토타입 문서에는 방향 전환 메모를 반영한다.
- 완료 기준:
  - 시스템 관리자 전용 데이터 관리 UI 의 목적과 접근 제어가 문서화되어야 한다.
  - 관리 대상 4분류와 권장 화면 구조가 정리되어야 한다.
  - 기존 UI 프로토타입 문서와 역할 기반 UX 문서에도 새 방향이 연결되어야 한다.
- 작업 결과: 완료. `integrated_data_management_ui_direction.md` 를 새로 작성해 시스템 관리자 전용 통합 데이터 관리 UI 의 목적, 접근 제어, 정보구조, 4개 관리 축, 권장 화면 구조와 시나리오를 정리했다. 또한 기존 `ui_ux_prototype.md` 에는 현재 프로토타입이 최종 운영 UI 방향을 대표하지 않는다는 점과 새 기준 문서 링크를 반영했고, `role_based_ux_direction.md`, `application_and_governance_architecture_draft.md`, 개요/문서 위키 인덱스에도 새 방향 문서를 연결했다.

## TASK-135 통합 데이터 관리 센터 프로토타입 1차 구현

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_admin.html`
  - `src/ui_prototype/index.html`
  - `src/ui_prototype/app.js`
  - `src/ui_prototype/styles.css`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 새로 정의한 방향에 맞춰 시스템 관리자 전용 통합 데이터 관리 센터 프로토타입을 별도 페이지로 만들고, 내부 `ALM` 데이터, 외부 시스템별 데이터, 조직, 인력을 관리 대상별로 분리해 표현한다.
- 진행 현황:
  - 2026-04-08 기존 `admin.html` 과 `organization.html` 은 방향 전환 이후 최종 구조로 유지하지 않기로 했다.
  - 2026-04-08 새 페이지 `data_admin.html` 을 추가하고, 좌측 탐색과 본문을 관리 대상 4축 중심으로 다시 구성한다.
  - 2026-04-08 현재 존재하는 관리자 API 를 이용해 내부 `ALM`, 외부 시스템 실행 이력, 조직, 인력 데이터를 라이브로 표시하는 최소선을 연결한다.
- 완료 기준:
  - 시스템 관리자 전용 데이터 관리 센터 페이지가 생성되어야 한다.
  - 내부 `ALM`, 외부 시스템, 조직, 인력 관리 섹션이 분리되어 보여야 한다.
  - 현재 운영 API 를 이용한 라이브 데이터 표시가 가능해야 한다.
- 작업 결과: 완료. `src/ui_prototype/data_admin.html` 을 새로 추가해 시스템 관리자 전용 통합 데이터 관리 센터 프로토타입을 만들었다. 화면은 `자체 ALM 데이터`, `외부 연결 시스템 데이터`, `조직 관리`, `인력 관리` 4개 섹션으로 분리되어 있고, `app.js` 의 새 데이터 로더가 `sync-runs`, `master-data`, `projects`, `work-items` 관리자 API 를 읽어 각 섹션에 라이브 데이터를 표시한다. 외부 시스템 데이터는 현재 가용 API 범위 안에서 `sync-runs` 를 `Jira`, `Bitbucket`, `Bamboo`, `Confluence` 기준으로 분리해 보여주고, 인력 관리는 조직 코드 필터 기준으로 목록을 표시하도록 구현했다. `index.html` 탐색에도 새 페이지를 추가했고, `ui_ux_prototype.md` 에 새 프로토타입 위치를 반영했다.

## TASK-144 조직 관리 UI 레퍼런스 조사 및 디자인 재구성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/styles.css`
  - `docs/overview/organization_admin_ui_reference_research.md`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/overview/integrated_data_management_ui_direction.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 시스템 관리자용 조직 관리 UI 가 실제 운영 콘솔처럼 보이도록 웹 레퍼런스를 조사하고, 공통 패턴을 정리한 뒤 조직 관리 프로토타입을 새 구조로 재디자인한다.
- 진행 현황:
  - 2026-04-08 사용자가 현재 컨셉에 맞는 UI/UX 레퍼런스를 웹에서 조사하고, 거기에 맞게 디자인을 새로 해보자고 요청했다.
  - 2026-04-08 Atlassian, Microsoft Entra/Microsoft 365, Okta, IBM 데이터 거버넌스 계열 화면을 기준으로 목록/상세/액션/감사 구조를 조사했다.
  - 2026-04-08 조사 내용을 `organization_admin_ui_reference_research.md` 에 정리하고, 조직 관리 화면을 `Directory + Inspector + Action Rail` 구조로 재배치한다.
  - 2026-04-08 `data_organizations.html` 과 `styles.css` 를 재구성해 더 차분한 엔터프라이즈 관리 콘솔 톤과 3열 작업면을 적용한다.
  - 2026-04-08 `node --check`, `git diff --check`, 조직 관리 페이지 `HTTP 200` 응답으로 정적 검증을 수행한다.
- 완료 기준:
  - 웹 레퍼런스 조사 결과가 문서로 남아야 한다.
  - 조직 관리 화면이 조사 결과를 반영한 새 레이아웃으로 바뀌어야 한다.
  - 관련 문서와 정적 검증이 완료되어야 한다.
- 작업 결과: 완료. 조직 관리 UI 레퍼런스를 Atlassian 관리 콘솔, Microsoft Entra/Microsoft 365 관리자 화면, Okta Lifecycle Management, IBM Governance and Catalog 기준으로 조사해 공통 패턴을 정리했다. 이를 바탕으로 조직 관리 프로토타입은 `Directory + Inspector + Action Rail` 구조, 상단 명령 덱, 중앙 구조 인스펙터, 우측 액션 레일, 하단 감사 이력 패널을 갖는 엔터프라이즈 관리 콘솔 스타일로 재구성했다. 관련 기준 문서도 함께 갱신했고 정적 검증도 통과했다.

## TASK-145 데이터 관리 하위 화면 콘솔 톤 일괄 재구성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/data_alm.html`
  - `src/ui_prototype/data_external_jira.html`
  - `src/ui_prototype/data_external_bitbucket.html`
  - `src/ui_prototype/data_external_bamboo.html`
  - `src/ui_prototype/data_external_confluence.html`
  - `src/ui_prototype/styles.css`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 관리 화면에서 정리한 관리 콘솔 톤을 인력 관리, 자체 `ALM`, 외부 시스템별 데이터 관리 화면에도 확장 적용한다.
- 진행 현황:
  - 2026-04-08 사용자가 다른 페이지도 같은 톤으로 재구성하고 결과를 볼 수 있게 해달라고 요청했다.
  - 2026-04-08 `data_workforce`, `data_alm`, 외부 시스템 4개 화면을 상단 명령 덱, 중앙 인스펙터, 우측 보조 패널 구조로 다시 배치한다.
  - 2026-04-08 `styles.css` 에 공통 `data-*` 계열 콘솔 스타일을 추가해 조직 관리와 시각 톤을 맞춘다.
  - 2026-04-08 `node --check`, `git diff --check`, 하위 화면 전부 `HTTP 200` 응답으로 정적 검증을 수행한다.
- 완료 기준:
  - 데이터 관리 하위 화면들이 같은 디자인 시스템 톤을 가져야 한다.
  - 기존 API 바인딩은 유지되어야 한다.
  - 주요 화면들이 실제로 열리고 정적 검증이 통과해야 한다.
- 작업 결과: 완료. 인력 관리, 자체 `ALM`, 외부 시스템 4개 화면을 조직 관리와 같은 엔터프라이즈 관리 콘솔 톤으로 재구성했다. 각 화면은 상단 명령 덱, 중앙 인스펙터, 우측 보조 패널 또는 액션 레일 구조를 갖고, 기존 API 바인딩 ID 는 유지해 즉시 연결 가능한 상태를 유지했다. `node --check src/ui_prototype/app.js`, `git diff --check`, 대상 페이지 7개 `HTTP 200` 응답도 모두 확인했다.

## TASK-146 조직 관리 검증용 더미 데이터 적재

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 관리 화면의 현재 구현 범위를 구체적으로 확인할 수 있도록 다단계 조직 구조와 샘플 구성원을 실제 관리자 API 에 적재한다.
- 진행 현황:
  - 2026-04-08 사용자가 조직 관리부터 집중해서 구체화해보자고 했고, 현재 구현 상황을 더 명확히 보기 위해 더미 데이터 생성을 요청했다.
  - 2026-04-08 백엔드가 내려가 있음을 확인하고 `cargo run --manifest-path backend/Cargo.toml` 로 개발 서버를 다시 기동했다.
  - 2026-04-08 `platform_div -> platform_team -> datahub_group -> ingest_part/normalize_part`, `innovation_div -> alm_team -> project_group -> pm_part`, `delivery_team -> release_group -> ci_part` 구조의 샘플 조직을 관리자 API로 적재했다.
  - 2026-04-08 샘플 구성원 6명을 적재하고, 일부 조직명 수정, 구성원 이동, 구성원 비활성화를 추가로 실행해 조직 변경 이력과 구성원 이동 이력이 함께 보이도록 만들었다.
  - 2026-04-08 구조 스냅샷, 활성 조직 목록, 활성 인력 목록, 조직/구성원 이력 API 를 조회해 적재 결과를 검증했다.
- 완료 기준:
  - 다단계 조직 구조와 샘플 구성원이 실제 API 에 적재되어야 한다.
  - 조직 구조 스냅샷, 조직 변경 이력, 구성원 이동 이력에서 결과를 확인할 수 있어야 한다.
  - 조직 관리 프로토타입 검토에 사용할 수 있는 구체적 예시가 준비되어야 한다.
- 작업 결과: 완료. `플랫폼사업부`, `업무혁신사업부` 아래에 팀/그룹/파트 구조를 적재했고, `박연계`, `최수집`, `한배포`, `윤프로젝트`, `오관리` 등 샘플 구성원을 넣었다. `데이터허브그룹 -> 데이터통합허브그룹` 조직명 변경, `E9002` 의 `수집연계파트 -> 표준화파트` 이동, `E9003` 비활성화를 함께 실행해 조직 변경 이력과 구성원 이동 이력까지 채워졌다. 현재 `platform_team` 구조 스냅샷은 상위 `플랫폼사업부`, 직속 하위 `데이터통합허브그룹`, 하위 포함 4개 조직, 활성 구성원 2명으로 조회된다.

## TASK-147 운영 UI API 연결 CORS 보강

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `backend/Cargo.toml`
  - `backend/src/http/router.rs`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 정적 프로토타입 서버와 Rust 백엔드 API 가 서로 다른 origin 으로 동작하면서 브라우저에서 조직/인력 더미 데이터 조회가 막히는 문제를 해결하기 위해 개발용 CORS 설정을 추가한다.
- 진행 현황:
  - 2026-04-08 조직/인력 더미 데이터는 API 에 정상 적재되어 있음을 확인했다.
  - 2026-04-08 `127.0.0.1:8001` 에서 서빙하는 프로토타입이 `127.0.0.1:8080` API 를 호출하는 구조라 브라우저에서 CORS 차단 가능성이 높음을 확인했다.
  - 2026-04-08 백엔드 라우터에 개발용 CORS 허용 origin 과 메서드를 추가한다.
- 완료 기준:
  - 프로토타입 페이지에서 브라우저 fetch 로 관리자 API 조회가 가능해야 한다.
  - 조직 관리 화면에서 적재된 더미 데이터가 표시되어야 한다.
  - 변경 사항이 백로그에 기록되어야 한다.
- 작업 결과: 완료. `tower-http` 의 `CorsLayer` 를 백엔드 라우터에 추가해 `http://127.0.0.1:8000`, `http://localhost:8000`, `http://127.0.0.1:8001`, `http://localhost:8001` 에서 관리자 API 를 브라우저로 호출할 수 있게 했다. `GET`, `POST`, `PATCH`, `DELETE`, `OPTIONS` 메서드를 허용했고, 실제로 `Origin: http://127.0.0.1:8001` 요청에 `access-control-allow-origin` 이 포함되는 것을 확인했다. `PATCH` 기준 preflight 응답도 정상 확인했으며, `cargo test --manifest-path backend/Cargo.toml`, `node --check src/ui_prototype/app.js` 검증까지 마쳤다.

## TASK-148 조직 관리 액션 영향 미리보기 보강

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 삭제, 상위 조직 변경, 구성원 이동, 구성원 비활성화 전에 영향 범위를 즉시 확인할 수 있도록 조직 관리 화면의 상호작용을 보강한다.
- 진행 현황:
  - 2026-04-08 사용자가 조직 화면 상호작용을 더 다듬자고 했고, 우선순위로 삭제/이동 영향 미리보기를 선택했다.
  - 2026-04-08 조직 디렉터리 행 클릭으로 선택 조직을 바꿀 수 있게 연결한다.
  - 2026-04-08 우측 조직 액션 패널 아래에 계층 이동/삭제 영향 미리보기, 구성원 액션 패널 아래에 이동/비활성화 영향 미리보기를 추가한다.
  - 2026-04-08 입력값 변경 시 현재 적재된 조직/인력 데이터 기준으로 실시간 프리뷰를 다시 계산하도록 구현한다.
- 완료 기준:
  - 조직 코드나 상위 조직 코드 변경 시 계층 이동/삭제 영향이 즉시 표시되어야 한다.
  - 구성원 이동 대상 조직 변경 시 현재 조직과 대상 조직의 인원 영향이 즉시 표시되어야 한다.
  - 조직 디렉터리에서도 선택 조직 전환이 가능해야 한다.
- 작업 결과: 완료. 조직 관리 화면에 `organization-action-preview`, `organization-member-action-preview` 패널을 추가했고, `app.js` 에 현재 조직/인력 상태를 캐시해 삭제 가능 여부, 하위 조직 수, 직속 활성 구성원 수, 구성원 이동 전후 인원 수를 실시간 계산하도록 붙였다. 조직 디렉터리 테이블의 행 클릭도 선택 동작과 연결되어 트리뿐 아니라 목록에서도 같은 조직을 바로 선택할 수 있다.

## TASK-149 조직 관리 선택 상태 하이라이트 보강

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 트리, 디렉터리, 인스펙터가 같은 선택 조직을 보고 있다는 점을 더 명확히 드러내기 위해 선택 배지와 목록 하이라이트를 추가한다.
- 진행 현황:
  - 2026-04-08 사용자가 선택 상태를 더 분명하게 보이게 해달라고 요청했다.
  - 2026-04-08 인스펙터 상단에 현재 선택 조직 배지와 경로 요약 배너를 추가한다.
  - 2026-04-08 조직 디렉터리 표의 현재 선택 행을 하이라이트 처리하고 hover 상태도 강화한다.
  - 2026-04-08 선택 조직이 바뀔 때 배너, 트리, 디렉터리 행 하이라이트가 함께 갱신되도록 `app.js` 에 선택 상태 렌더러를 추가한다.
- 완료 기준:
  - 현재 선택 조직이 인스펙터 상단에 고정 표시되어야 한다.
  - 조직 디렉터리 목록에서 선택 행이 시각적으로 구분되어야 한다.
  - 선택 조직 변경 시 배너와 목록 하이라이트가 함께 갱신되어야 한다.
- 작업 결과: 완료. 인스펙터 상단에 `현재 선택` 배지와 상위 경로, 조직 코드, 직속 활성 구성원 수를 보여주는 배너를 추가했다. 조직 디렉터리 목록은 현재 선택 조직을 `active-row` 스타일로 강조하고, 트리/디렉터리/폼 선택이 바뀔 때 같은 기준으로 다시 렌더링되도록 연결했다.

## TASK-150 조직 관리 검색 및 범위 필터 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 수가 늘어도 빠르게 탐색할 수 있도록 조직 관리 화면에 코드/이름 검색과 범위 필터를 추가한다.
- 진행 현황:
  - 2026-04-08 사용자가 필터는 필수라고 했고, 조직 화면 탐색성을 우선 보강하기로 했다.
  - 2026-04-08 상단 명령 덱에 `조직 검색`, `범위 필터` 입력을 추가한다.
  - 2026-04-08 전체 조직, 최상위 조직만, 선택 조직 하위만 보기 범위를 지원하도록 `app.js` 에 클라이언트 필터 로직을 추가한다.
  - 2026-04-08 검색 시 매칭 조직의 상위 경로와 하위 맥락이 같이 보이도록 가시 조직 계산 로직을 보강한다.
- 완료 기준:
  - 조직명 또는 조직 코드 검색이 즉시 반영되어야 한다.
  - `전체 조직`, `최상위 조직만`, `선택 조직 하위만` 범위 전환이 가능해야 한다.
  - 필터 변경 시 디렉터리와 트리가 함께 갱신되어야 한다.
- 작업 결과: 완료. 조직 관리 화면 상단에 검색 입력과 범위 셀렉트를 추가했고, 현재 불러온 조직 데이터를 기준으로 즉시 필터링하도록 구현했다. 검색은 조직 코드와 이름을 모두 대상으로 하며, 매칭된 조직의 상위 경로와 하위 문맥도 함께 보이도록 처리했다. 범위는 전체, 최상위만, 현재 선택 조직의 하위만 보기로 전환할 수 있고, 디렉터리 목록과 트리가 동시에 갱신된다.

## TASK-151 연결 설정 화면 분리 및 운영 화면 재배치

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_settings.html`
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/data_alm.html`
  - `src/ui_prototype/data_external_jira.html`
  - `src/ui_prototype/data_external_bitbucket.html`
  - `src/ui_prototype/data_external_bamboo.html`
  - `src/ui_prototype/data_external_confluence.html`
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 데이터 관리 운영 화면에서 `API Base URL` 설정을 제거하고, 별도 연결 설정 화면에서만 공통 연결 정보를 저장·검증하도록 구조를 재배치한다.
- 진행 현황:
  - 2026-04-08 사용자가 필터 추가 후 조직 화면 배치가 어색하다고 했고, `API URL` 은 별도 설정 페이지에서 일괄 관리하는 편이 더 낫다고 제안했다.
  - 2026-04-08 이어서 사용자가 운영 화면에서 데이터 `CRUD` 는 그대로 가능해야 한다고 강조했다.
  - 2026-04-08 조직/인력/ALM/외부 시스템 화면에서 `API Base URL` 입력을 제거하고, 저장된 연결 정보 배너만 표시하도록 바꾼다.
  - 2026-04-08 새 `data_settings.html` 을 추가해 관리자 API URL 저장, 기본값 복원, 헬스 체크를 수행하도록 구현한다.
  - 2026-04-08 `app.js` 에 공통 연결 정보 표시기와 설정 페이지 초기화 로직을 추가하고, 운영 화면은 저장된 값만 읽어 쓰도록 조정한다.
  - 2026-04-08 추가로 사용자가 `마스터 연결`, `새로고침`, `더미 데이터 생성` 같은 관리성 액션도 설정 화면으로 빼자고 요청했다.
  - 2026-04-08 조직/인력/ALM/외부 시스템 화면에서 `새로고침`, `더미 데이터 생성` 버튼을 제거하고, 설정 화면에 `조직/인력 더미 데이터 생성` 지원 액션을 추가한다.
  - 2026-04-08 이어서 사용자가 운영 화면에서 연결 정보 자체도 숨기고 연결 상태만 보이게 하자고 요청했다.
- 완료 기준:
  - 운영 화면에서 `API Base URL` 설정 입력이 사라져야 한다.
  - 별도 설정 화면에서 관리자 API URL 저장과 연결 확인이 가능해야 한다.
  - 데이터 `CRUD` 액션은 조직/인력 같은 운영 화면에 그대로 남아 있어야 한다.
- 작업 결과: 완료. `data_settings.html` 을 새로 추가해 관리자 API URL을 별도 설정 화면에서 저장·검증할 수 있게 했고, 조직/인력/내부 ALM/외부 시스템 화면에서는 연결 값 자체를 숨기고 연결 상태 칩과 상태 문구만 보이도록 재배치했다. 운영 화면의 `새로고침`, `더미 데이터 생성` 버튼은 제거하고 자동 로드만 남겼으며, 설정 화면에 `조직/인력 더미 데이터 생성` 지원 액션을 추가했다. `app.js` 는 입력 필드가 없는 운영 화면에서도 저장된 API URL을 공통으로 읽어 사용하도록 보강했고, 설정 화면의 안내 문구도 `CRUD는 운영 화면`, `연결 설정과 보조 액션만 분리` 원칙으로 정리했다.

## TASK-152 운영 UI 사용성 점검 및 필터 개선

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 현재 운영 UI 를 다시 점검하고, 필터 적용 결과를 더 명확히 보여주고 초기화 흐름을 단순하게 만들어 탐색성을 개선한다.
- 진행 현황:
  - 2026-04-08 사용자가 전체 UI 를 검토해서 사용성이 더 좋아지도록 정리해보자고 요청했다.
  - 2026-04-08 조직 화면은 필터 결과가 얼마나 반영됐는지 바로 알기 어렵고, 초기화 액션이 없다는 점을 개선 포인트로 정리했다.
  - 2026-04-08 인력 화면은 조직 코드만으로는 탐색성이 약해 이름/사번 검색과 필터 요약이 필요하다고 판단했다.
  - 2026-04-08 조직 화면에 필터 요약 바와 `필터 초기화` 버튼을 추가하고, 인력 화면에도 이름/사번 검색과 필터 초기화, 결과 요약을 추가한다.
- 완료 기준:
  - 조직 화면에서 필터 결과 건수와 범위가 바로 보여야 한다.
  - 인력 화면에서 이름 또는 사번 검색이 가능해야 한다.
  - 두 화면 모두 필터 초기화가 가능해야 한다.
- 작업 결과: 완료. 조직 화면은 `표시 N/M`, 현재 범위, 검색어 상태를 즉시 보여주는 필터 요약 바를 추가했고 `필터 초기화` 버튼으로 검색과 범위를 한 번에 되돌릴 수 있게 했다. 인력 화면은 조직 코드 외에 이름/사번 검색을 지원하도록 확장했고, 검색 결과 수와 검색어 상태를 함께 표시한다. 공통 스타일도 요약 바 중심으로 다듬어 필터 상태가 더 잘 드러나도록 정리했다.

## TASK-153 운영 화면 필터 컨트롤 정렬 보정

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직/인력 관리 화면에서 텍스트 입력, 드롭다운, 필터 초기화 버튼의 높이와 정렬이 어색한 문제를 보정한다.
- 진행 현황:
  - 2026-04-08 사용자가 드롭다운과 텍스트 입력 크기가 달라 보이고, 인력 관리 페이지의 `필터 초기화` 버튼이 세로로 길게 늘어난다고 피드백했다.
  - 2026-04-08 공통 입력 높이, 버튼 높이, 액션 컨테이너 정렬을 통일하도록 스타일을 조정한다.
  - 2026-04-08 `select` 기본 렌더링 차이를 줄이기 위해 공통 드롭다운 스타일을 추가한다.
- 완료 기준:
  - 텍스트 입력과 드롭다운 높이가 시각적으로 일치해야 한다.
  - 필터 초기화 버튼이 과도하게 세로로 늘어나지 않아야 한다.
  - 공통 컨트롤 정렬이 더 자연스러워야 한다.
- 작업 결과: 완료. `control-input` 과 `control-button` 에 공통 최소 높이를 부여하고, `control-actions` 에 `align-self: end` 와 `align-items: center` 를 적용해 버튼 그룹이 세로로 늘어나지 않도록 보정했다. `select.control-input` 에도 공통 드롭다운 스타일을 추가해 텍스트 입력과 높이 및 시각 톤을 맞췄다.

## TASK-154 상단 카드 풀폭 재배치 및 필터 행 정렬 통일

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 페이지 설명 카드와 필터 영역 카드가 2분할로 좁아 보이는 문제를 해소하고, 조직/인력 화면의 필터 행 정렬을 같은 패턴으로 통일한다.
- 진행 현황:
  - 2026-04-08 사용자가 상단 설명 카드와 필터 카드가 2분할이라 좁아 보인다고 피드백했다.
  - 2026-04-08 또한 조직 관리에서는 `필터 초기화`가 줄바꿈으로 내려가고 인력 관리에서는 한 줄에 있어 페이지 이동 시 어색하다고 지적했다.
  - 2026-04-08 `org-command-deck`, `data-command-deck` 를 모두 단일 열 풀폭 배치로 변경한다.
  - 2026-04-08 조직/인력 필터 그리드를 모두 `입력 + 입력 + 버튼` 한 줄 구조로 통일한다.
- 완료 기준:
  - 설명 카드와 필터 카드가 각각 풀폭으로 배치되어야 한다.
  - 조직/인력 화면의 필터 초기화 버튼이 같은 줄 리듬으로 보여야 한다.
  - 페이지 간 이동 시 상단 영역의 구조감이 더 일관되어야 한다.
- 작업 결과: 완료. 조직/인력 계열 상단 `command deck` 을 모두 1열 풀폭 구조로 바꿔 설명 카드와 필터 카드가 각각 충분한 너비를 갖게 했다. 조직 관리의 필터 그리드는 `검색`, `범위`, `필터 초기화`가 한 줄에 정렬되도록 보정했고, 인력 관리도 같은 `입력 + 입력 + 버튼` 리듬으로 통일했다.

## TASK-155 조직 관리 본문 3열 균형 보정

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 관리 본문 3열 레이아웃의 정보 밀도와 시선 흐름을 더 안정적으로 만들기 위해 열 비율과 액션 레일 고정 방식을 보정한다.
- 진행 현황:
  - 2026-04-08 상단 정리 후에도 본문 3열에서 인스펙터 대비 액션 레일이 아래로 길게 흘러 사용성이 떨어질 수 있다고 판단했다.
  - 2026-04-08 디렉터리, 인스펙터, 액션 레일의 열 비율을 다시 조정한다.
  - 2026-04-08 데스크톱 구간에서는 액션 레일이 상단에 더 오래 붙어 있도록 `sticky` 정렬을 적용하고, 좁은 화면에서는 자동 해제한다.
- 완료 기준:
  - 조직 관리 본문 3열이 더 균형 있게 보여야 한다.
  - 인스펙터 중심 시선 흐름이 더 자연스러워야 한다.
  - 액션 레일이 데스크톱에서 과도하게 아래로 흘러가지 않아야 한다.
- 작업 결과: 완료. 조직 관리 `org-workbench` 열 비율을 디렉터리보다 인스펙터에 더 중심이 가도록 조정했고, 액션 레일 컬럼은 데스크톱에서 `sticky` 동작으로 상단에 고정되게 다듬었다. 2열 이하 반응형 구간에서는 자동으로 일반 흐름으로 돌아가도록 처리해 좁은 화면도 유지했다.

## TASK-156 조직 액션 패널 가독성 보강

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/styles.css`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 조직 관리 우측 액션 레일에서 `조직 액션` 과 `구성원 액션` 의 의미 차이가 더 빠르게 읽히도록 패널 톤과 필드 구획을 보강한다.
- 진행 현황:
  - 2026-04-08 사용자가 조직 화면 가독성을 더 높여보자고 요청했다.
  - 2026-04-08 액션 레일의 두 패널이 구조적으로 비슷해 보여 읽기 흐름이 섞이는 점을 개선 포인트로 잡았다.
  - 2026-04-08 각 패널에 짧은 설명 문장을 추가하고, 조직 액션과 구성원 액션에 서로 다른 배경 톤과 강조 바를 적용한다.
  - 2026-04-08 각 폼 필드를 카드형 구획으로 감싸 읽기 단위를 더 잘 끊어 보이게 정리한다.
- 완료 기준:
  - 조직 액션과 구성원 액션의 역할 차이가 더 분명해야 한다.
  - 패널 내부 필드가 더 또렷하게 구분되어야 한다.
  - 우측 액션 레일의 스캔 속도가 더 좋아져야 한다.
- 작업 결과: 완료. 조직 액션 패널은 구조 조정 성격이 더 드러나는 블루 톤, 구성원 액션 패널은 인력 이동/비활성 성격이 드러나는 그린 톤으로 차별화했다. 각 패널 상단에는 설명 문장을 추가했고, 개별 필드는 가벼운 카드형 구획으로 감싸 빠르게 읽을 수 있게 정리했다.

## TASK-157 조직/인력 관리 화면 사용자 목표 기준 재구성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-08
- 완료일: 2026-04-08
- 담당: Codex
- 영향 문서:
  - `src/ui_prototype/data_workforce.html`
  - `src/ui_prototype/data_organizations.html`
  - `src/ui_prototype/styles.css`
  - `src/ui_prototype/app.js`
  - `docs/overview/ui_ux_prototype.md`
  - `docs/operations/backlog/2026-04-08.md`
- 작업 내용: 인력 관리 화면을 조직 관리와 같은 수준의 운영 작업면으로 다듬고, 두 페이지 모두 “사용자가 여기서 어떤 정보를 얻고 어떤 작업을 하려는가” 기준으로 정보 구조와 카피를 재정리한다.
- 진행 현황:
  - 2026-04-08 사용자가 인력 관리 화면도 다듬고, 조직 관리 페이지 구성안은 사용자가 얻고 싶은 정보와 하고 싶은 작업 기준으로 검토해보자고 요청했다.
  - 2026-04-08 인력 관리 화면은 아직 목록 중심 성격이 강해, 선택 인력 중심 인스펙터와 영향 미리보기, 조직 맥락 요약이 부족하다고 판단했다.
  - 2026-04-08 조직 관리 화면은 기능은 충분하지만, 패널 이름과 안내 문구가 “이 조직에서 지금 뭘 확인하고 뭘 할 수 있는지”를 더 직접적으로 드러내면 좋겠다고 판단했다.
  - 2026-04-08 인력 관리 화면에 선택 인력 배너, 선택 행 하이라이트, 조직 맥락 요약, 액션 미리보기, 후속 점검 패널을 추가한다.
  - 2026-04-08 조직 관리 화면의 카피와 패널 제목을 사용자 질문 중심으로 조정하고, 프로토타입 문서에 조직/인력 페이지의 사용자 목표 기준을 정리한다.
- 완료 기준:
  - 인력 관리 화면에서 선택 인력 중심의 상세/조치 흐름이 더 분명해야 한다.
  - 조직 관리 화면에서 사용자가 확인할 정보와 수행할 작업이 패널 이름과 안내 문구만으로도 더 잘 드러나야 한다.
  - 프로토타입 문서에 조직/인력 관리 화면의 사용자 목표 기준이 반영되어야 한다.
- 작업 결과: 완료. 인력 관리 화면은 선택 인력 인스펙터, 조직 맥락 요약, 액션 미리보기, 후속 점검 패널을 추가해 단순 목록 화면이 아니라 운영 조치 작업면으로 재구성했다. 목록 행 클릭 시 선택 하이라이트와 폼 동기화가 함께 일어나고, 이동/비활성화 입력에 따라 영향 미리보기도 바로 갱신된다. 조직 관리 화면은 패널 제목과 설명 문구를 “이 조직을 이해하기”, “이 조직의 직속 구성원”, “변경 전에 확인할 영향”, “조직 구조 변경”, “구성원 배치 변경”처럼 사용자 질문 중심으로 조정했다. `ui_ux_prototype.md` 에도 두 페이지가 각각 어떤 정보를 보여주고 어떤 작업을 돕는지 기준을 추가했다.
