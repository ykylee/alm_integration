# 2026-04-07 작업 백로그

- 문서 목적: 2026-04-07에 수행한 작업의 계획, 진행 현황, 완료 내역을 기록한다.
- 범위: 2026-04-07 작업 이력
- 대상 독자: 프로젝트 참여자, 문서 작성자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-07
- 관련 문서: `docs/operations/work_backlog.md`

## TASK-064 초기 릴리스 엔터티 범위 구분

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일:
- 담당: Codex
- 영향 문서:
  - `docs/architecture/domain_entity_definition_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
  - `docs/operations/work_backlog.md`
- 작업 내용: 초기 릴리스 기준으로 필수 엔터티와 후순위 엔터티를 구분해 구현 우선순위를 정리하고, 아키텍처 계획 문서에도 반영한다.
- 진행 현황:
  - 2026-04-07 최근 작업으로 `work_item` 본체, 상태 모델, 계획 연결 규칙이 정리된 상태를 확인했다.
  - 2026-04-07 다음 단계로 엔터티 초안에서 초기 구현 범위와 후속 확장 범위를 구분하는 작업을 시작한다.
  - 2026-04-07 엔터티 문서에 `초기 릴리스 필수`, `선택 포함`, `후속 확장` 구간을 추가하고 구현 순서를 정리했다.
  - 2026-04-07 계획 문서에도 초기 릴리스 범위 판단 작업이 진행 현황으로 반영되도록 갱신했다.
- 완료 기준:
  - 초기 릴리스 필수 엔터티 목록이 정리되어야 한다.
  - 후속 확장 또는 선택 적용 엔터티 목록이 정리되어야 한다.
  - 계획 문서와 오늘자 백로그가 같은 판단을 반영해야 한다.
- 작업 결과: 완료. 초기 릴리스 기준으로 공통 업무 관리, 상태 모델, 계획 연결, 조직/권한 최소선에 해당하는 필수 엔터티를 구분했고, 연계/품질/운영 고도화 및 `AI` 보조 엔터티는 선택 포함 또는 후속 확장 범위로 정리했다.

## TASK-065 초기 릴리스 필수 엔터티 관계 초안 정리

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/domain_entity_definition_draft.md`
  - `docs/architecture/domain_model_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 초기 릴리스 필수 엔터티만 대상으로 논리 관계와 기본 `cardinality` 를 정리해 이후 `ERD` 초안의 기준을 마련한다.
- 진행 현황:
  - 2026-04-07 엔터티 우선순위 초안을 기준으로 필수 엔터티만 추려 관계 초안 정리 대상으로 확정했다.
  - 2026-04-07 `project`, `work_item`, 상태 모델, 계획 연결, 조직/권한, 감사 엔터티 간 기본 관계와 `1:N`/`N:1` 방향을 문서화했다.
  - 2026-04-07 구현 시 필요한 핵심 제약으로 단일 기본 프로세스 모델, 단일 부모 원칙, 계획 연결 규칙 검증, 상태 이력 일관성 유지 항목을 함께 정리했다.
- 완료 기준:
  - 필수 엔터티 관계 초안이 문서에 정리되어야 한다.
  - 이후 `ERD` 초안으로 이어질 수 있는 `cardinality` 기준이 있어야 한다.
  - 계획 문서와 오늘자 백로그가 같은 진행 상태를 반영해야 한다.
- 작업 결과: 완료. 초기 릴리스 필수 엔터티만 대상으로 논리 관계와 기본 `cardinality` 를 정리했고, 이후 물리 모델이나 `ERD` 초안은 이 관계 초안을 기준으로 내려갈 수 있게 됐다.

## TASK-066 초기 릴리스 Mermaid ERD 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/initial_release_erd_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 초기 릴리스 필수 엔터티 관계 초안을 바탕으로 `Mermaid` 기반 `ERD` 문서를 작성하고 아키텍처 위키에 연결한다.
- 진행 현황:
  - 2026-04-07 관계 초안만으로는 시각적 검토가 어려워 `Mermaid` 다이어그램이 필요하다고 판단했다.
  - 2026-04-07 `업무/프로세스/계획` 과 `조직/권한/감사` 영역을 분리한 `Mermaid erDiagram` 초안을 작성했다.
  - 2026-04-07 아키텍처 위키 인덱스와 계획 문서에도 새 `ERD` 문서를 연결했다.
- 완료 기준:
  - 초기 릴리스 필수 엔터티 기준 `Mermaid ERD` 문서가 생성되어야 한다.
  - 시각화 문서가 아키텍처 위키에서 탐색 가능해야 한다.
  - 오늘자 백로그와 계획 문서가 작업 상태를 반영해야 한다.
- 작업 결과: 완료. 초기 릴리스 필수 엔터티를 기준으로 `Mermaid` 기반 `ERD` 초안을 작성했고, 복잡도를 줄이기 위해 업무/프로세스/계획과 조직/권한/감사 영역으로 나눠 시각화했다.

## TASK-067 다형 관계 표현 기준 반영 및 ERD 보정

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/initial_release_erd_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `ERD` 검토 결과를 반영해 다형 참조와 간접 상속 관계의 표현 기준을 정리하고, `Mermaid` 다이어그램을 그 기준에 맞게 보정한다.
- 진행 현황:
  - 2026-04-07 `WORK_ITEM_PLAN_LINK`, `ROLE_ASSIGNMENT`, `AUDIT_LOG` 가 다형 참조인데도 `ERD` 에서는 직접 `FK`처럼 읽힐 수 있는 표현이 있음을 확인했다.
  - 2026-04-07 `PLANNING_SCHEME` 과 계획 단위의 관계도 직접 소속이 아니라 프로젝트 프로세스 설정을 통한 간접 상속으로 보는 편이 맞다고 판단했다.
  - 2026-04-07 `ERD` 에서 직접 `FK`로 오해될 수 있는 관계선을 제거하고, 대신 다형 참조/간접 상속 메모를 추가해 표현 기준을 문서화했다.
- 완료 기준:
  - 다형 참조와 간접 상속의 표현 기준이 문서에 정리되어야 한다.
  - `ERD` 가 엔터티 정의와 덜 충돌하는 방향으로 보정되어야 한다.
  - 계획 문서와 오늘자 백로그가 같은 판단을 반영해야 한다.
- 작업 결과: 완료. `Mermaid ERD` 에서 직접 `FK`처럼 보이던 일부 관계를 정리하고, 다형 참조와 간접 상속은 속성과 보조 설명으로 표현하는 기준을 반영해 엔터티 정의와의 충돌을 줄였다.

## TASK-068 논리 참조 규칙 문서 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/logical_reference_rules_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 직접 참조, 다형 참조, 간접 상속 관계를 구분하는 논리 참조 규칙 문서를 작성해 `ERD` 와 엔터티 정의 해석 기준을 고정한다.
- 진행 현황:
  - 2026-04-07 `ERD` 재검토 결과 `work_item_plan_link`, `role_assignment`, `audit_log`, `project.default_process_model_definition_id` 주변에서 참조 방식 해석 기준이 더 필요하다고 판단했다.
  - 2026-04-07 다형 참조, 직접 참조, 간접 상속을 구분하는 문서를 만들고 현재 초안 엔터티에 적용하는 기준을 정리했다.
  - 2026-04-07 아키텍처 위키와 계획 문서에도 새 논리 참조 규칙 문서를 연결했다.
- 완료 기준:
  - 논리 참조 규칙 문서가 생성되어야 한다.
  - `ERD` 와 엔터티 정의 해석 기준이 문서로 고정되어야 한다.
  - 아키텍처 위키와 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 직접 참조, 다형 참조, 간접 상속을 구분하는 논리 참조 규칙 문서를 작성했고, 이후 `ERD` 보정과 물리 모델 논의가 이 기준을 따르도록 정리했다.

## TASK-069 기본 프로세스 모델 기준 정리 및 코드값 표 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/domain_entity_definition_draft.md`
  - `docs/architecture/logical_reference_rules_draft.md`
  - `docs/architecture/initial_release_erd_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `project.default_process_model_definition_id` 를 제거 방향으로 정리하고, `plan_type` 과 `role_type` 코드값 표를 문서에 추가한다.
- 진행 현황:
  - 2026-04-07 `project` 와 `project_process_model` 의 기본 프로세스 모델 책임이 중복된 상태를 확인했다.
  - 2026-04-07 `project` 본체에서는 기본 프로세스 모델 참조를 제거하고, `project_process_model.is_primary` 를 유일한 기준으로 해석하도록 문서를 정리했다.
  - 2026-04-07 논리 참조 규칙 문서에 `work_item_plan_link.plan_type` 과 `role_assignment.role_type` 코드값 표를 추가해 다형 참조 해석 기준을 보강했다.
- 완료 기준:
  - `project` 본체에서 기본 프로세스 모델 중복 속성이 제거되어야 한다.
  - 기본 프로세스 모델 해석 기준이 문서로 고정되어야 한다.
  - `plan_type`, `role_type` 코드값 표가 추가되어야 한다.
- 작업 결과: 완료. `project` 의 기본 프로세스 모델 책임을 `project_process_model.is_primary` 로 일원화했고, `plan_type` 과 `role_type` 코드값 표를 추가해 이후 `ERD` 와 물리 모델 논의의 해석 기준을 더 명확히 했다.

## TASK-070 ERD 표현 일관성 추가 보정

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/initial_release_erd_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 최신 엔터티 정의와 논리 참조 규칙을 기준으로 `ERD` 에 남아 있는 표현 불일치를 추가 보정한다.
- 진행 현황:
  - 2026-04-07 `ERD` 에서 `project_process_model` 이 직접 참조하는 `view_scheme` 가 빠져 있어 엔터티 정의와 불일치함을 확인했다.
  - 2026-04-07 조직/권한/감사 다이어그램의 `audit_log.actor_id` 표기가 다형 참조인데도 물리 `FK`처럼 읽힐 수 있음을 확인했다.
  - 2026-04-07 `view_scheme` 엔터티와 직접 관계를 `ERD` 에 추가하고, `audit_log` 와 `role_policy` 관련 보조 설명을 최신 기준에 맞게 보정했다.
- 완료 기준:
  - `ERD` 가 현재 엔터티 정의와 논리 참조 규칙을 더 정확히 반영해야 한다.
  - 다형 참조와 직접 참조가 혼동되지 않도록 표기가 보정되어야 한다.
  - 계획 문서와 오늘자 백로그에 작업 내역이 반영되어야 한다.
- 작업 결과: 완료. `ERD` 에 `view_scheme` 관계를 복원하고 `project_process_model` 속성 표기를 보강했으며, `audit_log` 와 `role_policy` 주변 설명을 정리해 다이어그램 해석 기준을 더 일관되게 맞췄다.

## TASK-071 초기 릴리스 물리 모델 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/initial_release_physical_model_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 초기 릴리스 필수 엔터티를 기준으로 물리 모델 초안을 별도 문서로 정리하고, 기본키/유니크키/직접 참조/다형 참조/인덱스 방향을 문서화한다.
- 진행 현황:
  - 2026-04-07 `ERD`, 엔터티 정의, 논리 참조 규칙을 기준으로 물리 저장 모델 초안이 다음 단계로 필요하다고 판단했다.
  - 2026-04-07 새 문서에서 공통 물리 설계 원칙, 필수 테이블별 키와 제약, 다형 참조 처리 원칙, 초기 인덱스 방향을 정리했다.
  - 2026-04-07 아키텍처 위키 인덱스와 계획 문서에도 새 물리 모델 초안 문서를 연결했다.
- 완료 기준:
  - 초기 릴리스 물리 모델 초안 문서가 생성되어야 한다.
  - 필수 테이블별 기본키, 후보 유니크키, 직접 참조, 초기 인덱스 방향이 정리되어야 한다.
  - 계획 문서와 오늘자 백로그가 작업 상태를 반영해야 한다.
- 작업 결과: 완료. 초기 릴리스 필수 엔터티를 기준으로 물리 모델 초안을 별도 문서로 정리했고, 직접 참조와 다형 참조를 구분한 저장 기준, 기본 제약, 초기 인덱스 방향, 구현 우선순위를 정리했다.

## TASK-072 초기 릴리스 DDL 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/initial_release_ddl_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 초기 릴리스 물리 모델 초안을 바탕으로 핵심 테이블의 `DDL` 스타일 초안을 작성하고, 실제 구현 준비 수준의 키/제약/인덱스 예시를 문서화한다.
- 진행 현황:
  - 2026-04-07 물리 모델 초안은 충분히 정리됐지만 실제 테이블 정의 감각이 부족해 다음 단계 문서가 필요하다고 판단했다.
  - 2026-04-07 프로젝트, 업무 항목, 프로세스/상태 모델, 계획 모델, 권한/감사 축을 중심으로 `create table` 수준의 초안을 작성했다.
  - 2026-04-07 다형 참조는 외래키로 닫지 않고 `CHECK`와 보조 인덱스로 표현하는 방향을 `DDL` 문서에도 일관되게 반영했다.
- 완료 기준:
  - 핵심 테이블에 대한 `DDL` 스타일 초안 문서가 생성되어야 한다.
  - 기본키, 외래키, 유니크 제약, `CHECK`, 인덱스 예시가 포함되어야 한다.
  - 아키텍처 위키와 계획 문서, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 초기 릴리스 핵심 축에 대한 `DDL` 스타일 초안을 작성했고, 실제 구현 단계로 이어질 수 있도록 키/제약/인덱스 예시와 다형 참조 처리 방식을 구체화했다.

## TASK-073 다형 참조 무결성 검증 문서 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/polymorphic_reference_validation_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `work_item_plan_link`, `role_assignment`, `audit_log` 를 기준으로 다형 참조 무결성 검증 책임과 처리 방식을 별도 문서로 정리한다.
- 진행 현황:
  - 2026-04-07 `DDL` 초안까지 내려오면서 다형 참조는 외래키로 닫지 않는 대신 어떤 계층이 무엇을 검증하는지 기준이 더 필요하다고 판단했다.
  - 2026-04-07 새 문서에서 저장 계층, 응용 서비스, 운영 보완 계층의 검증 책임을 분리하고 대상별 검증 흐름을 정리했다.
  - 2026-04-07 동기 오류와 비동기 보완을 구분하고, 정합성 점검 배치에서 볼 운영 항목도 함께 정리했다.
- 완료 기준:
  - 다형 참조 무결성 검증 문서가 생성되어야 한다.
  - 대상별 검증 흐름과 오류 처리 방식이 정리되어야 한다.
  - 아키텍처 위키와 계획 문서, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 다형 참조 무결성 검증 책임을 저장 계층, 서비스 계층, 운영 계층으로 분리해 정리했고, 초기 릴리스에서 실제로 적용할 검증 흐름과 운영 보완 방안을 문서화했다.

## TASK-074 참조 정합성 점검 배치 및 오류 큐 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/reference_integrity_batch_and_error_queue_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 다형 참조 운영 보완 구조를 위해 정합성 점검 배치, 오류 큐, 재처리 흐름과 최소 엔터티 초안을 문서화한다.
- 진행 현황:
  - 2026-04-07 다형 참조 무결성 검증 문서 기준으로 저장 시점 검증만으로 부족한 운영 보완 구조가 필요하다고 판단했다.
  - 2026-04-07 정합성 점검 배치, 오류 큐, 재처리 큐, 운영자 조치 이력에 해당하는 최소 엔터티 초안을 새 문서로 정리했다.
  - 2026-04-07 오류 유형, 심각도, 상태 모델, 운영 지표, 초기 릴리스 권장 운영안까지 함께 정리했다.
- 완료 기준:
  - 정합성 점검 배치와 오류 큐 초안 문서가 생성되어야 한다.
  - 최소 엔터티, 오류 유형, 재처리 흐름이 정리되어야 한다.
  - 아키텍처 위키와 계획 문서, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 다형 참조 운영 보완을 위한 정합성 점검 배치와 오류 큐 구조를 문서화했고, 재처리 흐름, 운영 지표, 최소 엔터티 초안을 정리해 후속 구현과 운영 설계의 기반을 마련했다.

## TASK-075 참조 정합성 운영 DDL 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/reference_integrity_operations_ddl_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 정합성 점검 배치와 오류 큐 운영 모델을 실제 테이블 구조 감각으로 검토할 수 있도록 `DDL` 스타일 초안을 작성한다.
- 진행 현황:
  - 2026-04-07 운영 보완 구조 문서만으로는 실제 구현 수준의 키/제약/인덱스 검토가 어렵다고 판단했다.
  - 2026-04-07 `reference_integrity_check_job`, `reference_integrity_check_run`, `reference_integrity_issue`, `reference_integrity_issue_action`, `reference_retry_queue` 테이블 초안을 작성했다.
  - 2026-04-07 상태 코드 `CHECK`, 복합 인덱스, 후속 partial index 검토 포인트까지 함께 정리했다.
- 완료 기준:
  - 참조 정합성 운영 `DDL` 초안 문서가 생성되어야 한다.
  - 배치/오류 큐 핵심 테이블의 키, 제약, 인덱스 예시가 포함되어야 한다.
  - 아키텍처 위키와 계획 문서, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 참조 정합성 점검 배치와 오류 큐 운영 모델을 `DDL` 스타일로 구체화했고, 이후 운영 구현에서 필요한 상태 제약, 조회 인덱스, 후속 고급 제약 검토 포인트까지 정리했다.

## TASK-076 참조 정합성 대시보드 조회 모델 초안 작성

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/reference_integrity_dashboard_query_model_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 운영 대시보드에서 사용할 참조 정합성 집계/목록 조회 모델과 위젯 구성을 문서화한다.
- 진행 현황:
  - 2026-04-07 운영 보완 `DDL` 초안까지 내려오면서, 실제 운영 화면이 요구하는 집계와 목록을 별도로 정의할 필요가 있다고 판단했다.
  - 2026-04-07 요약 카드, 분포 차트, 추세 차트, 운영 리스트 기준으로 대시보드 위젯 구성을 정리했다.
  - 2026-04-07 `summary`, `distribution`, `trend`, `open_issue_list`, `retry_queue_monitor`, `check_run_monitor` 조회 모델 초안을 추가했다.
- 완료 기준:
  - 대시보드 조회 모델 초안 문서가 생성되어야 한다.
  - 운영 위젯, 필터, 집계 단위, 조회 모델이 정리되어야 한다.
  - 아키텍처 위키와 계획 문서, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 참조 정합성 운영 대시보드에 필요한 집계와 목록 조회 모델 초안을 문서화했고, 위젯 구성과 갱신 주기까지 정리해 화면 설계와 조회 최적화의 기준을 마련했다.

## TASK-077 참조 정합성 대시보드 UI 및 접근 제어 초안 작성

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/reference_integrity_dashboard_ui_and_access_draft.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 참조 정합성 운영 대시보드의 화면 구조, 역할별 기본 랜딩, 메뉴/탭, 액션 권한과 접근 범위를 문서화한다.
- 진행 현황:
  - 2026-04-07 조회 모델 초안이 정리되면서, 이를 어떤 역할이 어떤 화면에서 어떻게 보는지까지 연결할 필요가 있다고 판단했다.
  - 2026-04-07 시스템 관리자, 조직 운영 담당자, 프로젝트 운영 담당자, 운영 관리자 기준으로 접근 범위와 기본 필터를 구분했다.
  - 2026-04-07 홈, 오류 목록, 배치 실행, 재처리 큐, 오류 상세 화면 구조와 역할별 허용 액션을 문서화했다.
- 완료 기준:
  - 대시보드 UI 및 접근 제어 초안 문서가 생성되어야 한다.
  - 역할별 접근 범위, 기본 랜딩, 허용 액션이 정리되어야 한다.
  - 아키텍처 위키와 계획 문서, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 참조 정합성 운영 대시보드의 화면 구조와 역할별 접근 제어 기준을 문서화했고, 이후 운영 화면 설계와 권한 매트릭스 반영의 기준을 마련했다.

## TASK-078 참조 정합성 운영 대시보드 요구사항 동기화

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/requirements/system_srs.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 아키텍처 문서에서 정의한 참조 정합성 운영 대시보드의 화면/권한 경계를 `SRS`와 권한 매트릭스에 반영한다.
- 진행 현황:
  - 2026-04-07 운영 대시보드 조회 모델과 UI/접근 제어 초안이 정리되면서 요구사항 문서와의 정합화가 필요하다고 판단했다.
  - 2026-04-07 `FR-023D`, `FR-028A`, `FR-029E`, `FR-029F` 를 추가해 운영 대시보드 요약, 접근 통제, 기본 랜딩, 액션 권한 요구를 반영했다.
  - 2026-04-07 역할별 권한 매트릭스에 참조 정합성 대시보드 관련 조회/상세/재처리 권한을 추가하고 운영 요구사항에 모니터링/알림/조치 이력을 보강했다.
- 완료 기준:
  - 참조 정합성 운영 대시보드 관련 요구사항이 `SRS`에 반영되어야 한다.
  - 역할별 권한 매트릭스가 최신 아키텍처 방향과 일치해야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. 참조 정합성 운영 대시보드의 역할별 메뉴 노출, 기본 랜딩, 액션 권한, 모니터링/알림/조치 요구를 `SRS`와 권한 매트릭스에 반영해 아키텍처와 요구사항 문서의 정합성을 맞췄다.

## TASK-079 참조 정합성 운영 대시보드 UX 방향 동기화

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/overview/role_based_ux_direction.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 참조 정합성 운영 대시보드의 위치와 역할 경계를 역할 기반 UX 방향 문서에 반영한다.
- 진행 현황:
  - 2026-04-07 아키텍처와 `SRS`에는 운영 대시보드 기준이 반영됐지만, UX 방향 문서에는 아직 관리자 콘솔 하위 작업면으로 정리되지 않았음을 확인했다.
  - 2026-04-07 관리자 콘솔 안의 `reference-integrity` 서브플로우 구조와 시스템 관리자용 대표 화면 항목을 추가했다.
  - 2026-04-07 UX 리스크, 프로토타입 수정 방향, 다음 보완 우선순위에도 참조 정합성 운영 대시보드 항목을 반영했다.
- 완료 기준:
  - 운영 대시보드의 위치와 역할별 진입 원칙이 UX 방향 문서에 반영되어야 한다.
  - 관리자 콘솔과 일반 업무 화면의 경계가 UX 문서에서도 일관되게 드러나야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. 참조 정합성 운영 대시보드를 관리자 콘솔의 별도 운영 작업면으로 정의하고, 역할별 진입 차이와 액션 경계까지 UX 방향 문서에 반영해 아키텍처, 요구사항, UX 문서 간 정합성을 맞췄다.

## TASK-080 시스템 통합 DB 백엔드 설계 플랜 수립

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/README.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 시스템 통합 DB 구축용 백엔드 개발을 우선순위로 두고, 현재 아키텍처 문서에서 보완이 필요한 항목을 검토한 뒤 별도 설계 플랜 문서를 작성한다.
- 진행 현황:
  - 2026-04-07 현재 문서는 도메인 모델과 `DDL`은 비교적 정리됐지만, 백엔드 서비스 분해와 연계 수집 파이프라인 상세는 아직 부족하다고 판단했다.
  - 2026-04-07 백엔드 우선 관점에서 부족한 항목을 런타임 서비스 경계, 저장 파이프라인, 초기 API/배치, `DB` 운영 전략으로 나눠 정리했다.
  - 2026-04-07 별도 플랜 문서에 단계별 설계 순서와 우선순위, 바로 이어서 보완할 문서 후보를 추가했다.
- 완료 기준:
  - 백엔드 우선 설계 플랜 문서가 생성되어야 한다.
  - 현재 아키텍처의 보완 필요 항목과 단계별 설계 순서가 정리되어야 한다.
  - 아키텍처 위키와 계획 문서, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 시스템 통합 DB 구축용 백엔드 관점에서 현재 아키텍처의 부족한 부분을 검토하고, 런타임 구조, 수집/정규화/저장 흐름, 초기 API/배치, `DB` 운영 전략 중심의 설계 플랜을 별도 문서로 정리했다.

## TASK-081 시스템 통합 DB 백엔드 컴포넌트 및 유스케이스 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_backend_component_draft.md`
  - `docs/architecture/system_context_and_integration_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 시스템 통합 DB 구축용 백엔드의 런타임 컴포넌트 경계, 쓰기 책임, 핵심 유스케이스를 정리하고 `Mermaid` 다이어그램으로 시각화한다.
- 진행 현황:
  - 2026-04-07 백엔드 설계 플랜 기준으로 `STEP-BE-02` 에 해당하는 런타임 컴포넌트 상세화가 다음 우선순위라고 판단했다.
  - 2026-04-07 새 문서에 연계 어댑터, 오케스트레이션, 원시 적재, 표준화/식별자 매핑, 도메인 쓰기, 운영 통제, 운영 API 계층을 분리해 정리했다.
  - 2026-04-07 컴포넌트 다이어그램과 유스케이스 다이어그램을 `Mermaid` 로 추가하고, 상위 연계 문서와 백엔드 설계 플랜에도 연결했다.
- 완료 기준:
  - 백엔드 컴포넌트 초안 문서가 생성되어야 한다.
  - 핵심 유스케이스와 컴포넌트 책임이 정리되어야 한다.
  - `Mermaid` 기반 컴포넌트/유스케이스 다이어그램이 포함되어야 한다.
  - 아키텍처 위키, 설계 플랜, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 시스템 통합 DB 백엔드의 런타임 컴포넌트를 연계 어댑터, 오케스트레이션, 원시 적재, 표준화/식별자 매핑, 도메인 쓰기, 운영 통제, 운영 API 계층으로 분해해 정리했고, 핵심 유스케이스와 `Mermaid` 컴포넌트/유스케이스 다이어그램을 별도 문서로 작성해 이후 수집 파이프라인 시퀀스와 API 설계의 입력으로 연결했다.

## TASK-082 시스템 통합 DB 데이터 수집 시퀀스 및 충돌 방지 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_data_ingestion_sequence_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/system_context_and_integration_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `pulling` 수집과 `API push` 수신을 모두 지원하는 데이터 수집 시퀀스를 정리하고, 정합성과 충돌 방지 구조를 `Mermaid` 다이어그램과 함께 문서화한다.
- 진행 현황:
  - 2026-04-07 다음 설계 단계로 `STEP-BE-03` 인 수집/정규화/저장 시퀀스 상세화가 필요하다고 판단했다.
  - 2026-04-07 `pull` 과 `push` 모두를 공통 원시 적재 및 표준화 파이프라인으로 수렴시키는 구조를 정리했다.
  - 2026-04-07 멱등 수집 키, 최신성 판정, 단일 쓰기 경로, 엔터티 단위 직렬화, 참조 보류/재처리 규칙을 추가하고 `Mermaid` 시퀀스 다이어그램 두 개를 작성했다.
- 완료 기준:
  - 데이터 수집 시퀀스 초안 문서가 생성되어야 한다.
  - `pulling` 과 `API push` 경로가 모두 정리되어야 한다.
  - 데이터 정합성과 충돌 방지 구조가 문서에 포함되어야 한다.
  - 아키텍처 위키, 설계 플랜, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. `pulling` 과 `API push` 를 함께 수용하는 공통 적재 파이프라인을 정의하고, 멱등 키, 최신성 기준, 단일 쓰기 경로, 참조 보류와 재처리 원칙을 포함한 충돌 방지 구조를 `Mermaid` 시퀀스 다이어그램과 함께 문서화했다.

## TASK-083 시스템 통합 DB 백엔드 API 및 배치 계약 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_backend_api_and_batch_contract_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/system_context_and_integration_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 수집 수신 API, 동기화 운영 API, 정합성 운영 API, 기준정보 API, 내부 배치 실행 계약을 포함한 초기 백엔드 인터페이스 초안을 작성한다.
- 진행 현황:
  - 2026-04-07 데이터 수집 시퀀스 문서를 기준으로 외부 수신 API 와 운영 API 를 분리해야 한다고 판단했다.
  - 2026-04-07 `ingestion_api`, `sync_operation_api`, `integrity_operation_api`, `master_data_api`, `policy_admin_api` 와 내부 배치 명령 모델을 정리했다.
  - 2026-04-07 공통 헤더, 멱등 키, 실행 응답 규칙, 운영 감사 기준, 권장 구현 순서를 함께 문서화했다.
- 완료 기준:
  - 초기 API/배치 계약 초안 문서가 생성되어야 한다.
  - 외부 수신 API 와 운영 API, 배치 실행 인터페이스가 구분되어 정리되어야 한다.
  - 멱등 실행과 감사 기준이 포함되어야 한다.
  - 아키텍처 위키, 설계 플랜, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 초기 릴리스 기준의 수집 수신 API, 동기화 실행/조회 API, 정합성 운영 API, 기준정보 API, 정책 관리 API 와 내부 배치 명령 모델을 정리했고, 공통 헤더, 응답 규칙, 멱등 키, 감사 기준과 권장 구현 순서까지 포함한 계약 초안을 작성했다.

## TASK-084 시스템 통합 DB 마이그레이션 및 시드 데이터 운영 전략 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_db_migration_and_seed_strategy_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/system_context_and_integration_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 초기 릴리스 스키마 변경, 시드 데이터, 환경 승격, 파괴적 변경 대응, 롤백 기준을 포함한 `DB` 운영 전략 초안을 작성한다.
- 진행 현황:
  - 2026-04-07 `STEP-BE-05` 단계로 넘어가면서 `DDL` 초안만으로는 실제 운영 적용 전략이 부족하다고 판단했다.
  - 2026-04-07 `schema`, `seed`, `backfill` 단위 분리, 마이그레이션 히스토리, 환경 승격 절차, 파괴적 변경 회피 원칙을 정리했다.
  - 2026-04-07 초기 릴리스 권장 적용 순서와 검증 기준, 운영 가드레일까지 별도 문서로 추가했다.
- 완료 기준:
  - `DB` 마이그레이션 및 시드 데이터 운영 전략 문서가 생성되어야 한다.
  - 스키마 변경, 시드 반영, 환경 승격, 롤백 기준이 정리되어야 한다.
  - 아키텍처 위키, 설계 플랜, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 시스템 통합 DB 백엔드의 스키마 변경과 시드 데이터 운영 전략을 별도 문서로 정리했고, 마이그레이션 단위 분리, 환경 승격, 파괴적 변경 대응, 롤백 우선순위, 초기 적용 순서와 검증 기준까지 포함해 구현 착수 전 운영 기준을 마련했다.

## TASK-085 시스템 통합 DB 백엔드 문서 리뷰 및 초기 구현 체크리스트 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 백엔드 관련 아키텍처 문서를 전반 검토하고, 초기 구현 순서와 개발 착수 체크리스트를 별도 문서로 정리한다.
- 진행 현황:
  - 2026-04-07 백엔드 설계 플랜, 컴포넌트, 수집 시퀀스, API 계약, 마이그레이션 전략 문서를 다시 검토해 구현 가능 수준과 남은 보완 포인트를 정리했다.
  - 2026-04-07 저장 기초선, 원시 적재, 표준화/도메인 반영, 운영 통제, 운영 API 최소선 기준으로 초기 구현 단계를 재구성했다.
  - 2026-04-07 개발 착수 전 확인할 공통 준비, 저장 모델 준비, 수집/정합성 준비, API/운영 준비, 검증 준비 체크리스트를 별도 문서로 작성했다.
- 완료 기준:
  - 백엔드 문서 리뷰 결과가 정리되어야 한다.
  - 초기 구현 순서와 단계별 완료 기준이 정리되어야 한다.
  - 개발 착수 체크리스트 문서가 생성되어야 한다.
  - 설계 플랜, 아키텍처 위키, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 백엔드 관련 설계 문서를 전반 검토한 뒤 현재 구현 가능한 범위와 잔여 보완 포인트를 정리했고, 초기 구현 순서와 개발 착수 체크리스트를 별도 문서로 고정해 실제 백엔드 개발 시작 기준으로 사용할 수 있게 정리했다.

## TASK-086 외부 시스템 연결 정보 및 자격증명 관리 요구 반영

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/requirements/system_srs.md`
  - `docs/architecture/domain_entity_definition_draft.md`
  - `docs/architecture/integration_backend_component_draft.md`
  - `docs/architecture/integration_backend_api_and_batch_contract_draft.md`
  - `docs/architecture/integration_db_migration_and_seed_strategy_draft.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 관리자가 웹 또는 API 를 통해 외부 시스템 연결 URL, 계정 ID, 비밀번호/토큰 등을 등록·수정할 수 있고, 민감정보는 암호화 저장해야 한다는 요구를 관련 문서에 반영한다.
- 진행 현황:
  - 2026-04-07 기존 `FR-002`, `integration_endpoint`, API 계약 문서를 검토해 연결 정보 관리 요구가 부분적으로만 반영된 상태임을 확인했다.
  - 2026-04-07 `SRS` 에 연결 정보 등록/수정, 민감정보 비노출, 암호화 저장 요구를 추가했다.
  - 2026-04-07 엔터티 문서에 `integration_credential` 을 추가하고, 관리자 API 와 마이그레이션/체크리스트 문서에 암호화 저장 및 시드 금지 원칙을 반영했다.
- 완료 기준:
  - 연결 정보와 자격증명 입력/수정 요구가 요구사항 문서에 반영되어야 한다.
  - 민감정보 암호화 저장 원칙이 아키텍처 문서에 반영되어야 한다.
  - 관리자 API 경로와 구현 체크리스트가 갱신되어야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. 외부 시스템 연결 정보와 자격증명을 관리자 웹/API 로 등록·수정할 수 있어야 한다는 요구와 민감정보 암호화 저장 원칙을 요구사항, 엔터티, 백엔드 컴포넌트, API 계약, 마이그레이션 전략, 구현 체크리스트 문서에 반영했다.

## TASK-087 시스템 통합 DB 인증 및 서명 정책 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_auth_and_signing_policy_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 외부 수신 `ingestion_api`, 관리자 운영 API, 외부 시스템 자격증명 관리에 적용할 인증 및 서명 정책 기준을 별도 문서로 정리한다.
- 진행 현황:
  - 2026-04-07 백엔드 설계 플랜의 잔여 보완 포인트 중 인증/서명 정책이 우선이라고 판단했다.
  - 2026-04-07 `ingestion_api` 에 대한 시스템 간 인증, `HMAC` 기반 서명 검증, 관리자 API 인증 분리 원칙을 정리했다.
  - 2026-04-07 자격증명 암호화 키 버전, 마스킹, 로그 비노출, 실패 처리 기준까지 별도 문서로 추가했다.
- 완료 기준:
  - 인증 및 서명 정책 초안 문서가 생성되어야 한다.
  - 외부 수신 API 와 관리자 API 의 인증 기준이 구분되어 정리되어야 한다.
  - 자격증명 암호화/마스킹 정책이 포함되어야 한다.
  - 아키텍처 위키, 설계 플랜, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 외부 수신 `ingestion_api` 와 관리자 운영 API 의 인증 기준을 분리하고, `HMAC` 서명 검증, 자격증명 암호화 저장, 키 버전 관리, 로그 마스킹, 실패 처리 기준을 포함한 인증 및 서명 정책 초안을 별도 문서로 작성했다.

## TASK-088 연계 및 수집 엔터티 물리 모델/DDL 반영

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/initial_release_physical_model_draft.md`
  - `docs/architecture/initial_release_ddl_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `integration_system`, `integration_endpoint`, `integration_credential`, `integration_job`, `integration_run`, `raw_ingestion_event` 등 연계/수집 엔터티를 물리 모델과 `DDL` 초안에 반영한다.
- 진행 현황:
  - 2026-04-07 연결 정보와 자격증명 관리 요구가 반영되면서 연계/수집 축도 실제 구현 입력 수준의 물리 모델이 필요하다고 판단했다.
  - 2026-04-07 물리 모델 문서에 연계 시스템, 엔드포인트, 암호화 자격증명, 실행 이력, 원시 적재, 표준화 참조, 동기화 오류 저장 구조를 추가했다.
  - 2026-04-07 `DDL` 문서에 동일 엔터티의 `create table`, 유니크 제약, 직접 참조, 멱등 키, 인덱스 초안을 추가했다.
- 완료 기준:
  - 연계/수집 엔터티가 물리 모델 문서에 추가되어야 한다.
  - 동일 엔터티가 `DDL` 초안에 반영되어야 한다.
  - 자격증명 암호화 저장과 원시 적재 멱등 키가 구조에 드러나야 한다.
  - 계획 문서와 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 연계 시스템, 엔드포인트, 암호화 자격증명, 동기화 실행, 원시 적재, 표준화 참조, 동기화 오류 구조를 물리 모델과 `DDL` 초안에 반영해 실제 백엔드 구현에서 바로 사용할 수 있는 수준으로 보강했다.

## TASK-089 시스템 통합 DB 동기화 실행 상태 전이 초안 작성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_run_state_transition_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `sync_run`/`integration_run` 관점의 실행 상태 집합, 상태 전이 규칙, 부분 완료, 재시도, 취소 처리 기준을 별도 문서로 정리한다.
- 진행 현황:
  - 2026-04-07 API 계약과 수집 시퀀스 문서에는 실행 상태가 흩어져 있고, 부분 완료와 재시도 기준이 명확하지 않다고 판단했다.
  - 2026-04-07 `queued`, `running`, `partially_completed`, `completed`, `failed`, `cancelled` 상태 집합과 정상/금지 전이를 정리했다.
  - 2026-04-07 `ignored` 는 실행 상태가 아니라 요청 처리 결과로 해석하는 기준과 `integration_run` 컬럼 보강 후보까지 별도 문서로 추가했다.
- 완료 기준:
  - 동기화 실행 상태 전이 초안 문서가 생성되어야 한다.
  - 상태 집합과 전이 규칙, 금지 전이가 정리되어야 한다.
  - 부분 완료, 재시도, 취소 기준이 문서에 포함되어야 한다.
  - 아키텍처 위키, 설계 플랜, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. `sync_run`/`integration_run` 상태 집합과 전이 규칙, 재시도/취소/부분 완료 기준을 별도 문서로 정리했고, `ignored` 를 실행 상태가 아닌 요청 처리 결과로 해석하는 기준과 `integration_run` 컬럼 보강 후보를 함께 정리했다.

## TASK-090 동기화 실행 상태 모델의 물리 모델/DDL 반영

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/initial_release_physical_model_draft.md`
  - `docs/architecture/initial_release_ddl_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 상태 전이 초안에서 제안한 `integration_run` 보강 컬럼과 상태 제약을 물리 모델 및 `DDL` 초안에 반영한다.
- 진행 현황:
  - 2026-04-07 상태 전이 문서에서 `queued_at`, 상태 사유, 재시도 계보, `pending_count` 컬럼 보강이 필요하다고 정리했다.
  - 2026-04-07 물리 모델 문서에 동일 컬럼과 `retry_of_run_id` 참조, 추가 인덱스를 반영했다.
  - 2026-04-07 `DDL` 문서에 상태 `CHECK`, 재시도 자기참조 `FK`, 보강 컬럼 정의를 추가했다.
- 완료 기준:
  - `integration_run` 보강 컬럼이 물리 모델 문서에 반영되어야 한다.
  - 동일 컬럼과 상태 제약이 `DDL` 초안에 반영되어야 한다.
  - 계획 문서와 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. 상태 전이 초안의 보강 항목을 기준으로 `integration_run` 에 `queued_at`, 상태 사유, 재시도 원본 실행 참조, `pending_count` 를 추가했고, 물리 모델과 `DDL` 초안에 상태 제약과 인덱스까지 반영해 저장 구조와 상태 모델을 맞췄다.

## TASK-091 워커 슬롯 및 실행 취소/롤백 정책 구체화

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_worker_slot_and_cancellation_policy_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/architecture/README.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 구현 전 안전장치 구체화를 위해 워커 슬롯 상한, 엔터티 직렬화, 실행 취소 처리, 롤백 경계를 별도 정책 문서로 정리하고 관련 설계 플랜과 체크리스트에 반영한다.
- 진행 현황:
  - 2026-04-07 상태 전이 문서와 수집 시퀀스 문서를 기준으로 워커 동시성 제어와 취소 시 데이터 보존 기준이 아직 별도 정책으로 고정되지 않았음을 확인했다.
  - 2026-04-07 `pull`/`push`/재처리/정합성 점검 배치의 우선순위, `integration_endpoint` 단위 실행 상한, 엔터티 직렬화 키, 협조적 취소와 청크 단위 롤백 원칙을 새 문서로 정리했다.
  - 2026-04-07 백엔드 설계 플랜, 구현 체크리스트, 아키텍처 위키 인덱스, 상위 아키텍처 계획 문서에 새 정책 문서를 연결하고 남은 보완 포인트를 갱신했다.
- 완료 기준:
  - 워커 슬롯 및 취소/롤백 정책 문서가 생성되어야 한다.
  - 동시 실행 상한, 우선순위, 직렬화 키, 취소 단계별 처리 기준이 정리되어야 한다.
  - 개발 착수 체크리스트와 설계 플랜이 같은 기준을 반영해야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. 워커 슬롯 배분, 직렬화 키 기준, 협조적 취소, 현재 트랜잭션 또는 청크 단위 롤백, 이미 커밋된 원시 적재/감사/도메인 반영 비삭제 원칙을 별도 정책 문서로 정리했고, 관련 설계 플랜과 개발 착수 체크리스트를 같은 기준으로 갱신했다.

## TASK-092 취소 요청 감사 컬럼의 물리 모델 및 DDL 반영

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/initial_release_physical_model_draft.md`
  - `docs/architecture/initial_release_ddl_draft.md`
  - `docs/architecture/integration_run_state_transition_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 워커 슬롯 및 취소/롤백 정책 문서에서 정의한 취소 요청 감사 정보를 `integration_run` 물리 모델과 `DDL` 초안에 반영하고, 관련 설계 문서의 잔여 과제를 갱신한다.
- 진행 현황:
  - 2026-04-07 `integration_run` 이 `queued` 상태를 가지는데도 `started_at` 이 필수처럼 정리돼 있어 상태 모델과 저장 구조가 완전히 일치하지 않음을 확인했다.
  - 2026-04-07 물리 모델과 `DDL` 초안에 `cancel_requested_at`, `cancel_requested_by`, `cancel_reason_code` 를 추가하고, `started_at` 을 nullable 로 조정했다.
  - 2026-04-07 상태 전이 문서, 백엔드 설계 플랜, 구현 체크리스트에서 취소 감사 컬럼 반영 상태와 남은 보완 포인트를 갱신했다.
- 완료 기준:
  - `integration_run` 취소 요청 감사 컬럼이 물리 모델 문서에 반영되어야 한다.
  - 동일 컬럼과 nullable `started_at` 이 `DDL` 초안에 반영되어야 한다.
  - 상태 전이 문서, 설계 플랜, 구현 체크리스트가 같은 기준을 반영해야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. `integration_run` 에 취소 요청 시각, 요청 주체, 사유 코드를 저장하는 감사 컬럼을 추가했고, `queued` 상태와 맞도록 `started_at` 을 nullable 로 조정했다. 관련 상태 전이 문서와 설계 플랜, 구현 체크리스트도 같은 기준으로 갱신했다.

## TASK-093 동기화 운영 API 계약의 취소 처리 반영

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_backend_api_and_batch_contract_draft.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `integration_run` 취소 감사 컬럼과 협조적 취소 정책을 기준으로 `sync-runs` 운영 API 계약을 갱신하고, 구현 체크리스트와 설계 계획 문서에 반영한다.
- 진행 현황:
  - 2026-04-07 `sync-runs` 관련 API 계약이 실행/조회/재시도까지만 있고 취소 요청 등록 API 와 취소 관련 응답 필드는 빠져 있음을 확인했다.
  - 2026-04-07 `GET /api/v1/admin/sync-runs`, `GET /api/v1/admin/sync-runs/{run_id}` 응답에 취소 요청 필드를 추가하고, `POST /api/v1/admin/sync-runs/{run_id}/cancel` 계약을 새로 정리했다.
  - 2026-04-07 구현 체크리스트와 설계 계획 문서에 취소 API 호출 권한, 종료 상태 예외 처리, API 계약 반영 상태를 함께 갱신했다.
- 완료 기준:
  - `sync-runs` 목록/상세 API 에 취소 요청 관련 응답 필드가 반영되어야 한다.
  - `sync-runs/{run_id}/cancel` API 계약이 문서에 추가되어야 한다.
  - 구현 체크리스트와 설계 계획 문서가 같은 기준을 반영해야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. 동기화 운영 API 계약에 취소 요청 등록 API, 취소 요청 시각/사유 응답 필드, 협조적 취소 원칙을 반영했고, 구현 체크리스트와 설계 계획 문서도 같은 기준으로 갱신했다.

## TASK-094 동기화 운영 API 상태별 응답 규칙 보강

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_backend_api_and_batch_contract_draft.md`
  - `docs/architecture/integration_run_state_transition_draft.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `sync-runs` 실행, 재시도, 취소 API 의 상태별 `HTTP` 응답 규칙과 운영 오류 코드를 구체화하고, 상태 전이 문서 및 체크리스트에 반영한다.
- 진행 현황:
  - 2026-04-07 기존 API 계약에는 엔드포인트와 필드만 있고 `retry`/`cancel` 시 종료 상태 예외와 멱등 재호출 시 응답 규칙이 부족하다고 판단했다.
  - 2026-04-07 `POST /api/v1/admin/sync-runs`, `POST /api/v1/admin/sync-runs/{run_id}/retry`, `POST /api/v1/admin/sync-runs/{run_id}/cancel` 에 `202/200/409/404` 기준과 오류 코드 예시를 추가했다.
  - 2026-04-07 상태 전이 문서에 운영 API 보조 응답 규칙을 반영하고, 구현 체크리스트에 `HTTP`/오류 코드 합의 항목을 추가했다.
- 완료 기준:
  - `sync-runs` 실행/재시도/취소 API 에 상태별 응답 규칙이 정리되어야 한다.
  - 운영 오류 코드가 문서에 추가되어야 한다.
  - 상태 전이 문서와 구현 체크리스트가 같은 기준을 반영해야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. 동기화 운영 API 계약에 상태별 `HTTP` 응답 규칙과 운영 오류 코드(`RUN_ALREADY_FINISHED`, `RUN_NOT_CANCELLABLE`, `RUN_NOT_RETRIABLE` 등)를 추가했고, 상태 전이 문서와 구현 체크리스트에도 같은 기준을 반영했다.

## TASK-095 백엔드 초기 스캐폴딩 생성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `pyproject.toml`
  - `Makefile`
  - `README.md`
  - `AGENTS.md`
  - `src/alm_integration_backend/...`
  - `tests/test_health.py`
  - `migrations/README.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `FastAPI + SQLAlchemy + Alembic` 기준으로 최소 실행 가능한 백엔드 골격, 테스트 진입점, 마이그레이션 디렉터리, `sync-runs` 운영 API stub 를 생성한다.
- 진행 현황:
  - 2026-04-07 저장소에 백엔드 스택과 실행 진입점이 없어 구현 착수를 위해 최소 스캐폴딩이 필요하다고 판단했다.
  - 2026-04-07 `pyproject.toml`, `Makefile`, `src/alm_integration_backend/`, `tests/`, `migrations/` 골격과 헬스체크, `sync-runs` stub API 를 추가했다.
  - 2026-04-07 `README.md`, `AGENTS.md`, 아키텍처 계획 문서에 새 실행 명령과 스캐폴딩 반영 상태를 함께 기록했다.
  - 2026-04-07 `python3 -m compileall src` 는 성공했지만, 로컬 환경에 `pytest` 와 `python3-venv` 가 없어 전체 테스트 실행은 확인하지 못했다.
- 완료 기준:
  - 로컬에서 실행 가능한 백엔드 프로젝트 골격이 생성되어야 한다.
  - 최소 테스트가 추가되어야 한다.
  - 실행 명령이 `README.md` 와 `AGENTS.md` 에 기록되어야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. `FastAPI + SQLAlchemy + Alembic` 기준의 초기 백엔드 스캐폴딩을 추가했고, 헬스체크와 `sync-runs` stub API, 테스트 진입점, 마이그레이션 디렉터리, 기본 실행 명령까지 함께 정리했다. 문법 컴파일은 확인했지만, `pytest` 및 `python3-venv` 부재로 전체 테스트 실행은 후속 환경 준비 후 다시 확인해야 한다.

## TASK-096 기술 스택 1차 검토안 반영

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `README.md`
  - `AGENTS.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 프론트엔드 `React`, 백엔드 `Rust` 를 1차 검토안으로 문서에 반영하고, 현재 Python 스캐폴딩의 위치를 임시 구조 검증 골격으로 재정의한다.
- 진행 현황:
  - 2026-04-07 현재 문서와 `README`, `AGENTS.md` 에는 Python 스캐폴딩이 곧 구현 기준처럼 읽힐 수 있는 표현이 남아 있음을 확인했다.
  - 2026-04-07 프론트엔드 `React`, 백엔드 `Rust` 를 1차 검토안으로 명시하고, Python 스캐폴딩은 임시 구조 검증용이라는 설명을 추가했다.
  - 2026-04-07 백엔드 구현 체크리스트에 `Rust` 프레임워크/ORM/마이그레이션 도구 재매핑 항목을 추가했다.
- 완료 기준:
  - 주요 안내 문서에 `React`/`Rust` 1차 검토안이 반영되어야 한다.
  - 현재 Python 스캐폴딩이 임시 골격이라는 점이 명시되어야 한다.
  - 구현 체크리스트에 `Rust` 재매핑 준비 항목이 추가되어야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. 프론트엔드 `React`, 백엔드 `Rust` 를 1차 검토안으로 문서에 반영했고, 현재 Python 기반 스캐폴딩은 최종 기술 스택이 아닌 임시 구조 검증용 골격으로 재정의했다. 구현 체크리스트에는 `Rust` 프레임워크, ORM, 마이그레이션 도구 재매핑 항목을 추가했다.

## TASK-097 Rust `axum + sqlx` 채택 계획 수립

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/rust_axum_sqlx_adoption_plan.md`
  - `docs/architecture/README.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `Rust` 백엔드 후보를 `axum + sqlx` 로 좁히고, 선택 배경, 대안 비교, 전환 단계, 임시 Python 골격 처리 원칙을 별도 계획 문서로 정리한다.
- 진행 현황:
  - 2026-04-07 백엔드 1차 검토안이 `Rust` 로 좁혀진 상태에서 웹 프레임워크와 DB 접근 계층 조합을 더 구체화할 필요가 있다고 판단했다.
  - 2026-04-07 `axum + sqlx` 를 권장 조합으로 두고, `actix-web + sqlx`, `axum + SeaORM` 대비 장단점을 정리한 별도 계획 문서를 작성했다.
  - 2026-04-07 구현 체크리스트에는 `axum`, `sqlx`, `sqlx migrate` 를 1차 기준으로 반영하고, 남은 의사결정 항목으로 프로젝트 위치와 `sqlx` offline mode 여부를 추가했다.
- 완료 기준:
  - `axum + sqlx` 채택 계획 문서가 생성되어야 한다.
  - 상위 설계 플랜과 구현 체크리스트가 같은 기준을 반영해야 한다.
  - 아키텍처 위키 인덱스와 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. `Rust` 백엔드 1차 권장 조합을 `axum + sqlx` 로 정리했고, 선택 배경, 대체 후보 비교, 단계별 전환 계획, 임시 Python 골격 처리 원칙, 남은 선결정 항목을 별도 문서로 고정했다.

## TASK-098 `backend/` 하위 Rust 골격 생성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/Cargo.toml`
  - `backend/src/...`
  - `backend/tests/http.rs`
  - `backend/migrations/README.md`
  - `README.md`
  - `AGENTS.md`
  - `docs/architecture/rust_axum_sqlx_adoption_plan.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: Rust 1차 구현 경로를 `backend/` 하위로 확정하고, `axum + sqlx` 기준의 최소 실행 가능한 백엔드 골격과 테스트 진입점을 생성한다.
- 진행 현황:
  - 2026-04-07 Rust 프로젝트 위치를 저장소 루트가 아니라 `backend/` 하위로 두는 방향으로 결정했다.
  - 2026-04-07 `cargo init --bin backend` 로 기본 골격을 만든 뒤, `axum`, `tokio`, `sqlx`, `tracing` 의존성과 헬스체크, `sync-runs` stub API, 인메모리 상태 저장, 테스트 파일을 추가했다.
  - 2026-04-07 README, AGENTS, 아키텍처 계획 문서에 Rust 실행 명령과 `backend/` 하위 경로 결정을 반영했다.
- 완료 기준:
  - `backend/` 하위 Rust 프로젝트 골격이 생성되어야 한다.
  - `axum + sqlx` 기준 최소 실행 코드와 테스트 파일이 추가되어야 한다.
  - 문서에 `backend/` 경로 결정과 실행 명령이 반영되어야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. `backend/` 하위에 Rust 프로젝트를 생성하고 `axum + sqlx` 최소 골격, 헬스체크, `sync-runs` 운영 API stub, 테스트 진입점, Rust 전용 마이그레이션 디렉터리를 추가했다. Python 골격은 계속 임시 검증용으로 두고, Rust 골격이 1차 구현 기준이 되도록 문서와 명령을 갱신했다.

## TASK-099 TDD 작업 방식 지침 반영

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `AGENTS.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 구현 작업 시 단위 테스트를 먼저 만들고 실패를 확인한 뒤 구현하는 `TDD` 방식을 저장소 작업 지침과 백엔드 구현 체크리스트에 반영한다.
- 진행 현황:
  - 2026-04-07 구현 방식 지침에는 자동화 테스트 필요성은 있었지만, 테스트 선작성 원칙은 명시되어 있지 않음을 확인했다.
  - 2026-04-07 `AGENTS.md` 의 테스트 지침에 `TDD` 원칙과 구현 순서를 추가했다.
  - 2026-04-07 백엔드 구현 체크리스트에도 실패 테스트 목록을 먼저 정의하도록 항목을 추가했다.
- 완료 기준:
  - 저장소 작업 지침에 `TDD` 원칙이 반영되어야 한다.
  - 백엔드 구현 체크리스트에 테스트 선작성 기준이 반영되어야 한다.
  - 오늘자 백로그에 작업 상태가 기록되어야 한다.
- 작업 결과: 완료. 구현 작업은 기본적으로 `TDD` 를 따르도록 저장소 작업 지침에 반영했고, 백엔드 구현 체크리스트에도 실패 테스트를 먼저 정의하는 항목을 추가했다.

## TASK-100 Rust 서비스 단위 테스트 보강

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/services/sync_runs.rs`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 현재 Rust 골격에서 HTTP 테스트만으로 부족한 서비스 핵심 로직을 검토하고, `sync_runs` 상태 전이/재시도/취소 감사 필드 기록에 대한 단위 테스트를 보강한다.
- 진행 현황:
  - 2026-04-07 현재 Rust 골격은 `health` 와 `sync-runs` HTTP 경로만 검증하고, 서비스 내부 상태 전이 로직에 대한 단위 테스트는 없음을 확인했다.
  - 2026-04-07 `SyncRunStore` 의 기본 생성, 재시도 가능 여부, 재시도 생성, 취소 감사 필드 기록, 종료 상태 취소 처리에 대한 단위 테스트를 추가했다.
  - 2026-04-07 구현 체크리스트에도 서비스 핵심 로직 단위 테스트 선반영 항목을 추가했다.
- 완료 기준:
  - 서비스 레벨 핵심 로직에 대한 단위 테스트가 추가되어야 한다.
  - 상태 전이와 취소/재시도 규칙이 테스트로 검증되어야 한다.
  - 오늘자 백로그와 구현 체크리스트가 같은 기준을 반영해야 한다.
- 작업 결과: 완료. `sync_runs` 서비스에 생성, 재시도, 취소 로직 단위 테스트를 추가해 HTTP 테스트에 비해 비어 있던 핵심 상태 전이 규칙 검증을 보강했다.

## TASK-101 Rust 초기 마이그레이션 골격 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일:
- 담당: Codex
- 영향 문서:
  - `backend/src/db/...`
  - `backend/tests/migrations.rs`
  - `backend/migrations/...`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `sqlx migrate` 기준의 Rust 마이그레이션 골격을 초기화하고, `integration_job`, `integration_run`, `raw_ingestion_event` 1차 테이블 정의를 migration SQL 로 추가한다.
- 진행 현황:
  - 2026-04-07 `TDD` 원칙에 따라 먼저 migration 파일 존재와 핵심 테이블 정의를 검증하는 테스트를 추가했다.
- 완료 기준:
  - Rust 마이그레이션 로더가 코드에 추가되어야 한다.
  - `integration_job`, `integration_run`, `raw_ingestion_event` 1차 migration SQL 이 추가되어야 한다.
  - 테스트가 통과해야 한다.
  - 오늘자 백로그에 작업 상태가 갱신되어야 한다.
- 작업 결과: 완료. `sqlx::migrate!` 기반 Rust 마이그레이션 로더를 추가했고, `integration_job`, `integration_run`, `raw_ingestion_event` 1차 migration SQL 과 down migration 을 `backend/migrations/` 아래에 생성했다. `TDD` 순서에 따라 migration 파일 존재와 핵심 테이블 정의를 먼저 검증하는 테스트를 추가한 뒤 구현했고, `cargo test --manifest-path backend/Cargo.toml` 로 전체 테스트를 통과시켰다.

## TASK-102 Rust DB 설정 및 런타임 초기화 연결

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/config.rs`
  - `backend/src/app_state.rs`
  - `backend/src/db/...`
  - `backend/src/main.rs`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `TDD` 기준으로 Rust 백엔드 설정 파싱 테스트를 먼저 추가하고, `ALM_BACKEND_DATABASE_URL` 기반 `PgPool` 생성과 선택적 migration 실행 경로를 앱 시작 시점에 연결한다.
- 진행 현황:
  - 2026-04-07 `config.rs` 에서 `DB URL`, 최대 연결 수, migration 자동 실행 플래그가 아직 정의되지 않았음을 확인했다.
  - 2026-04-07 먼저 `Settings::from_map` 기준 테스트를 추가해 `DB URL` 필수 여부와 기본값 동작을 실패 테스트로 고정했다.
  - 2026-04-07 `PgPool` 생성과 migration 실행 책임을 `backend/src/db/pool.rs` 로 분리하고, `main.rs` 에서 설정 로드 후 `PgPool` 생성과 선택적 migration 실행을 거쳐 `AppState` 에 연결하도록 구현했다.
  - 2026-04-07 `cargo test --manifest-path backend/Cargo.toml` 로 전체 테스트를 다시 통과시켰다.
- 완료 기준:
  - Rust 설정 구조에 `DB URL`, 최대 연결 수, migration 실행 플래그가 반영되어야 한다.
  - 앱 시작 시 `PgPool` 생성과 선택적 migration 실행 경로가 연결되어야 한다.
  - 테스트가 통과해야 한다.
  - 관련 구현 체크리스트와 계획 문서가 갱신되어야 한다.
- 작업 결과: 완료. `Settings` 에 `ALM_BACKEND_DATABASE_URL`, `ALM_BACKEND_DATABASE_MAX_CONNECTIONS`, `ALM_BACKEND_AUTO_APPLY_MIGRATIONS` 를 추가하고 테스트로 기본값/필수값 규칙을 고정했다. `db/pool.rs` 에 `PgPool` 생성과 migration 실행 함수를 분리했으며, `main.rs` 에서 설정 로드 후 `PgPool` 연결과 선택적 migration 실행을 거쳐 `AppState` 를 초기화하도록 연결했다. 구현 후 `cargo test --manifest-path backend/Cargo.toml` 가 통과했다.

## TASK-103 Rust 빈 PostgreSQL 부트스트랩 통합 테스트 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/tests/bootstrap.rs`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `TDD` 기준으로 빈 `PostgreSQL` 데이터베이스 기준 부트스트랩과 migration 실행을 검증하는 통합 테스트를 추가하고, 테스트 DB 생성/정리 전략을 환경 변수 기반으로 정리한다.
- 진행 현황:
  - 2026-04-07 현재 테스트는 설정 파싱, 서비스 상태 전이, migration 파일 존재 여부까지만 검증하고 실제 `PostgreSQL` 부트스트랩은 확인하지 않고 있음을 확인했다.
  - 2026-04-07 `backend/tests/bootstrap.rs` 에 `ALM_BACKEND_TEST_DATABASE_ADMIN_URL` 기반 임시 테스트 데이터베이스 생성 헬퍼를 추가했다.
  - 2026-04-07 먼저 빈 테스트 데이터베이스 연결 검증과 migration 적용 후 핵심 테이블 존재 여부를 확인하는 통합 테스트를 작성했다.
  - 2026-04-07 `cargo test --manifest-path backend/Cargo.toml` 로 전체 테스트를 다시 통과시켰다.
- 완료 기준:
  - 빈 `PostgreSQL` 기준 부트스트랩 테스트가 추가되어야 한다.
  - 실제 migration 실행 후 핵심 테이블 존재가 검증되어야 한다.
  - 구현 체크리스트와 계획 문서에 진행 상태가 반영되어야 한다.
- 작업 결과: 완료. `ALM_BACKEND_TEST_DATABASE_ADMIN_URL` 기반 임시 테스트 DB 생성/정리 헬퍼를 추가했고, 빈 `PostgreSQL` 데이터베이스 연결 검증과 `integration_job`, `integration_run`, `raw_ingestion_event` migration 적용 검증 테스트를 추가했다. 구현 후 `cargo test --manifest-path backend/Cargo.toml` 로 전체 테스트가 통과했다.

## TASK-104 `sync-runs` SQL 저장소 전환

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/services/sync_runs.rs`
  - `backend/src/http/routes/sync_runs.rs`
  - `backend/migrations/...`
  - `backend/tests/sync_run_repository.rs`
  - `backend/tests/migrations.rs`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `TDD` 기준으로 `sync-runs` 저장소 전환 테스트를 먼저 추가하고, `integration_run` 에 API 저장 필드를 반영하는 확장 마이그레이션과 `sqlx` 기반 `SyncRunRepository`를 구현해 런타임에서 `db_pool` 존재 시 실제 DB를 사용하도록 전환한다.
- 진행 현황:
  - 2026-04-07 현재 `sync-runs` API 는 인메모리 저장소를 사용하고 있고, `integration_run` 에는 `source_system`, `mode`, `scope`, `reason` 저장 컬럼이 없음을 확인했다.
  - 2026-04-07 먼저 `backend/tests/sync_run_repository.rs` 에 생성, 조회, 재시도, 취소 규칙을 검증하는 리포지토리 통합 테스트를 추가했다.
  - 2026-04-07 `integration_run` 확장 마이그레이션을 추가해 `external_run_id`, `source_system`, `run_mode`, `run_scope`, `reason` 컬럼을 반영했다.
  - 2026-04-07 `SyncRunRepository` 를 구현하고, `sync-runs` 라우트가 `db_pool` 이 있으면 `sqlx` 저장소를 사용하고 없으면 기존 인메모리 저장소를 사용하는 방식으로 연결했다.
  - 2026-04-07 `cargo test --manifest-path backend/Cargo.toml` 로 전체 테스트를 다시 통과시켰다.
- 완료 기준:
  - `sync-runs` 리포지토리 테스트가 추가되어야 한다.
  - `integration_run` 확장 마이그레이션이 추가되어야 한다.
  - 런타임에서 `db_pool` 존재 시 `sqlx` 저장소를 사용해야 한다.
  - 전체 테스트가 통과해야 한다.
- 작업 결과: 완료. `sync-runs` API 에 필요한 저장 필드를 `integration_run` 에 반영하는 확장 마이그레이션을 추가했고, `sqlx` 기반 `SyncRunRepository` 와 리포지토리 통합 테스트를 구현했다. 라우트는 `db_pool` 존재 시 실제 DB를 사용하고, 테스트/임시 구조에서는 기존 인메모리 저장소를 유지하도록 연결했다. 구현 후 `cargo test --manifest-path backend/Cargo.toml` 가 통과했다.

## TASK-105 `ingestion/events` 원시 적재 경로 구현

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/services/raw_ingestion.rs`
  - `backend/src/services/mod.rs`
  - `backend/src/app_state.rs`
  - `backend/src/http/routes/ingestion.rs`
  - `backend/src/http/routes/mod.rs`
  - `backend/src/http/router.rs`
  - `backend/tests/raw_ingestion_repository.rs`
  - `backend/tests/http.rs`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `TDD` 기준으로 `POST /api/v1/ingestion/events` 원시 적재 테스트를 먼저 추가하고, `RawIngestionRepository` 와 수신 라우트를 구현해 원시 적재, 멱등 처리, timestamp 검증을 실제 DB 경로로 연결한다.
- 진행 현황:
  - 2026-04-07 현재 Rust 런타임에는 `sync-runs` 경로만 있고, `POST /api/v1/ingestion/events` 는 아직 구현되지 않았음을 확인했다.
  - 2026-04-07 먼저 `backend/tests/raw_ingestion_repository.rs` 에 원시 적재 저장, 중복 이벤트 멱등 처리, 잘못된 `source_updated_at` 검증 테스트를 추가했다.
  - 2026-04-07 `backend/tests/http.rs` 에 `ingestion/events` 수신 테스트를 추가했다.
  - 2026-04-07 `RawIngestionRepository`, 인메모리 `RawIngestionStore`, `ingestion` 라우트를 추가하고, `db_pool` 이 있으면 실제 DB 원시 적재 경로를 사용하도록 연결했다.
  - 2026-04-07 `cargo test --manifest-path backend/Cargo.toml` 로 전체 테스트를 다시 통과시켰다.
- 완료 기준:
  - `ingestion/events` 리포지토리 테스트가 추가되어야 한다.
  - HTTP 수신 테스트가 추가되어야 한다.
  - 런타임에서 `db_pool` 존재 시 원시 적재와 멱등 처리가 실제 DB 경로로 수행되어야 한다.
  - 전체 테스트가 통과해야 한다.
- 작업 결과: 완료. `POST /api/v1/ingestion/events` 수신 경로와 `RawIngestionRepository` 를 구현했고, `raw_ingestion_event` 에 원시 적재를 저장하면서 중복 이벤트는 같은 `source_event_key` 기준으로 중복 응답을 반환하도록 연결했다. 잘못된 `source_updated_at` 값은 저장 전에 검증하며, 인메모리 폴백도 유지했다. 구현 후 `cargo test --manifest-path backend/Cargo.toml` 가 통과했다.

## TASK-106 `pull` 실행 오케스트레이션 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/services/pull_sync.rs`
  - `backend/src/services/raw_ingestion.rs`
  - `backend/src/services/sync_runs.rs`
  - `backend/src/services/mod.rs`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `TDD` 기준으로 `pull` 실행 오케스트레이션 테스트를 먼저 추가하고, 수동 `pull` 실행이 `sync-run` 생성, `raw_ingestion_event` 적재, 실행 상태 종료 처리까지 이어지는 `PullSyncOrchestrator` 를 구현한다.
- 진행 현황:
  - 2026-04-07 현재 구현은 수동 `sync-run` 등록과 `push` 원시 적재까지만 가능하고, `pull` 실행에서 가져온 레코드를 하나의 실행 단위로 묶는 서비스는 없음을 확인했다.
  - 2026-04-07 먼저 `backend/tests/pull_sync_orchestrator.rs` 에 성공 케이스와 부분 실패 케이스를 검증하는 통합 테스트를 추가했다.
  - 2026-04-07 `RawIngestionRepository` 에 기존 `sync-run` 아래로 원시 적재를 넣는 `create_for_run` 경로를 추가했다.
  - 2026-04-07 `SyncRunRepository` 에 `complete_pull_run` 을 추가해 `completed`/`partially_completed` 상태 종료와 집계 카운트를 업데이트하도록 구현했다.
  - 2026-04-07 `PullSyncOrchestrator` 를 구현해 하나의 실행에서 여러 레코드를 공통 적재 경로로 처리하고 결과 카운트로 실행 상태를 종료했다.
  - 2026-04-07 `cargo test --manifest-path backend/Cargo.toml` 로 전체 테스트를 다시 통과시켰다.
- 완료 기준:
  - `pull` 실행 오케스트레이션 테스트가 추가되어야 한다.
  - 수동 `pull` 실행이 `sync-run` 생성부터 원시 적재, 상태 종료까지 공통 경로로 처리되어야 한다.
  - 성공/부분 실패 카운트가 `integration_run` 에 반영되어야 한다.
  - 전체 테스트가 통과해야 한다.
- 작업 결과: 완료. `PullSyncOrchestrator` 를 추가해 수동 `pull` 실행이 하나의 `sync-run` 아래에서 여러 레코드를 `raw_ingestion_event` 로 적재하고, 처리 결과에 따라 `completed` 또는 `partially_completed` 로 종료되도록 구현했다. 기존 `RawIngestionRepository` 와 `SyncRunRepository` 를 재사용해 `pull`/`push` 가 원시 적재 경로를 공유하도록 만들었고, 통합 테스트로 성공/부분 실패 시나리오를 검증했다.

## TASK-107 외부 시스템 어댑터 레지스트리 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/adapters/mod.rs`
  - `backend/src/app_state.rs`
  - `backend/src/http/routes/ingestion.rs`
  - `backend/src/services/pull_sync.rs`
  - `backend/tests/adapter_integration.rs`
  - `docs/architecture/integration_backend_component_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 외부 시스템 API 호출과 수신 payload 변환을 시스템별 세부 구현으로 분리할 수 있도록 `pull`/`push` 어댑터 인터페이스와 `AdapterRegistry` 를 추가하고, `ingestion` 라우트와 `PullSyncOrchestrator` 가 레지스트리를 통해 시스템별 구현을 찾도록 연결한다.
- 진행 현황:
  - 2026-04-07 현재 `pull`/`push` 구현은 공통 저장 경로는 정리됐지만, 외부 시스템별 세부 호출/수신 변환을 붙일 확장 지점은 없음을 확인했다.
  - 2026-04-07 먼저 `backend/src/adapters/mod.rs` 에 `PullSourceAdapter`, `PushEventAdapter`, `AdapterRegistry` 와 공통 요청/오류 모델을 추가했다.
  - 2026-04-07 `AppState` 에 어댑터 레지스트리를 포함시키고, `ingestion` 라우트는 등록된 `push` 어댑터가 있으면 이를 통해 수신 payload 를 공통 원시 적재 입력으로 변환하도록 연결했다.
  - 2026-04-07 `PullSyncOrchestrator` 에 `run_from_adapter` 를 추가해 등록된 `pull` 어댑터가 외부 시스템 레코드를 반환하면 공통 `sync-run`/원시 적재 경로로 처리되도록 연결했다.
  - 2026-04-07 `backend/tests/adapter_integration.rs` 에 `push` 수신 라우트가 등록된 어댑터를 사용하는지, `pull` 오케스트레이터가 등록된 어댑터를 사용하는지 검증하는 통합 테스트를 추가했다.
  - 2026-04-07 `cargo test --manifest-path backend/Cargo.toml` 로 전체 테스트를 다시 통과시켰다.
- 완료 기준:
  - `pull`/`push` 어댑터 인터페이스와 레지스트리가 추가되어야 한다.
  - `ingestion` 라우트와 `PullSyncOrchestrator` 가 레지스트리를 통해 시스템별 구현을 사용할 수 있어야 한다.
  - 어댑터 통합 테스트가 추가되어야 한다.
  - 전체 테스트가 통과해야 한다.
- 작업 결과: 완료. 시스템별 `pull`/`push` 세부 구현을 분리할 수 있도록 공통 어댑터 인터페이스와 `AdapterRegistry` 를 추가했고, `ingestion` 라우트와 `PullSyncOrchestrator` 를 이 레지스트리에 연결했다. `Jira` 예시 어댑터 기반 통합 테스트를 추가해 수신 변환과 외부 조회 대체 경로를 검증했고, 전체 테스트를 통과시켰다.

## TASK-108 시스템별 concrete adapter 구현 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/adapters/mod.rs`
  - `backend/src/adapters/jira.rs`
  - `backend/src/adapters/bitbucket.rs`
  - `backend/src/adapters/bamboo.rs`
  - `backend/src/adapters/confluence.rs`
  - `backend/src/app_state.rs`
  - `backend/src/http/routes/ingestion.rs`
  - `backend/src/services/pull_sync.rs`
  - `backend/tests/adapter_integration.rs`
  - `backend/tests/http.rs`
  - `docs/architecture/integration_backend_component_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 현재 연계 컨셉으로 제시된 대표 시스템을 기준으로 실제 `pull`/`push` 어댑터 구현을 추가하고, 레지스트리에 기본 등록되도록 연결한다. 각 어댑터는 시스템별 API 응답과 webhook payload 를 공통 원시 적재 입력으로 변환할 수 있어야 하며, 단위 테스트와 통합 테스트로 검증한다.
- 진행 현황:
  - 2026-04-07 `backend/src/adapters/mod.rs` 에 공통 `HttpTransport`, `ReqwestTransport`, `build_default_registry` 를 추가하고 기본 레지스트리 생성 경로를 정리했다.
  - 2026-04-07 `Jira`, `Bitbucket`, `Bamboo`, `Confluence` 용 `pull`/`push` 어댑터 모듈을 각각 추가해 URL 조합, 응답 파싱, webhook payload 변환 로직을 구현했다.
  - 2026-04-07 `AppState` 기본 생성 경로가 기본 어댑터 레지스트리를 사용하도록 연결하고, `ingestion` 라우트와 `PullSyncOrchestrator` 가 이 concrete adapter 를 사용할 수 있게 정리했다.
  - 2026-04-07 시스템별 어댑터 단위 테스트와 레지스트리 기반 통합 테스트를 추가했고, `ingestion` HTTP 테스트 payload 를 concrete `Jira` 어댑터 형식에 맞게 보정했다.
  - 2026-04-07 `cargo fmt --manifest-path backend/Cargo.toml --all`, `cargo test --manifest-path backend/Cargo.toml` 를 실행해 전체 테스트 통과를 확인했다.
- 완료 기준:
  - 대표 외부 시스템 기준 concrete `pull`/`push` 어댑터가 구현되어야 한다.
  - 기본 레지스트리에서 concrete adapter 를 조회할 수 있어야 한다.
  - 단위 테스트와 통합 테스트가 concrete adapter 기준으로 통과해야 한다.
  - 관련 아키텍처 문서와 오늘자 백로그가 같은 상태를 반영해야 한다.
- 작업 결과: 완료. `Jira`, `Bitbucket`, `Bamboo`, `Confluence` 기준 concrete `pull`/`push` 어댑터를 추가했고, 공통 `ReqwestTransport` 위에서 시스템별 API 응답과 webhook payload 를 공통 원시 적재 입력으로 변환하도록 구현했다. 기본 `AdapterRegistry` 와 `AppState` 에 연결했고, 어댑터 단위 테스트, 레지스트리 통합 테스트, 전체 Rust 테스트를 모두 통과시켰다.

## TASK-109 외부 시스템 어댑터 연동 가이드 작성

- 상태: done
- 우선순위: medium
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `docs/architecture/integration_adapter_onboarding_guide.md`
  - `docs/architecture/README.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 새 외부 시스템을 `pull`/`push` 어댑터로 연동하는 방법을 개발자 가이드로 정리한다. 공통 인터페이스, 파일 위치, 구현 순서, 테스트 기준, 레지스트리 등록 방법, 설정 연결 원칙이 포함되어야 한다.
- 진행 현황:
  - 2026-04-07 현재 concrete adapter 구현과 공통 인터페이스 구조를 기준으로 개발자용 온보딩 문서가 필요하다고 판단했다.
  - 2026-04-07 `backend/src/adapters/mod.rs`, 기존 `Jira` 예시 adapter, `ingestion` 라우트, `PullSyncOrchestrator` 구조를 다시 확인해 실제 코드 기준 절차를 정리했다.
  - 2026-04-07 새 가이드 문서에 `pull`/`push` 구현 절차, `source_event_key` 설계 기준, `TDD` 테스트 순서, registry 등록, 환경변수 기반 설정과 후속 `integration_endpoint` 연계 원칙을 정리했다.
- 완료 기준:
  - 새 외부 시스템 adapter 를 붙이는 절차 문서가 생성되어야 한다.
  - 개발자가 바로 따라할 수 있는 파일 위치, 구현 단계, 테스트 기준이 포함되어야 한다.
  - 아키텍처 위키와 계획 문서, 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. concrete adapter 구현 코드를 기준으로 외부 시스템 adapter 연동 가이드를 작성했고, `pull`/`push` 구현 절차, 레지스트리 등록, 테스트 우선 순서, 설정 연결 원칙을 문서로 고정했다. 문서 작업만 수행했으며 별도 테스트는 추가하지 않았다.

## TASK-110 어댑터 설정 레코드 builder 재구성

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/adapters/mod.rs`
  - `backend/src/adapters/jira.rs`
  - `backend/src/adapters/bitbucket.rs`
  - `backend/src/adapters/bamboo.rs`
  - `backend/src/adapters/confluence.rs`
  - `docs/architecture/integration_adapter_onboarding_guide.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 기본 어댑터 registry 생성을 환경변수 직접 등록 방식에서 `AdapterEndpointConfig` 기반 builder 방식으로 재구성한다. 이 경로는 현재 환경변수 기반 기본 registry 와 후속 `integration_endpoint`/`integration_credential` 로더가 함께 재사용할 수 있어야 한다.
- 진행 현황:
  - 2026-04-07 현재 concrete adapter 는 구현됐지만, 기본 registry 생성이 각 시스템의 `from_env()` 를 직접 호출하는 구조라 후속 설정 로더와 조립 경로를 공유하기 어렵다고 판단했다.
  - 2026-04-07 `AdapterEndpointConfig` 와 `build_registry_from_endpoint_configs()` 를 추가하고, source system 별 `pull`/`push` adapter 생성 책임을 공통 builder 로 모았다.
  - 2026-04-07 각 concrete `pull` adapter 에 공통 생성자 `new(transport, base_url, bearer_token)` 를 추가하고, `from_env()` 는 보조 진입점으로 남겼다.
  - 2026-04-07 builder 단위 테스트로 capability 등록과 미지원 시스템 실패를 먼저 고정했고, 전체 `cargo test --manifest-path backend/Cargo.toml` 통과를 확인했다.
  - 2026-04-07 adapter 온보딩 가이드와 백엔드 설계 문서에 새 조립 경로와 후속 설정 로더 방향을 반영했다.
- 완료 기준:
  - 설정 레코드 기반 registry builder 가 추가되어야 한다.
  - 기본 registry 생성이 이 builder 를 재사용해야 한다.
  - builder 단위 테스트와 전체 Rust 테스트가 통과해야 한다.
  - 관련 아키텍처 문서와 백로그가 갱신되어야 한다.
- 작업 결과: 완료. `AdapterEndpointConfig` 기반 registry builder 를 추가해 환경변수 기반 기본 registry 와 후속 설정 로더가 같은 조립 경로를 공유할 수 있게 정리했다. concrete `pull` adapter 공통 생성자를 추가했고, builder 단위 테스트와 전체 Rust 테스트를 모두 통과시켰다.

## TASK-111 DB 기반 어댑터 설정 로더 연결

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일: 2026-04-07
- 담당: Codex
- 영향 문서:
  - `backend/src/adapters/config_loader.rs`
  - `backend/src/adapters/mod.rs`
  - `backend/src/main.rs`
  - `backend/migrations/20260407120000_create_integration_connection_tables.up.sql`
  - `backend/migrations/20260407120000_create_integration_connection_tables.down.sql`
  - `backend/tests/adapter_config_loader.rs`
  - `backend/tests/migrations.rs`
  - `docs/architecture/integration_adapter_onboarding_guide.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/architecture/architecture_drafting_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `integration_system`, `integration_endpoint`, `integration_credential` 을 실제 마이그레이션으로 추가하고, 이 설정을 읽어 `AdapterEndpointConfig` 와 `AdapterRegistry` 를 구성하는 `DB` 기반 로더를 구현한다. 앱 시작 시 DB 설정이 존재하면 이 로더를 우선 사용하고, 설정이 없거나 실패하면 환경변수 기반 기본 registry 로 fallback 한다.
- 진행 현황:
  - 2026-04-07 현재 adapter 조립 경로는 `AdapterEndpointConfig` builder 까지 정리됐지만, 실제 `DB` 설정을 읽는 로더는 없음을 확인했다.
  - 2026-04-07 `integration_system`, `integration_endpoint`, `integration_credential` 최소 테이블 마이그레이션을 추가했다.
  - 2026-04-07 `DbAdapterConfigLoader` 를 추가해 활성 endpoint 와 유효 credential 을 읽고 `AdapterEndpointConfig` 목록 또는 `AdapterRegistry` 를 구성하도록 구현했다.
  - 2026-04-07 앱 시작 시 `DbAdapterConfigLoader` 결과가 있으면 DB 기반 registry 를 사용하고, 비어 있거나 오류가 있으면 기존 기본 registry 로 fallback 하도록 `main.rs` 를 연결했다.
  - 2026-04-07 로더 통합 테스트를 먼저 추가해 DB 설정 조회와 registry 구성 경로를 검증했고, 전체 `cargo test --manifest-path backend/Cargo.toml` 통과를 확인했다.
- 완료 기준:
  - 연결 설정 테이블 마이그레이션이 추가되어야 한다.
  - DB 기반 adapter 설정 로더가 구현되어야 한다.
  - 앱 시작 시 로더를 통해 registry 를 구성하는 경로가 연결되어야 한다.
  - 로더 테스트와 전체 Rust 테스트가 통과해야 한다.
  - 관련 문서와 백로그가 갱신되어야 한다.
- 작업 결과: 완료. `integration_system`, `integration_endpoint`, `integration_credential` 최소 마이그레이션과 `DbAdapterConfigLoader` 를 추가했고, 앱 시작 시 DB 설정 기반 adapter registry 를 우선 사용하도록 연결했다. DB 설정이 없을 때는 기존 환경변수 기반 registry 로 fallback 하며, 로더 통합 테스트와 전체 Rust 테스트를 모두 통과시켰다.

## TASK-112 자격증명 복호화 서비스 연결

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일:
- 담당: Codex
- 영향 문서:
  - `backend/src/adapters/config_loader.rs`
  - `backend/src/adapters/mod.rs`
  - `backend/src/main.rs`
  - `backend/src/lib.rs`
  - `backend/src/security/`
  - `backend/tests/adapter_config_loader.rs`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/architecture/integration_auth_and_signing_policy_draft.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `integration_credential.secret_ciphertext` 를 실제 복호화해 adapter 구성에 주입하는 비밀정보 서비스를 추가하고, 앱 시작 시 DB 로더가 이 복호화 계층을 사용하도록 연결한다.
- 진행 현황:
  - 2026-04-07 최근 완료 작업 기준으로 DB 기반 adapter 설정 로더까지 연결됐지만, `secret_ciphertext` 는 아직 복호화 없이 그대로 `bearer_token` 으로 전달되고 있음을 확인했다.
  - 2026-04-07 `enc-v1` 형식 복호화와 레거시 평문 호환을 함께 지원하는 `EnvSecretCipher` 를 추가했다.
  - 2026-04-07 `DbAdapterConfigLoader` 가 `secret_key_version` 과 함께 자격증명을 복호화해 adapter 및 `push` 서명 검증 secret 에 재사용하도록 연결했다.
  - 2026-04-07 관련 설계 문서와 구현 체크리스트를 갱신했다.
- 완료 기준:
  - 자격증명 복호화 서비스가 추가되어야 한다.
  - DB 로더가 복호화된 secret 을 adapter 구성에 사용해야 한다.
  - 관련 테스트와 전체 Rust 테스트가 통과해야 한다.
  - 설계 문서와 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. `secret_ciphertext` 복호화 서비스와 DB 로더 연계를 추가했고, 복호화된 secret 을 adapter 구성과 `push` 서명 검증에 함께 사용할 수 있도록 정리했다. 다만 현재 실행 환경에는 `cargo`/`rustc` 가 없어 Rust 테스트는 실제로 실행하지 못했다.

## TASK-113 원시 적재 최신성·참조 보류 테스트 보강

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일:
- 담당: Codex
- 영향 문서:
  - `backend/src/services/raw_ingestion.rs`
  - `backend/tests/raw_ingestion_repository.rs`
  - `docs/architecture/integration_data_ingestion_sequence_draft.md`
  - `docs/architecture/integration_backend_implementation_rollout_and_checklist_draft.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: 중복 이벤트 외에 늦게 도착한 이벤트와 참조 미충족 이벤트 시나리오 테스트를 추가하고, 원시 적재 단계의 상태 판정 기준을 코드와 문서에 반영한다.
- 진행 현황:
  - 2026-04-07 현재 `RawIngestionRepository` 는 중복과 timestamp 형식 오류만 다루고 있으며, 최신성 저하 이벤트와 `pending_reference` 시나리오는 아직 테스트와 코드에 반영되지 않았음을 확인했다.
  - 2026-04-07 늦게 도착한 이벤트와 참조 미충족 이벤트 통합 테스트를 추가하고, 원시 적재 단계에서 `stale`, `pending_reference` 상태를 판정하도록 저장 로직을 보강했다.
  - 2026-04-07 구현 체크리스트와 수집/보안 문서에 상태 판정 기준을 반영했다.
- 완료 기준:
  - 늦게 도착한 이벤트와 참조 미충족 이벤트 테스트가 추가되어야 한다.
  - 원시 적재 상태 판정 로직이 테스트와 일치해야 한다.
  - 관련 문서와 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. `RawIngestionRepository` 가 중복 외에 늦게 도착한 이벤트를 `stale`, 참조 미충족 payload 를 `pending_reference` 로 저장하도록 보강했고 관련 테스트를 추가했다. 다만 현재 실행 환경에는 `cargo`/`rustc` 가 없어 Rust 테스트는 실제로 실행하지 못했다.

## TASK-114 `ingestion/events` 서명 검증 추가

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일:
- 담당: Codex
- 영향 문서:
  - `backend/src/http/routes/ingestion.rs`
  - `backend/src/app_state.rs`
  - `backend/src/http/router.rs`
  - `backend/tests/http.rs`
  - `docs/architecture/integration_auth_and_signing_policy_draft.md`
  - `docs/architecture/integration_backend_api_and_batch_contract_draft.md`
  - `docs/architecture/integration_backend_design_plan.md`
  - `docs/operations/backlog/2026-04-07.md`
- 작업 내용: `POST /api/v1/ingestion/events` 에 `HMAC` 기반 서명 검증과 필수 헤더 체크를 추가하고, 실패/성공 시나리오 테스트와 정책 문서를 갱신한다.
- 진행 현황:
  - 2026-04-07 현재 `ingestion/events` 는 본문 검증과 적재만 수행하며, 인증/서명 검증은 아직 연결되지 않았음을 확인했다.
  - 2026-04-07 `IngestionAuthRegistry` 와 `HMAC-SHA256` 서명 검증 유틸을 추가하고, `POST /api/v1/ingestion/events` 에 필수 헤더 검사와 서명 검증을 연결했다.
  - 2026-04-07 성공, 누락 헤더, 잘못된 서명 시나리오 테스트를 추가하고 API 계약 및 보안 정책 문서를 갱신했다.
- 완료 기준:
  - 필수 헤더와 `HMAC` 서명 검증이 구현되어야 한다.
  - 성공, 누락 헤더, 잘못된 서명 테스트가 추가되어야 한다.
  - 관련 문서와 오늘자 백로그가 갱신되어야 한다.
- 작업 결과: 완료. `ingestion/events` 에 `X-Source-System`, `X-Signature`, `X-Signature-Timestamp`, `X-Idempotency-Key` 기반 검증을 추가했고, 서명 문자열은 `timestamp + method + path + body_hash` 규칙으로 맞췄다. source 별 secret 이 없는 환경은 기존 호환을 위해 무서명 수신을 허용한다. 다만 현재 실행 환경에는 `cargo`/`rustc` 가 없어 Rust 테스트는 실제로 실행하지 못했다.

## TASK-115 로컬 개발 및 테스트 환경 구축

- 상태: done
- 우선순위: high
- 요청일: 2026-04-07
- 완료일:
- 담당: Codex
- 영향 문서:
  - `docs/operations/backlog/2026-04-07.md`
  - `README.md`
  - `backend/`
  - `docker-compose.yml`
  - `.env.example`
- 작업 내용: 현재 환경에서 Rust 개발과 PostgreSQL 기반 테스트를 실제로 돌릴 수 있도록 로컬 개발 환경을 구성한다. `brew` 기반 컨테이너 런타임 설치, `PostgreSQL` 컨테이너 실행 경로, 환경변수 예시, 검증 명령이 포함되어야 한다.
- 진행 현황:
  - 2026-04-07 현재 `brew`, `git`, `npm`, `cargo`, `rustc` 가 사용 가능함을 확인했다.
  - 2026-04-07 `docker`/`colima` 는 아직 설치되어 있지 않아, `brew` 기반 설치와 저장소 내 `PostgreSQL` 실행 진입점 구성이 다음 단계라고 판단했다.
  - 2026-04-07 `brew install colima docker docker-compose` 로 컨테이너 런타임과 CLI를 설치했고, `~/.docker/config.json` 에 Compose 플러그인 경로를 반영했다.
  - 2026-04-07 `colima start` 로 Docker 런타임을 기동하고 저장소 루트에 `docker-compose.yml`, `.env.example`, `Makefile` 대상, `README.md` 실행 절차를 추가했다.
  - 2026-04-07 `docker compose up -d postgres` 로 `PostgreSQL` 컨테이너를 기동했고, `.env.example` 환경변수 기준으로 `cargo test --manifest-path backend/Cargo.toml` 전체 통과를 확인했다.
- 완료 기준:
  - 컨테이너 런타임이 설치되어 `docker` 명령을 사용할 수 있어야 한다.
  - `PostgreSQL` 개발/테스트용 실행 진입점이 저장소에 추가되어야 한다.
  - 백엔드 테스트에 필요한 환경변수와 실행 절차가 정리되어야 한다.
  - 실제 검증 결과가 백로그에 기록되어야 한다.
- 작업 결과: 완료. 로컬 환경에서 `cargo`, `docker`, `docker compose`, `colima` 를 사용할 수 있게 구성했고, 저장소 루트의 `docker-compose.yml` 과 `.env.example` 을 기준으로 `PostgreSQL` 개발/테스트 환경을 재현할 수 있게 했다. `colima` 기동 후 `docker compose up -d postgres` 와 `cargo test --manifest-path backend/Cargo.toml` 를 실행해 전체 Rust 테스트가 통과했다.
